# Run log: 2026-09-08

**Catch-up run, Tuesday of week 01.** Started by
`Relaunch-MissedTasks.ps1` because the scheduled draft run of 2026-09-08
did not run or did not finish while the computer was unavailable. The
operator instruction was: process the missed dates first, in date order,
skip any row already at `image_ready` or later, then handle today's row if
it is due and not yet done. Nothing was drafted, quality-passed, imaged,
published, built or committed. No question was asked; every decision below
was taken from `CLAUDE.md`, `SPEC.md` and `RUNBOOK.md`.

| Field | Value |
|---|---|
| Brief | 01A (Anchor, `briefs/2026-09-08-china-distributor-vs-own-store.md`) |
| Slot | Anchor |
| Output | `output/china-distributor-vs-own-store.md` (already exists, already published) |
| Asset (if any) | none (week 01 carries no asset) |
| Body word count | n/a for this run |
| Body char count | n/a for this run |
| Status reached | no change: row already `published` |

## Slot selection

- Read `CLAUDE.md`, `SPEC.md`, `RUNBOOK.md` and `schedule.csv` before
  deciding. `briefs/PREAMBLE.md` and the source files were not needed
  because no row qualified for drafting.
- Missed date processed first: 2026-09-08 (Tuesday). Its only row is 01A,
  status `published` since the forced test run of 2026-09-04 (see
  `logs/2026-09-04.md`, commit 67e9380 `feat(insights): publish
  china-distributor-vs-own-store`). Verified in the repo, not assumed:
  `src/content/insights/china-distributor-vs-own-store.md`,
  `src/content/insights-fr/distributeur-ou-boutique-en-propre-en-chine.md`,
  `src/content/insights-de/china-distributor-vs-own-store.md`,
  `src/content/insights-es/distribuidor-o-tienda-propia-china.md` and
  `public/Images/insights/china-distributor-vs-own-store.webp` all exist.
  `published` is later than `image_ready`, so the row was skipped per the
  operator instruction.
- The earlier row of the week, 01S (2026-09-07), was also checked: status
  `skipped`, kill condition fired, logged in `logs/2026-09-07.md`. Nothing
  to redo.
- Today's row: today is also 2026-09-08, so today's row is the same 01A row.
  It is due and already done. No draft.
- Decision on drafting ahead: the next `not_started` row with a brief is
  01L (`2026-09-09`, Ledger A, footwear). `RUNBOOK.md` step 2 only falls
  through to the oldest `not_started` row when today has no row at all.
  Today has a row, so the fallback does not apply, and the operator
  instruction limits this run to the missed dates plus today. 01L stays
  `not_started` for its own scheduled run on 2026-09-09 at 01:00 Shanghai.
- Kill condition checked: not applicable (Anchors always ship, and this one
  already has).

## Research note (written before iteration 1)

Not applicable. No body copy was written and no figure was researched.

## Iterations (createarticle)

Not run.

## Quality pass (content-quality-us)

Not run.

## Image

Not generated. The hero for 01A already exists at
`public/Images/insights/china-distributor-vs-own-store.webp`.

## Sources

- New figures added to the ledger: none.

## Flags

- TODO: client sign-off items: none.
- Territory hand-offs: none.
- Conflicts between the brief and the live site: none checked this run.
- Internal link targets that do not exist yet: none checked this run.
- Schedule note: 01A's `publish_date` is 2026-09-08 but the piece went
  live on 2026-09-04 through the forced test. The row's `published_on`
  already records 2026-09-04, so nothing was edited.
- Uncommitted working tree: `editorial/RUNBOOK.md`, `editorial/schedule.csv`,
  `editorial/scripts/register-tasks.ps1` (modified) and
  `editorial/logs/2026-09-07.md` (untracked) were already pending before
  this run, plus this log. Not committed, per the run instruction.

## SEO counts (after the quality pass)

| Field | Chars or words | Ceiling | Pass |
|---|---|---|---|
| Title | n/a | 52 | n/a |
| Meta description | n/a | 152 | n/a |
| Excerpt | n/a | 25 words | n/a |

## Publish (fill in when step 4 runs)

- Not applicable. No row moved to `image_ready`, so the next scheduled
  publish run has nothing new to pick up from this date.
- Model used: Claude Fable 5.1 (the most capable model available), for the
  whole run.
- Runbook substitutions: none.

---

## Publish run: 2026-09-08 (catch-up)

**Catch-up publish run**, started by `Relaunch-MissedTasks.ps1` because the
scheduled publish run of Tuesday 2026-09-08 did not run or did not finish
while the computer was unavailable. Unattended: no question was asked. This
run is the explicit build request that `.claude/CLAUDE.md` section 19
requires, but no build was triggered because nothing reached the build step.

**Result: nothing was due, so nothing was published.** No
`/createblogarticle`, no locale propagation, no `/deep-translate`, no
`npm run build`, no `npx astro check`, no commit, no push, no email.

### The scan

- Read `editorial/CLAUDE.md`, `editorial/SPEC.md` and `editorial/RUNBOOK.md`
  first, then parsed all 208 rows of `editorial/schedule.csv`.
- Status counts across the whole schedule: 1 `skipped`, 1 `published`,
  206 `not_started`. **Zero rows at `image_ready`,** on any date.
- Rows whose `publish_date` is 2026-09-08 or earlier:

| Date | Brief | Status | Why it was not published |
|---|---|---|---|
| 2026-09-07 | 01S (Signal) | `skipped` | Kill condition fired on the draft run: no platform or regulator change in the window moved a number. See `logs/2026-09-07.md`. |
| 2026-09-08 | 01A (Anchor, china-distributor-vs-own-store) | `published` | Already live since the forced test run of 2026-09-04, commit 67e9380. `published` is later than `image_ready`, so the row is not a publish candidate. |

- The next rows, 01L (2026-09-09, Ledger A, footwear) and 01T (2026-09-10,
  Teardown), are `not_started` and not yet due. They are not publish
  candidates.
- Checked the repo, not just the CSV: `editorial/output/` holds one file,
  `china-distributor-vs-own-store.md`, which is the piece already published.
  `editorial/output/guides/` and `editorial/output/reports/` are empty. No
  orphaned draft is waiting for a publish step.
- The catch-up draft run earlier today (12:21) reached the same conclusion
  from the drafting side and moved no row to `image_ready`, so this publish
  run had nothing new to pick up.

### Notification

No email sent. `scripts/notify-publish.mjs` runs only after a successful
push, and the build-failure email applies only when a build or check
actually failed. Neither happened: there was no article to process.

### Working tree

Left uncommitted, as before this run: `editorial/RUNBOOK.md`,
`editorial/schedule.csv`, `editorial/scripts/register-tasks.ps1`,
`editorial/scripts/run-daily.ps1` (modified), plus
`editorial/logs/2026-09-07.md` and `editorial/logs/2026-09-08.md`
(untracked). The publish rules commit only what an article touched, so
nothing was staged here.

### Next

The 2026-09-09 draft run (01L, footwear costs) is the next chance for a row
to reach `image_ready`; that day's publish run picks it up.

- Model used: Claude Opus 5 (the most capable model available), for the
  whole run.
- Runbook substitutions: none.
