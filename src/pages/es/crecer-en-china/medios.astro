---
import Layout from '../../../layouts/Layout.astro';

const whereYouAre = [
  {
    kicker: 'Lanzamiento',
    title: 'Está lanzando en China',
    body: 'Sin cuentas, sin datos, sin historial. Le hace falta alguien que arme el conjunto y lo ponga a correr sin quemar los tres primeros meses averiguando qué hace cada plataforma.',
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M14 4c4 0 6 2 6 6-4 0-6 2-6 6-4 0-6-2-6-6 4 0 6-2 6-6z"/><circle cx="14" cy="10" r="1.5"/><path d="M8 16l-3 3M11 19l-2 2M5 13l-2 2"/></svg>`,
  },
  {
    kicker: 'Sin retorno',
    title: 'Ya está invirtiendo y no funciona',
    body: 'Las campañas están en directo, quizá con otra agencia, y los números no cuadran. Le hace falta un equipo que lo desmonte y lo vuelva a armar bien.',
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M12 9v4"/><path d="M12 17h.01"/><path d="M10.3 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/></svg>`,
  },
  {
    kicker: 'Escalado',
    title: 'Está escalando',
    body: 'Lo básico ya tira. Ahora quiere apretar: plataformas nuevas, presupuestos mayores, livestreaming, medios eCommerce. Le hacen falta manos sénior, no tres júniors más.',
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 17 9 11 13 15 21 7"/><polyline points="15 7 21 7 21 13"/></svg>`,
  },
];

const brandReach = [
  {
    icon: 'wechat',
    title: 'WeChat Ads',
    body: 'Anuncios en Moments, banners en Cuentas Oficiales, ubicaciones en Mini Programas, anuncios de búsqueda. Si tiene datos de CRM, montamos la segmentación con ellos. Si no, tiramos de lookalikes. La creatividad se hace en casa, en chino, por gente que escribe para lectores chinos.',
  },
  {
    icon: 'video',
    title: 'Douyin Ads',
    body: 'Vídeo in-feed, TopView, Brand Challenges, anuncios de búsqueda y el conjunto Qianchuan completo para live commerce. Probamos creatividades cada semana. Cortamos lo que falla y metemos dinero en lo que tira. La mayoría de clientes ven su CPM estabilizarse al primer mes.',
  },
  {
    icon: 'heart',
    title: 'RED (Xiaohongshu) Ads',
    body: 'Anuncios in-feed, anuncios de búsqueda, siembra con KOC, amplificación con KOL. RED premia el contenido que parece escrito por una persona de carne y hueso. Ese contenido lo producimos nosotros. Las publicaciones que huelen a anuncio se hunden.',
  },
  {
    icon: 'search',
    title: 'Baidu SEM',
    body: 'Anuncios de búsqueda, Brand Zone de Baidu, red de display, SEO. Baidu sigue importando para búsquedas de alta intención, sobre todo en B2B, automóvil, finanzas, educación y lujo.',
  },
  {
    icon: 'film',
    title: 'Bilibili y Kuaishou',
    body: 'Audiencias más jóvenes, ciudades más pequeñas, videojuegos, anime, lifestyle. Las usamos cuando casan con la marca. No rellenamos planes con canales que no le hacen falta.',
  },
  {
    icon: 'target',
    title: 'Programática y display',
    body: 'Tencent Ads, ByteDance Ads, Alibaba UniDesk. Retargeting entre plataformas, modelos lookalike y caps de frecuencia que respetan la atención del usuario.',
  },
];

const ecommerceMedia = [
  {
    icon: 'store',
    title: 'Tmall Ads',
    body: 'Zhitongche, el "tren directo", para anuncios de producto por palabra clave. Wanxiangtai, "Súper Expreso", para puja dirigida por IA en toda la red Alimama. Diamond Booth para banners de alto tráfico. Planificamos el gasto contra objetivos de ventas y retorno, no contra impresiones. Eso cubre el mantenimiento diario, la rampa de preventa y el empuje a fondo en los grandes festivales: 618 en junio, Double 11 en noviembre y Año Nuevo Chino.',
  },
  {
    icon: 'cart',
    title: 'JD Ads',
    body: 'JD Express, o Kuaiche, para anuncios de producto guiados por búsqueda. Haitou, "Lanzamiento al Mar", para ubicaciones optimizadas por IA en los pools de tráfico de JD. JD Zhanwai para retargeting fuera de plataforma. El comprador de JD se comporta distinto al de Tmall: más decidido, más fiel a la marca y con peso en electrónica y hogar. Pujamos contra eso, no contra una plantilla genérica.',
  },
  {
    icon: 'live',
    title: 'Douyin eCommerce Ads',
    body: 'Qianchuan es el motor. Empuja tráfico pagado a vídeos de producto, salas en directo y páginas de tienda. Lo manejamos para clientes con el margen correcto y un ritmo de contenido constante: impulsando salas en directo en horas pico, metiendo dinero detrás de vídeos que ya tiran, montando campañas que llevan al espectador del interés al checkout.',
  },
  {
    icon: 'note',
    title: 'RED eCommerce Ads',
    body: 'Juguang promociona notas vinculadas a producto y emisiones en directo. La parte comercial de RED es más joven que la de Douyin pero crece rápido: skincare, moda, fragancia de nicho, hogar. Amplificar contenido KOC sembrado suele ganar a un anuncio de marca directo.',
  },
];

const whyStay = [
  {
    title: 'Un equipo, de principio a fin',
    body: 'Quien escribe su plan de medios es quien compra sus anuncios. Su estratega sénior no desaparece tras el kickoff dejándole con alguien que no conoce.',
  },
  {
    title: 'Medios y tienda, juntos',
    body: 'La mayoría de agencias parten la compra de medios y la operación de tienda entre dos equipos. A veces entre dos empresas. Por esa grieta se fuga el retorno. Nosotros los mantenemos bajo un mismo techo, así tráfico, conversión y trabajo de ficha avanzan en bloque.',
  },
  {
    title: 'Usted es dueño de todo',
    body: 'Las cuentas, los datos, los píxeles, las audiencias. Todo a su nombre, todo suyo. Si decide irse, se lleva el lote bajo el brazo.',
  },
  {
    title: 'Cumplimiento de fábrica',
    body: 'Ajustamos lo legal antes de que nada salga a directo. En China la otra cara son anuncios rechazados, cuentas suspendidas y multas, y hemos limpiado bastantes como para tomárnoslo en serio.',
  },
];

const weeks = [
  {
    name: 'Semana 1',
    tag: 'Auditoría y KPI',
    tabId: 'week-1',
    pill: 'Semana 1',
    title: 'Auditoría y KPI',
    desc: 'Repasamos su setup actual, sus activos, sus datos. Fijamos KPI que digan algo de verdad, no los que quedan bien en un informe.',
    outcome: 'Una línea base limpia. Sabe exactamente qué funciona hoy, qué fuga presupuesto y qué vamos a arreglar primero.',
    image: '/Images/grow-in-china/media/process-week1-audit.png',
    imageAlt: 'Dos personas frente a un portátil revisando una cuenta de anuncios existente, una señala la pantalla, impresos con anotaciones en chino sobre la mesa',
    does: [
      'Auditoría de cuentas en todas las plataformas activas',
      'Revisión de creatividad, segmentación y puja',
      'Definición de KPI atados a ingresos, no a métricas vanidosas',
      'Chequeo de salud del tracking y la atribución',
    ],
    deliverables: ['Informe de auditoría con arreglos priorizados', 'Marco de KPI acordado por escrito'],
  },
  {
    name: 'Semana 2',
    tag: 'Montar el plan',
    tabId: 'week-2',
    pill: 'Semana 2',
    title: 'Montar el plan',
    desc: 'Armamos el plan: mix de plataformas, reparto de presupuesto, briefing creativo y cómo vamos a medirlo todo.',
    outcome: 'Un plan de medios que puede defender de verdad en interno, con la cuenta detrás de cada línea.',
    image: '/Images/grow-in-china/media/process-week2-plan.png',
    imageAlt: 'Un plan de medios anotado a mano sobre papel o pizarra, con nombres de plataformas chinas visibles como notas manuscritas',
    does: [
      'Mix de plataformas y reparto de presupuesto por fase',
      'Briefing creativo para cada plataforma',
      'Plan de tests, plan de medición y ritmo de reporting',
      'Prechequeo de cumplimiento para cada claim y titular',
    ],
    deliverables: ['Dossier del plan de medios', 'Briefing creativo y calendario de producción'],
  },
  {
    name: 'Semana 3',
    tag: 'Salir a directo',
    tabId: 'week-3',
    pill: 'Semana 3',
    title: 'Salir a directo',
    desc: 'Salimos a directo. Creatividad producida, cuentas montadas, tracking en su sitio.',
    outcome: 'Campañas corriendo en las plataformas que importan, con el tracking demostrando ya que funcionan.',
    image: '/Images/grow-in-china/media/process-week3-golive.png',
    imageAlt: 'Un comprador de medios configurando una campaña en una plataforma china de anuncios, brillo de pantalla, atardecer, concentrado',
    does: [
      'Creatividad producida y adaptada a la spec de cada plataforma',
      'Cuentas de anuncios dadas de alta a su nombre',
      'Píxeles de tracking, UTM y conversiones de plataforma activas',
      'Puja, ritmo de presupuesto y caps de frecuencia desde el día uno',
    ],
    deliverables: ['Campañas activas en las plataformas previstas', 'Confirmación de tracking e informe de ritmo'],
  },
  {
    name: 'Semana 4 en adelante',
    tag: 'Optimizar y reportar',
    tabId: 'week-4',
    pill: 'Continuo',
    title: 'Optimizar y reportar',
    desc: 'Optimizamos cada semana y reportamos cada mes. Cuando los datos piden cambiar el plan, lo cambiamos. No nos quedamos sentados en un setup solo porque fue el que le presentamos.',
    outcome: 'Una operación de medios que funciona y se afila cada mes, con los números detrás.',
    image: '/Images/grow-in-china/media/process-week4-optimize.png',
    imageAlt: 'Una revisión semanal: dos colegas frente a un monitor con gráficas de rendimiento, en plena conversación, uno con un café en la mano',
    does: [
      'Optimización semanal de pujas, presupuesto y creatividad',
      'Revisión mensual con estratega sénior y comprador',
      'Planificación de rampa de festival y debrief post-festival',
      'Refresco creativo trimestral y reconstrucción de audiencias',
    ],
    deliverables: ['Dashboard en vivo con los mismos números que vemos nosotros', 'Informe mensual y revisión trimestral de negocio'],
  },
];

const compliance = [
  {
    title: 'La prohibición del superlativo',
    body: 'La Ley de Publicidad china prohíbe las afirmaciones absolutas. Nada de "el mejor", "número uno" ni "de máxima categoría". Las multas se cuentan en cientos de miles de RMB, y que la afirmación sea cierta no entra en la ecuación.',
  },
  {
    title: 'Las normas de livestreaming de 2026',
    body: 'Novedad de este año: grabaciones guardadas tres años, prohibición de superlativos de precio sin prueba a mano y responsabilidad compartida para cualquier agencia que toque interacciones falsas.',
  },
  {
    title: 'Datos y PIPL',
    body: 'Sacar datos de clientes chinos del país es una transferencia regulada, no la opción por defecto. Eso condiciona qué puede medir y dónde pueden vivir esos datos.',
  },
];

const faqs = [
  {
    cat: 'Operación',
    q: '¿Quién es dueño de las cuentas y de los datos?',
    a: 'Usted. Damos de alta las cuentas de Tmall, Douyin, Baidu y WeChat al nombre de su marca siempre que la plataforma lo permita. Píxeles, audiencias, historial de campañas, datos de rendimiento, todo es suyo. Si deja de trabajar con nosotros, se lleva el lote, sin drama de salida. Lo preguntamos primero porque suele ser lo primero que pregunta un cliente al que ya le hicieron la jugada antes.',
  },
  {
    cat: 'Coste',
    q: '¿Cuál es el presupuesto mínimo para hacer medios en China?',
    a: 'Realista: unos 100.000 RMB al mes, llámelo 14.000 dólares, para llevar una plataforma como toca, con creatividad y optimización detrás. Puede gastar menos. Pero no aprenderá mucho. Para un lanzamiento multiplataforma cuente con entre 500.000 y 2 millones de RMB durante los tres primeros meses. Trátelos como rangos de trabajo, no como cotización. Su categoría los mueve, a veces bastante.',
  },
  {
    cat: 'Coste',
    q: '¿Cuánto cuesta hacer publicidad en WeChat?',
    a: 'Los anuncios en Moments arrancan en torno a 50-120 RMB CPM, unos 7-16 dólares, y la segmentación apretada lo sube. El gasto mínimo de campaña en Moments suele ser 50.000 RMB. Los anuncios en artículos de Cuenta Oficial y los banners de Mini Programas entran más bajo, desde unos 15.000 RMB. Industria, segmentación y calidad creativa mueven la cifra.',
  },
  {
    cat: 'Coste',
    q: '¿Cuánto cuesta hacer publicidad en Douyin?',
    a: 'El CPM de vídeo in-feed suele moverse entre 30 y 60 RMB, unos 4-8 dólares. El mínimo técnico son 50 RMB al día por grupo de anuncios, pero eso no es una campaña real. Presupueste al menos 10.000 RMB para que el sistema tenga material que optimizar. TopView y Brand Challenges juegan en otra liga: cientos de miles de RMB.',
  },
  {
    cat: 'Coste',
    q: '¿Cuánto cuesta hacer publicidad en RED (Xiaohongshu)?',
    a: 'El CPM in-feed arranca en torno a 40-100 RMB, unos 6-14 dólares. Los anuncios de búsqueda van a coste por clic, normalmente 1-5 RMB el clic. La siembra con KOC empieza en torno a 300-1.500 RMB por publicación. Las campañas con KOL barren un rango ancho: unos 5.000 RMB para un creador pequeño, varios cientos de miles para un nombre top.',
  },
  {
    cat: 'Coste',
    q: '¿Cuánto cuesta Baidu SEM?',
    a: 'Va de la palabra clave. Un término genérico puede costar 1-3 RMB el clic. Uno competitivo en seguros, educación o medicina puede irse a 30-100 RMB. La Brand Zone de Baidu, el resultado de marca premium, arranca en torno a 200.000 RMB al año para la mayoría de categorías.',
  },
  {
    cat: 'Coste',
    q: '¿Cómo está estructurado su precio?',
    a: 'Tarifa de proyecto para el setup, retainer mensual para la gestión continua y la inversión de medios pasada a coste. Sin margen oculto sobre los medios. Le enseñamos las facturas de plataforma si las pide. El retainer mensual mínimo son 5.000 dólares.',
  },
  {
    cat: 'Plataformas',
    q: '¿Por qué plataforma deberíamos empezar?',
    a: 'Depende del producto y de quién lo compra. Cuidado de la piel y moda suelen arrancar en RED. El gran consumo y las compras de decisión rápida se van a Douyin. B2B, finanzas y todo lo de alta consideración lideran con WeChat y Baidu. Elegimos donde decide su comprador, no donde a nosotros nos resulta más cómodo.',
  },
  {
    cat: 'Setup',
    q: '¿Podemos hacer anuncios en China sin una empresa registrada en China?',
    a: 'Sí, mediante cuentas de anuncios cross-border. Douyin, WeChat, RED y Baidu aceptan anunciantes extranjeros. Espere verificación extra y tarifas algo más altas. El alta lleva de dos a cuatro semanas y pide documentos mercantiles, una carta de autorización de marca y los certificados que exija su categoría.',
  },
  {
    cat: 'Setup',
    q: '¿Cuánto tarda dar de alta una cuenta?',
    a: 'WeChat, de dos a tres semanas. Douyin, de una a dos para categorías estándar, de cuatro a seis para las reguladas (cosmética, alimentación, salud). RED, de dos a cuatro semanas. Baidu, de una a dos. Las categorías reguladas siempre tardan más por las aprobaciones previas. No hay forma de acelerar esa parte, y tampoco querría.',
  },
  {
    cat: 'Setup',
    q: '¿Necesitamos una landing en chino?',
    a: 'Casi siempre, sí. Mandar tráfico pagado a un sitio en inglés mata su conversión. La página tiene que cargar dentro de China, estar en chino y aceptar métodos de pago chinos cuando aplique. Las construimos nosotros, o adaptamos la suya.',
  },
  {
    cat: 'Setup',
    q: '¿En cuánto tiempo podemos salir a directo?',
    a: 'Categoría estándar, creatividad sencilla, una plataforma: unas tres semanas desde la firma hasta los anuncios activos. Categoría regulada, varias plataformas, producción creativa completa: de seis a diez semanas. No nos saltamos el paso de cumplimiento. Saltárselo es lo que termina con cuentas suspendidas.',
  },
  {
    cat: 'Operación',
    q: '¿Cómo medimos el rendimiento?',
    a: 'Los KPI digitales habituales aplican: CPM, CPC, CTR, coste por adquisición, retorno sobre la inversión publicitaria. Encima medimos las señales específicas de China que dicen más. Coste de conversión de seguidores en WeChat. Tasa de entrada a sala en directo de Douyin. Ratio guardado-impresión en RED. Lift de búsqueda de marca en Baidu. Acordamos los KPI al inicio y reportamos contra ellos cada mes.',
  },
  {
    cat: 'Operación',
    q: '¿Cómo funciona la atribución entre plataformas chinas?',
    a: 'Mal, sobre todo si la compara con la que conoce. Son jardines amurallados. Cada plataforma reporta sus propias conversiones y la atribución cruzada es genuinamente difícil. Usamos píxeles nativos de plataforma, etiquetas UTM cuando aguantan y encuestas post-compra en categorías de alto valor. Preferimos ser honestos sobre eso a venderle un dashboard que finge haberlo resuelto.',
  },
  {
    cat: 'Operación',
    q: '¿Podemos usar la misma creatividad en WeChat, Douyin y RED?',
    a: 'No. Cada plataforma tiene su voz, su formato y su expectativa de audiencia. Douyin quiere cortes rápidos y un gancho en los primeros dos segundos. RED quiere algo más lento, lifestyle, que parezca el post de una usuaria real. WeChat se sitúa en medio, más liderado por marca. Adaptamos o rehacemos para cada una.',
  },
  {
    cat: 'Operación',
    q: '¿Cómo manejan la producción creativa?',
    a: 'Estudio interno. Foto, vídeo, copy, motion graphics, con redactores, editores y diseñadores chinos haciendo el trabajo. También usamos herramientas de contenidos generados con IA para recortar tiempo y coste en lo de alto volumen, como variaciones de foto de producto y montajes cortos. El criterio sigue siendo humano. El trabajo bruto no tiene por qué serlo.',
  },
  {
    cat: 'Operación',
    q: '¿Las colaboraciones con KOL y KOC forman parte de la compra de medios?',
    a: 'Sí, y cada año más. En RED y Douyin, la inversión pagada detrás del contenido de KOL gana a menudo a un anuncio puro. Tratamos a KOL y KOC como parte del plan de medios, no como una partida aparte que gestiona otro. Los buscamos, les hacemos el briefing, contratamos y amplificamos lo que funciona.',
  },
  {
    cat: 'Plataformas',
    q: '¿Qué pasa con el livestreaming en Douyin y Taobao?',
    a: 'El live commerce es un canal propio con su propio oficio. El tráfico pagado a salas en directo se hace con Qianchuan en Douyin y Wantwang en Taobao. Lo manejamos para clientes con el producto y los márgenes adecuados. No toda marca debería hacer livestreaming, y si la suya es una de ellas, se lo decimos en vez de aceptar el presupuesto.',
  },
  {
    cat: 'Operación',
    q: '¿Cómo gestionan los cambios de algoritmo de plataforma?',
    a: 'Reconstruimos los segmentos y probamos creatividades nuevas. Los algoritmos de Douyin y RED cambian a menudo. No le prometemos que el setup del trimestre pasado siga rindiendo este. Lo que sí le prometemos es que detectamos el cambio rápido y nos movemos rápido.',
  },
  {
    cat: 'Cumplimiento',
    q: '¿Qué categorías están restringidas o prohibidas en publicidad en China?',
    a: 'Tabaco y medicamentos con receta, prohibidos por completo. Alcohol, finanzas, salud, leche infantil, cosmética y productos sanitarios, todos muy restringidos. Educación, juego y cripto, prohibidos en la práctica. Comprobamos si su categoría es siquiera elegible antes de presentarle un plan de medios, no después.',
  },
  {
    cat: 'Cumplimiento',
    q: '¿Qué palabras están prohibidas en el copy publicitario chino?',
    a: 'Los términos absolutos: mejor, top, número uno, máximo, más, de nivel nacional, de clase mundial y cualquier cosa de ese vecindario. La lista oficial no es exhaustiva, así que la aplicación pilla expresiones que no esperaría, y da igual si la afirmación es cierta. Los claims de salud, el lenguaje médico y las garantías de resultado están restringidos encima de eso. Las multas van de 200.000 a 1 millón de RMB. Escribimos copy que se mantiene bien lejos.',
  },
  {
    cat: 'Cumplimiento',
    q: '¿Podemos enviar los datos de clientes chinos a nuestro CRM global?',
    a: 'No de forma automática. Bajo la Ley de Protección de Información Personal de China, sacar datos personales del país es una transferencia transfronteriza regulada. Pide base legal, consentimiento del usuario y, a veces, una evaluación de seguridad. Vale aun cuando su empresa no tenga entidad en China. Montamos el tracking y los flujos de datos dentro de las reglas, y le contamos sin rodeos qué puede y qué no puede salir del país.',
  },
  {
    cat: 'Cumplimiento',
    q: '¿Qué cambió en 2026 con las normas de livestreaming?',
    a: 'Tres cosas que importan para medios. Las grabaciones de livestream se archivan al menos tres años. Los influencers no pueden usar superlativos de precio (el típico "precio más bajo de todo internet") sin prueba verificable a mano. Y las agencias que faciliten interacciones falsas comparten ahora responsabilidad legal con los creadores que contratan. Ejecutamos campañas de livestream dentro de esas reglas, y solo trabajamos con creadores que hacen lo mismo.',
  },
  {
    cat: 'Cumplimiento',
    q: '¿Qué es un denunciante profesional y por qué importa?',
    a: 'En China existe una población de denunciantes profesionales. Escanean anuncios buscando incumplimientos, sobre todo superlativos y claims no respaldados, y luego presentan denuncias por una recompensa económica. Trabajan a escala. Para una marca extranjera, eso significa que una sola palabra no conforme puede activar una investigación real y una multa real. Otra razón más para dejar el copy bien antes de que nada salga al aire.',
  },
  {
    cat: 'Plataformas',
    q: '¿Pueden ayudarnos con medios en Hong Kong y Taiwán?',
    a: 'Sí. Hong Kong funciona con Google, Meta y plataformas locales como HK01. Taiwán funciona con Facebook, Line y Dcard. Ecosistemas distintos, equipos distintos. Tenemos experiencia en ambos, separada de China continental.',
  },
];

const faqCategories = ['Todas', 'Coste', 'Setup', 'Plataformas', 'Cumplimiento', 'Operación'];

const icons: Record<string, string> = {
  wechat: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M9.5 4C5.4 4 2 6.7 2 10c0 1.9 1.1 3.6 2.9 4.7L4 18l3.4-1.7c.7.2 1.4.3 2.1.3"/><path d="M22 14c0-2.9-3-5.3-6.7-5.3S8.6 11.1 8.6 14s3 5.3 6.7 5.3c.6 0 1.2-.1 1.8-.2L20 20.5l-.7-2.5c1.7-1 2.7-2.5 2.7-4z"/><circle cx="6.5" cy="9.5" r=".8" fill="currentColor"/><circle cx="10.5" cy="9.5" r=".8" fill="currentColor"/><circle cx="13" cy="13.5" r=".8" fill="currentColor"/><circle cx="17" cy="13.5" r=".8" fill="currentColor"/></svg>`,
  video: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="6" width="14" height="12" rx="2"/><polygon points="17 9 22 6 22 18 17 15"/><circle cx="8" cy="11" r="1.2" fill="currentColor"/></svg>`,
  heart: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M12 21s-7-4.4-9.3-9C1.4 9 3 5 6.5 5c2 0 3.5 1.2 4.5 2.5C12 6.2 13.5 5 15.5 5 19 5 20.6 9 21.3 12 19 16.6 12 21 12 21z"/></svg>`,
  search: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="7"/><line x1="16.2" y1="16.2" x2="21" y2="21"/></svg>`,
  film: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="16" rx="2"/><line x1="7" y1="4" x2="7" y2="20"/><line x1="17" y1="4" x2="17" y2="20"/><line x1="3" y1="9" x2="7" y2="9"/><line x1="3" y1="15" x2="7" y2="15"/><line x1="17" y1="9" x2="21" y2="9"/><line x1="17" y1="15" x2="21" y2="15"/></svg>`,
  target: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><circle cx="12" cy="12" r="5"/><circle cx="12" cy="12" r="1.5" fill="currentColor"/></svg>`,
  store: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l1.5-5h15L21 9"/><path d="M3 9v10a1 1 0 0 0 1 1h16a1 1 0 0 0 1-1V9"/><path d="M3 9h18"/><path d="M9 13h6"/></svg>`,
  cart: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="20" r="1.5"/><circle cx="18" cy="20" r="1.5"/><path d="M3 4h2l2.7 11.5a1 1 0 0 0 1 .8h9.6a1 1 0 0 0 1-.78L21 8H6.2"/></svg>`,
  live: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="5" width="14" height="11" rx="2"/><polygon points="17 8 22 5.5 22 15.5 17 13"/><circle cx="20" cy="19" r="2" fill="#E24B4B" stroke="none"/></svg>`,
  note: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M14 3H7a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V8z"/><path d="M14 3v5h5"/><line x1="9" y1="13" x2="15" y2="13"/><line x1="9" y1="17" x2="13" y2="17"/></svg>`,
  doc: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M14 3H7a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V8z"/><path d="M14 3v5h5"/><line x1="9" y1="13" x2="15" y2="13"/><line x1="9" y1="17" x2="13" y2="17"/></svg>`,
};

const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((f) => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a },
  })),
};

const serviceJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  serviceType: 'Compra de medios pagados en China',
  provider: {
    '@type': 'Organization',
    name: 'Beyond Border Group',
    url: 'https://beyondbordergroup.com',
  },
  areaServed: { '@type': 'Country', name: 'China' },
};
---

<Layout
  title="Compra de medios en China: WeChat, Douyin, RED y Baidu"
  description="Medios pagados en China en WeChat, Douyin, RED y Baidu, llevados por un equipo sénior que se hace dueño de los números. Empiece con una auditoría de medios."
>
  <script type="application/ld+json" set:html={JSON.stringify(faqJsonLd)} is:inline />
  <script type="application/ld+json" set:html={JSON.stringify(serviceJsonLd)} is:inline />

  <div class="cbs-progress" aria-hidden="true"><div class="cbs-progress__bar"></div></div>
  <article class="cbs">

    {/* ---------- HERO ---------- */}
    <section class="cbs-hero" data-hero>
      <div class="cbs-hero__bg" aria-hidden="true" data-parallax>
        <img src="/Images/grow-in-china/media/hero-media-buying.png" alt="Una compradora de medios en su mesa en una oficina de Shanghái, al caer la tarde, dos monitores con paneles de plataformas chinas de anuncios" />
      </div>
      <div class="cbs-hero__overlay" aria-hidden="true"></div>
      <div class="cbs-hero__grain" aria-hidden="true"></div>

      <div class="container cbs-hero__inner">
        <nav class="cbs-hero__crumbs" aria-label="Migas de pan">
          <a href="/es">Inicio</a>
          <span aria-hidden="true">/</span>
          <a href="/es#services">Crecer en China</a>
        </nav>
        <span class="eyebrow eyebrow--on-dark cbs-reveal">Medios</span>
        <h1 class="cbs-hero__title cbs-reveal" style="--reveal-delay: 80ms;">
          <span class="cbs-hero__title-row">Su presupuesto de medios en China</span>
          <span class="cbs-hero__title-row cbs-hero__title-row--accent">merece un equipo sénior.</span>
          <span class="cbs-hero__title-row cbs-hero__title-row--highlight">Lo planificamos, lo compramos, lo ejecutamos.</span>
        </h1>
        <p class="cbs-hero__lead cbs-reveal" style="--reveal-delay: 200ms;">
          La misma gente que monta su cuenta es la que sigue en ella en la revisión
          mensual. Sin capas, sin entregas intermedias, sin un junior aprendiendo a usar
          las plataformas con su dinero.
        </p>
        <div class="cbs-hero__actions cbs-reveal" style="--reveal-delay: 280ms;">
          <a href="/es/contacto" class="btn btn--primary cbs-magnetic">Pedir una auditoría de medios</a>
          <a href="#how-we-work" class="btn btn--ghost-light cbs-magnetic">Ver nuestro proceso</a>
        </div>

        <div class="cbs-hero__meta cbs-reveal" style="--reveal-delay: 360ms;">
          <div class="cbs-hero__meta-item">
            <span class="cbs-hero__meta-num" data-counter="15" data-suffix="+">0</span>
            <span class="cbs-hero__meta-label">años llevando medios pagados para marcas extranjeras</span>
          </div>
          <div class="cbs-hero__meta-item">
            <span class="cbs-hero__meta-num" data-counter="40" data-suffix="+">0</span>
            <span class="cbs-hero__meta-label">cuentas de anuncios activas en plataformas chinas</span>
          </div>
          <div class="cbs-hero__meta-item">
            <span class="cbs-hero__meta-num">Un equipo</span>
            <span class="cbs-hero__meta-label">Estrategia, creatividad, compra y reporting de principio a fin</span>
          </div>
        </div>
      </div>

      <a href="#who-we-work-with" class="cbs-hero__scroll" aria-label="Bajar a la siguiente sección">
        <span class="cbs-hero__scroll-line"></span>
        <span class="cbs-hero__scroll-text">Bajar</span>
      </a>
    </section>

    {/* ---------- WHO WE WORK WITH ---------- */}
    <section id="who-we-work-with" class="section cbs-truths-section">
      <div class="cbs-truths-bg" aria-hidden="true">
        <div class="cbs-truths-bg__blob cbs-truths-bg__blob--a"></div>
        <div class="cbs-truths-bg__blob cbs-truths-bg__blob--b"></div>
        <div class="cbs-truths-bg__grid"></div>
      </div>

      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">De dónde parte</span>
          <h2>Casi todas las marcas llegan en uno de estos tres puntos.</h2>
          <p class="section-header__lead">
            Uno será probablemente el suyo. El plan cambia para cada uno. El equipo que
            lo ejecuta, no.
          </p>
        </header>

        <div class="cbs-truths cbs-truths--three">
          {whereYouAre.map((c, i) => (
            <article
              class={`cbs-truth cbs-truth--text${i === 1 ? ' cbs-truth--alt' : ''} cbs-reveal`}
              style={`--reveal-delay: ${i * 100}ms;`}
              data-truth-tilt
            >
              <div class="cbs-truth__inner">
                <div class="cbs-truth__icon" aria-hidden="true" set:html={c.icon} />
                <div class="cbs-truth__kicker">
                  <span class="cbs-truth__kicker-dot"></span>
                  {c.kicker}
                </div>
                <div class="cbs-truth__content">
                  <h3 class="cbs-truth__title">{c.title}</h3>
                  <p class="cbs-truth__body">{c.body}</p>
                </div>
              </div>
            </article>
          ))}
        </div>

        <p class="cbs-cards__note cbs-reveal">Hacemos los tres. El plan cambia en cada caso.</p>
      </div>
    </section>

    {/* ---------- WHAT WE DO ---------- */}
    <section class="section section--alt">
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">Qué hacemos</span>
          <h2>Dos áreas, un solo equipo.</h2>
          <p class="section-header__lead">
            Llevamos medios pagados en las plataformas que importan en China. Cada una
            funciona de manera distinta. Cada una pide su plan, su creatividad y su
            lógica de puja. Las conocemos porque entramos en las cuentas cada día, no
            porque leamos un caso de estudio sobre ellas.
          </p>
        </header>

        <div class="cbs-sub">
          <span class="eyebrow">Plataformas de marca y alcance</span>
        </div>

        <div class="cbs-services cbs-reveal">
          {brandReach.map((s, i) => (
            <article class="cbs-service cbs-service--static" style={`--reveal-delay: ${i * 60}ms;`}>
              <span class="cbs-service__icon" aria-hidden="true" set:html={icons[s.icon]} />
              <h3 class="cbs-service__title">{s.title}</h3>
              <p class="cbs-service__body">{s.body}</p>
            </article>
          ))}
        </div>

        <figure class="cbs-band cbs-reveal">
          <img
            src="/Images/grow-in-china/media/ecommerce-media-band.png"
            alt="Un móvil con la tienda de Douyin, sostenido en un entorno de cumplimiento logístico, con el almacén ligeramente desenfocado al fondo"
            loading="lazy"
            decoding="async"
          />
        </figure>

        <div class="cbs-sub cbs-sub--ec">
          <span class="eyebrow">Medios eCommerce</span>
          <p class="cbs-sub__lead">
            Aquí el tráfico pagado se convierte en ventas. Si está vendiendo en China,
            suele ser el sitio donde debe ir el presupuesto primero, antes que las
            partidas de notoriedad.
          </p>
        </div>

        <div class="cbs-services cbs-services--two cbs-reveal">
          {ecommerceMedia.map((s, i) => (
            <article class="cbs-service cbs-service--static" style={`--reveal-delay: ${i * 60}ms;`}>
              <span class="cbs-service__icon" aria-hidden="true" set:html={icons[s.icon]} />
              <h3 class="cbs-service__title">{s.title}</h3>
              <p class="cbs-service__body">{s.body}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    {/* ---------- WHY BRANDS STAY WITH US ---------- */}
    <section class="section">
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">Por qué las marcas se quedan</span>
          <h2>La mayoría de agencias deja su cuenta en manos de juniors. Nosotros no.</h2>
          <p class="section-header__lead">
            El equipo que firma el contrato es el equipo que ejecuta el trabajo. Mismos
            nombres en el kickoff, mismos nombres en la revisión mensual.
          </p>
        </header>

        <div class="cbs-cards cbs-cards--4 cbs-reveal">
          {whyStay.map((c, i) => (
            <article class="cbs-card cbs-card--accent" style={`--reveal-delay: ${i * 70}ms;`}>
              <h3 class="cbs-card__title">{c.title}</h3>
              <p class="cbs-card__body">{c.body}</p>
            </article>
          ))}
        </div>

        <blockquote class="cbs-quote cbs-reveal">
          <p>
            El equipo que construye su cuenta es el equipo que sigue al mando en la
            revisión mensual. En China esa continuidad es la diferencia entre gastar e
            invertir.
          </p>
        </blockquote>
      </div>
    </section>

    {/* ---------- HOW WE WORK ---------- */}
    <section id="how-we-work" class="section section--alt">
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">Cómo trabajamos</span>
          <h2>Cuatro semanas para estar a directo, después un ritmo semanal.</h2>
          <p class="section-header__lead">
            Un equipo. Estrategia, planificación de medios, creatividad, compra,
            optimización y reporting, todo en casa y todo sénior.
          </p>
        </header>

        <div class="cbs-tabs cbs-reveal" data-tabs>
          <div class="cbs-tabs__nav" role="tablist">
            {weeks.map((w, i) => (
              <button
                type="button"
                class={`cbs-tabs__tab${i === 0 ? ' is-active' : ''}`}
                role="tab"
                aria-selected={i === 0 ? 'true' : 'false'}
                aria-controls={`week-panel-${i}`}
                id={`week-tab-${i}`}
                data-tab-target={`week-panel-${i}`}
                data-tab-id={w.tabId}
              >
                <span class="cbs-tabs__tab-label">{w.name}</span>
                <span class="cbs-tabs__tab-tag">{w.tag}</span>
              </button>
            ))}
            <span class="cbs-tabs__indicator" aria-hidden="true"></span>
          </div>

          <div class="cbs-tabs__panels">
            {weeks.map((w, i) => (
              <div
                class={`cbs-tabs__panel${i === 0 ? ' is-active' : ''}`}
                role="tabpanel"
                id={`week-panel-${i}`}
                aria-labelledby={`week-tab-${i}`}
                hidden={i !== 0}
              >
                <div class="cbs-tabs__panel-grid">
                  <div class="cbs-tabs__lead">
                    <span class="cbs-tabs__pillset">
                      <span class="cbs-tabs__pill">{w.pill}</span>
                    </span>
                    <h3 class="cbs-tabs__title">{w.title}</h3>
                    <p class="cbs-tabs__desc">{w.desc}</p>

                    <div class="cbs-tabs__when">
                      <span class="cbs-tabs__when-label">Resultado</span>
                      <p>{w.outcome}</p>
                    </div>

                    <figure class="cbs-tabs__image">
                      <img src={w.image} alt={w.imageAlt} loading="lazy" decoding="async" />
                    </figure>
                  </div>

                  <div class="cbs-tabs__body">
                    <div class="cbs-tabs__list-wrap">
                      <span class="cbs-tabs__list-label">Qué hacemos</span>
                      <ul class="cbs-tabs__list">
                        {w.does.map((b) => (
                          <li>
                            <span class="cbs-tabs__check" aria-hidden="true">
                              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="5 12 10 17 19 8"/></svg>
                            </span>
                            <span>{b}</span>
                          </li>
                        ))}
                      </ul>
                    </div>

                    <div class="cbs-tabs__list-wrap cbs-tabs__list-wrap--alt">
                      <span class="cbs-tabs__list-label">Entregables</span>
                      <ul class="cbs-tabs__list">
                        {w.deliverables.map((d) => (
                          <li>
                            <span class="cbs-tabs__check cbs-tabs__check--alt" aria-hidden="true" set:html={icons.doc} />
                            <span>{d}</span>
                          </li>
                        ))}
                      </ul>
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        <p class="cbs-tabs__note cbs-reveal">
          Usted también recibe el dashboard. Los mismos números que vemos nosotros, a la
          misma hora.
        </p>
      </div>
    </section>

    {/* ---------- COMPLIANCE ---------- */}
    <section class="section">
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">Cumplimiento</span>
          <h2>En China, la ley decide qué puede decir su anuncio.</h2>
          <p class="section-header__lead">
            Tres reglas pillan a marcas extranjeras más que ninguna otra. La sección de
            preguntas frecuentes, más abajo, entra en detalle.
          </p>
        </header>

        <figure class="cbs-band cbs-band--narrow cbs-reveal">
          <img
            src="/Images/grow-in-china/media/compliance-adlaw-band.png"
            alt="Una hoja impresa con copy de marketing en chino y un bolígrafo rojo encima, con algunas palabras rodeadas, luz natural sobre la mesa"
            loading="lazy"
            decoding="async"
          />
        </figure>

        <div class="cbs-cards cbs-cards--3 cbs-reveal">
          {compliance.map((c, i) => (
            <article class="cbs-card" style={`--reveal-delay: ${i * 80}ms;`}>
              <h3 class="cbs-card__title">{c.title}</h3>
              <p class="cbs-card__body">{c.body}</p>
              <a href="#faq" class="cbs-card__link">Más en las preguntas frecuentes
                <span aria-hidden="true">→</span>
              </a>
            </article>
          ))}
        </div>
      </div>
    </section>

    {/* ---------- FAQ ---------- */}
    <section id="faq" class="section section--alt">
      <div class="container container--narrow">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">Preguntas frecuentes</span>
          <h2>Veinticinco preguntas, respondidas sin rodeos.</h2>
          <p class="section-header__lead">
            Lo que nos preguntan las marcas extranjeras en la primera llamada. Filtre por
            tema.
          </p>
        </header>

        <div class="cbs-faq-filter cbs-reveal" data-faq-filter role="tablist" aria-label="Filtrar preguntas por categoría">
          {faqCategories.map((c, i) => (
            <button
              type="button"
              class={`cbs-faq-chip${i === 0 ? ' is-active' : ''}`}
              role="tab"
              aria-selected={i === 0 ? 'true' : 'false'}
              data-faq-cat={c}
            >
              <span class="cbs-faq-chip__label">{c}</span>
              <span class="cbs-faq-chip__count">{c === 'Todas' ? faqs.length : faqs.filter((f) => f.cat === c).length}</span>
            </button>
          ))}
        </div>

        <div class="cbs-faq" data-faq-list>
          {faqs.map((f, i) => (
            <details
              class="cbs-faq__item cbs-reveal"
              data-faq-cat={f.cat}
              open={i === 0}
              style={`--reveal-delay: ${Math.min(i, 6) * 40}ms;`}
            >
              <summary class="cbs-faq__q">
                <span class="cbs-faq__cat-tag">{f.cat}</span>
                <span class="cbs-faq__q-text">{f.q}</span>
                <span class="cbs-faq__icon" aria-hidden="true">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
                </span>
              </summary>
              <div class="cbs-faq__a">
                <p>{f.a}</p>
              </div>
            </details>
          ))}
        </div>
      </div>
    </section>

    {/* ---------- FINAL CTA ---------- */}
    <section class="section section--tight">
      <div class="container container--narrow">
        <div class="cbs-cta">
          <span class="eyebrow eyebrow--on-dark">Hablemos</span>
          <h2 class="cbs-cta__title">¿Listo para poner su presupuesto de medios en China en manos sénior?</h2>
          <p class="cbs-cta__lead">
            Empiece por una auditoría. Miramos lo que está corriendo hoy, o lo que
            necesitaría para arrancar, y le devolvemos una lectura honesta: qué funciona,
            qué no y qué haríamos distinto. Sin un dossier lleno de jerga, la foto real.
            ¿Ya tiene un briefing? Envíelo.
          </p>
          <div class="cbs-cta__actions">
            <a href="/es/contacto" class="btn cbs-cta__btn-primary">Pedir una auditoría de medios</a>
            <a href="/es/proyectos" class="btn cbs-cta__btn-ghost">Ver nuestros proyectos</a>
          </div>
        </div>
      </div>
    </section>
  </article>

  <style is:global>
    /* ---------- Container narrow helper ---------- */
    .cbs .container--narrow { max-width: 880px; }

    /* ---------- REVEAL + MOTION ---------- */
    @media (prefers-reduced-motion: no-preference) {
      .cbs-reveal {
        opacity: 0;
        transform: translateY(16px);
        transition:
          opacity 700ms cubic-bezier(0.16, 1, 0.3, 1) var(--reveal-delay, 0ms),
          transform 700ms cubic-bezier(0.16, 1, 0.3, 1) var(--reveal-delay, 0ms);
      }
      .cbs-reveal.is-in { opacity: 1; transform: translateY(0); }
      .cbs-magnetic {
        will-change: transform;
        transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1),
                    box-shadow 220ms ease, background-color 200ms ease, color 200ms ease;
      }
    }

    /* ---------- SCROLL PROGRESS BAR ---------- */
    .cbs-progress {
      position: fixed; top: 0; left: 0; right: 0;
      height: 3px; z-index: 1000;
      background: transparent; pointer-events: none;
    }
    .cbs-progress__bar {
      height: 100%; width: var(--progress, 0%);
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 60%, #9DDBF5 100%);
      box-shadow: 0 0 12px rgba(38, 154, 218, 0.55);
      transition: width 80ms linear;
    }

    /* ---------- HERO ---------- */
    .cbs-hero {
      position: relative;
      min-height: clamp(360px, 48vh, 500px);
      display: flex; align-items: flex-start;
      padding: clamp(8px, 1.25vh, 16px) 0 clamp(48px, 7vh, 80px);
      color: var(--bbg-white);
      overflow: hidden; isolation: isolate;
    }
    body:has(.cbs-hero) .cbs-hero { padding-top: clamp(112px, 10vh, 144px); }
    @media (max-width: 1024px) {
      body:has(.cbs-hero) .cbs-hero { padding-top: clamp(96px, 9vh, 116px); }
    }
    .cbs-hero__bg { position: absolute; inset: -10% 0 0; z-index: -3; }
    .cbs-hero__bg img {
      width: 100%; height: 110%;
      object-fit: cover; object-position: center;
      transform: translate3d(0, var(--parallax-y, 0px), 0) scale(1.04);
      transition: transform 80ms linear;
      filter: saturate(0.95);
    }
    .cbs-hero__overlay {
      position: absolute; inset: 0; z-index: -2;
      background:
        linear-gradient(110deg,
          rgba(8, 18, 28, 0.92) 0%,
          rgba(8, 18, 28, 0.72) 42%,
          rgba(8, 18, 28, 0.32) 100%),
        radial-gradient(120% 80% at 0% 100%,
          rgba(38, 154, 218, 0.28) 0%,
          rgba(38, 154, 218, 0) 60%);
    }
    .cbs-hero__grain {
      position: absolute; inset: 0; z-index: -1;
      background-image: radial-gradient(rgba(255, 255, 255, 0.06) 1px, transparent 1px);
      background-size: 3px 3px;
      mix-blend-mode: overlay; opacity: 0.45;
      pointer-events: none;
    }
    .cbs-hero__inner { max-width: 980px; position: relative; }
    .cbs-hero__crumbs {
      margin-bottom: var(--space-4);
      font-size: var(--fs-small);
      display: flex; gap: 0.5rem; align-items: center;
      color: rgba(255, 255, 255, 0.7);
    }
    .cbs-hero__crumbs a { color: rgba(255, 255, 255, 0.85); font-weight: 500; }
    .cbs-hero__crumbs a:hover { color: var(--bbg-white); }
    .cbs-hero__inner .eyebrow--on-dark {
      display: inline-block;
      margin-bottom: var(--space-3);
      color: #9DDBF5;
    }
    .cbs-hero__title {
      font-size: clamp(2rem, 3.4vw + 1rem, 3.75rem);
      line-height: 1.06;
      letter-spacing: -0.025em;
      color: var(--bbg-white);
      margin: 0 0 var(--space-4);
      max-width: 22ch;
      font-weight: 700;
      display: flex; flex-direction: column;
      gap: 0.05em;
    }
    .cbs-hero__title-row { display: block; }
    .cbs-hero__title-row--accent {
      color: #9DDBF5; font-weight: 500;
      font-style: italic; letter-spacing: -0.018em;
    }
    .cbs-hero__title-row--highlight {
      position: relative; display: inline-block;
      align-self: flex-start; padding: 0 0.15em;
    }
    .cbs-hero__title-row--highlight::before {
      content: ""; position: absolute;
      left: 0; right: 0; bottom: 0.04em;
      height: 0.18em;
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      border-radius: 2px;
      transform-origin: left center;
      animation: cbs-underline 900ms cubic-bezier(0.16, 1, 0.3, 1) 600ms both;
      z-index: -1;
    }
    @keyframes cbs-underline { from { transform: scaleX(0); } to { transform: scaleX(1); } }
    .cbs-hero__lead {
      font-size: clamp(1rem, 0.5vw + 1rem, 1.1875rem);
      line-height: 1.55;
      color: rgba(255, 255, 255, 0.86);
      max-width: 58ch;
      margin: 0 0 var(--space-5);
    }
    .cbs-hero__actions { display: flex; flex-wrap: wrap; gap: var(--space-3); margin-bottom: var(--space-6); }
    .cbs-magnetic:hover { transform: translateY(-2px); }

    .cbs-hero__meta {
      display: flex; flex-wrap: nowrap;
      gap: clamp(var(--space-3), 3vw, var(--space-6));
      padding-top: var(--space-5);
      border-top: 1px solid rgba(255, 255, 255, 0.14);
      max-width: none;
    }
    .cbs-hero__meta-item {
      display: flex; flex-direction: column;
      gap: 0.2rem;
      min-width: 0;
      flex: 1 1 0;
    }
    @media (max-width: 720px) {
      .cbs-hero__meta { flex-wrap: wrap; }
      .cbs-hero__meta-item { flex: 1 1 auto; }
    }
    .cbs-hero__meta-num {
      font-size: clamp(1.625rem, 1.2vw + 1rem, 2.125rem);
      font-weight: 700; letter-spacing: -0.02em;
      line-height: 1; color: var(--bbg-white);
      font-feature-settings: "tnum";
    }
    .cbs-hero__meta-label {
      font-size: 0.85rem;
      color: rgba(255, 255, 255, 0.66);
      letter-spacing: 0.02em;
    }

    .cbs-hero__scroll {
      position: absolute;
      bottom: var(--space-5);
      right: clamp(var(--space-5), 4vw, var(--space-7));
      display: flex; flex-direction: column;
      align-items: center; gap: 0.5rem;
      color: rgba(255, 255, 255, 0.7);
      font-size: 0.7rem; letter-spacing: 0.18em;
      text-transform: uppercase; font-weight: 600;
      transition: color 200ms ease;
    }
    .cbs-hero__scroll:hover { color: var(--bbg-white); }
    .cbs-hero__scroll-line {
      width: 1px; height: 40px;
      background: linear-gradient(180deg, transparent 0%, rgba(255, 255, 255, 0.6) 100%);
      position: relative; overflow: hidden;
    }
    .cbs-hero__scroll-line::after {
      content: ""; position: absolute;
      top: -100%; left: 0; right: 0;
      height: 50%;
      background: linear-gradient(180deg, transparent 0%, var(--bbg-white) 100%);
      animation: cbs-scroll-tick 1.8s ease-in-out infinite;
    }
    @keyframes cbs-scroll-tick { 0% { top: -50%; } 100% { top: 100%; } }
    @media (max-width: 720px) {
      .cbs-hero__scroll { display: none; }
      .cbs-hero__meta { gap: var(--space-4); }
    }

    /* ---------- TEXT CARDS GRID ---------- */
    .cbs-cards {
      display: grid;
      gap: var(--space-5);
      margin-top: var(--space-6);
    }
    .cbs-cards--3 { grid-template-columns: repeat(3, 1fr); }
    .cbs-cards--4 { grid-template-columns: repeat(4, 1fr); }
    .cbs-card {
      position: relative;
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      padding: var(--space-6);
      display: flex; flex-direction: column;
      gap: var(--space-3);
      transition:
        transform 320ms cubic-bezier(0.16, 1, 0.3, 1),
        border-color 260ms ease,
        box-shadow 260ms ease;
      overflow: hidden;
    }
    .cbs-card::before {
      content: "";
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      transform: scaleX(0);
      transform-origin: left center;
      transition: transform 360ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .cbs-card:hover {
      transform: translateY(-4px);
      border-color: var(--bbg-blue);
      box-shadow: 0 16px 32px -20px rgba(38, 154, 218, 0.4);
    }
    .cbs-card:hover::before { transform: scaleX(1); }
    .cbs-card--accent::before { transform: scaleX(1); opacity: 0.4; }
    .cbs-card--accent:hover::before { opacity: 1; }
    .cbs-card__title {
      font-size: 1.125rem;
      line-height: 1.3;
      margin: 0;
      color: var(--bbg-ink);
      letter-spacing: -0.005em;
    }
    .cbs-card__body {
      font-size: 0.95rem;
      line-height: 1.6;
      color: var(--bbg-text);
      margin: 0;
    }
    .cbs-card__link {
      margin-top: auto;
      align-self: flex-start;
      display: inline-flex; align-items: center; gap: 0.4rem;
      padding-top: var(--space-3);
      font-size: 0.85rem; font-weight: 600;
      color: var(--bbg-blue);
    }
    .cbs-card__link span { transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1); }
    .cbs-card__link:hover span { transform: translateX(3px); }
    .cbs-cards__note {
      margin-top: var(--space-5);
      text-align: center;
      color: var(--bbg-text);
      font-size: 1rem;
      line-height: 1.6;
    }
    @media (max-width: 1024px) {
      .cbs-cards--3,
      .cbs-cards--4 { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 640px) {
      .cbs-cards--3,
      .cbs-cards--4 { grid-template-columns: 1fr; }
    }

    /* ---------- SUBHEADING ROW ---------- */
    .cbs-sub {
      margin-top: var(--space-7);
      margin-bottom: var(--space-4);
      display: flex; flex-direction: column;
      gap: var(--space-3);
    }
    .cbs-sub__lead {
      max-width: 70ch;
      color: var(--bbg-text);
      font-size: 1.0625rem;
      line-height: 1.6;
      margin: 0;
    }

    /* ---------- SERVICES (six- / four-card grid) ---------- */
    .cbs-services {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: var(--space-5);
    }
    .cbs-services--two { grid-template-columns: repeat(2, 1fr); }
    .cbs-service {
      position: relative;
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      padding: var(--space-6);
      display: flex; flex-direction: column;
      gap: var(--space-3);
      text-decoration: none;
      color: inherit;
      transition:
        transform 320ms cubic-bezier(0.16, 1, 0.3, 1),
        border-color 260ms ease,
        box-shadow 260ms ease;
      overflow: hidden;
    }
    .cbs-service::before {
      content: "";
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      transform: scaleX(0);
      transform-origin: left center;
      transition: transform 360ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .cbs-service:hover {
      transform: translateY(-4px);
      border-color: var(--bbg-blue);
      box-shadow: 0 16px 32px -20px rgba(38, 154, 218, 0.4);
    }
    .cbs-service:hover::before { transform: scaleX(1); }
    .cbs-service__icon {
      display: inline-flex;
      width: 44px; height: 44px;
      align-items: center; justify-content: center;
      border-radius: 12px;
      background: linear-gradient(135deg, var(--bbg-blue-light) 0%, rgba(38, 154, 218, 0.18) 100%);
      color: var(--bbg-blue);
      margin-bottom: var(--space-2);
    }
    .cbs-service__icon svg { width: 22px; height: 22px; }
    .cbs-service__title {
      font-size: 1.125rem;
      line-height: 1.25;
      margin: 0;
      color: var(--bbg-ink);
      letter-spacing: -0.005em;
    }
    .cbs-service__body {
      font-size: 0.95rem;
      line-height: 1.6;
      color: var(--bbg-text);
      margin: 0;
    }
    @media (max-width: 1024px) {
      .cbs-services,
      .cbs-services--two { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 640px) {
      .cbs-services,
      .cbs-services--two { grid-template-columns: 1fr; }
    }

    /* ---------- BAND IMAGE ---------- */
    .cbs-band {
      margin: var(--space-7) 0 var(--space-5);
      border-radius: var(--radius-lg);
      overflow: hidden;
      aspect-ratio: 21 / 8;
      background: var(--bbg-border);
    }
    .cbs-band--narrow { aspect-ratio: 21 / 6; margin: var(--space-5) 0 var(--space-6); }
    .cbs-band img {
      width: 100%; height: 100%;
      object-fit: cover; display: block;
    }
    @media (max-width: 720px) {
      .cbs-band { aspect-ratio: 4 / 3; }
      .cbs-band--narrow { aspect-ratio: 16 / 9; }
    }

    /* ---------- TABS ---------- */
    .cbs-tabs {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 16px 48px -28px rgba(8, 18, 28, 0.18);
    }
    .cbs-tabs__nav {
      position: relative;
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 0;
      background: linear-gradient(180deg, var(--bbg-surface) 0%, var(--bbg-white) 100%);
      border-bottom: 1px solid var(--bbg-border);
    }
    .cbs-tabs__tab {
      position: relative; background: transparent; border: none;
      padding: var(--space-5) var(--space-4);
      text-align: left; cursor: pointer;
      display: flex; flex-direction: column;
      gap: 0.35rem;
      color: var(--bbg-text-muted);
      transition: color 240ms ease, background 240ms ease;
      border-right: 1px solid var(--bbg-border);
      font-family: inherit;
    }
    .cbs-tabs__tab:last-child { border-right: none; }
    .cbs-tabs__tab:hover { color: var(--bbg-ink); background: rgba(38, 154, 218, 0.04); }
    .cbs-tabs__tab.is-active { color: var(--bbg-ink); background: var(--bbg-white); }
    .cbs-tabs__tab-label {
      font-size: 1.0625rem; font-weight: 700;
      letter-spacing: -0.005em; line-height: 1.2;
    }
    .cbs-tabs__tab-tag {
      font-size: 0.78rem; color: var(--bbg-text-muted);
      line-height: 1.35;
    }
    .cbs-tabs__tab.is-active .cbs-tabs__tab-tag { color: var(--bbg-blue); }
    .cbs-tabs__indicator {
      position: absolute;
      bottom: -1px; left: 0;
      width: 25%; height: 3px;
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      border-radius: 2px 2px 0 0;
      transform: translateX(0);
      transition: transform 360ms cubic-bezier(0.16, 1, 0.3, 1);
      pointer-events: none;
    }
    .cbs-tabs__panels { background: var(--bbg-white); }
    .cbs-tabs__panel {
      padding: clamp(var(--space-5), 4vw + 1rem, var(--space-7));
      animation: cbs-tab-in 360ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    @keyframes cbs-tab-in {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .cbs-tabs__panel[hidden] { display: none; }
    .cbs-tabs__panel-grid {
      display: grid;
      grid-template-columns: 1.1fr 1fr;
      gap: clamp(var(--space-5), 3vw, var(--space-7));
      align-items: start;
    }
    .cbs-tabs__lead { display: flex; flex-direction: column; gap: var(--space-4); }
    .cbs-tabs__pillset { display: flex; flex-wrap: wrap; gap: 0.5rem; }
    .cbs-tabs__pill {
      font-size: 0.78rem; font-weight: 600;
      padding: 0.35em 0.85em;
      border-radius: 999px;
      background: var(--bbg-blue-light);
      color: var(--bbg-blue);
      letter-spacing: 0.01em;
    }
    .cbs-tabs__title {
      font-size: clamp(1.625rem, 1.8vw + 1rem, 2.25rem);
      line-height: 1.1; margin: 0;
      letter-spacing: -0.015em;
    }
    .cbs-tabs__desc {
      font-size: 1rem; line-height: 1.6;
      color: var(--bbg-text); margin: 0;
    }
    .cbs-tabs__when {
      padding: var(--space-4);
      background: var(--bbg-surface);
      border-radius: var(--radius-md);
      border-left: 3px solid var(--bbg-blue);
    }
    .cbs-tabs__when-label {
      display: block;
      font-size: 0.72rem; font-weight: 700;
      letter-spacing: 0.1em; text-transform: uppercase;
      color: var(--bbg-blue);
      margin-bottom: 0.35rem;
    }
    .cbs-tabs__when p { margin: 0; font-size: 0.95rem; line-height: 1.55; color: var(--bbg-text); }
    .cbs-tabs__body { display: flex; flex-direction: column; gap: var(--space-4); }
    .cbs-tabs__image {
      margin: 0;
      aspect-ratio: 16 / 10;
      overflow: hidden;
      border-radius: var(--radius-md);
      background: var(--bbg-border);
    }
    .cbs-tabs__image img {
      width: 100%; height: 100%;
      object-fit: cover; display: block;
    }
    .cbs-tabs__list-wrap {
      background: var(--bbg-surface);
      border-radius: var(--radius-md);
      padding: var(--space-5);
    }
    .cbs-tabs__list-wrap--alt { background: var(--bbg-blue-light); }
    .cbs-tabs__list-label {
      display: block;
      font-size: 0.72rem; font-weight: 700;
      letter-spacing: 0.1em; text-transform: uppercase;
      color: var(--bbg-text-muted);
      margin-bottom: var(--space-3);
    }
    .cbs-tabs__list-wrap--alt .cbs-tabs__list-label { color: var(--bbg-blue-dark); }
    .cbs-tabs__list {
      list-style: none; padding: 0; margin: 0;
      display: flex; flex-direction: column;
      gap: var(--space-3);
    }
    .cbs-tabs__list li {
      display: grid;
      grid-template-columns: 24px 1fr;
      gap: 0.75rem;
      align-items: start;
      font-size: 0.95rem; line-height: 1.5;
      color: var(--bbg-text);
    }
    .cbs-tabs__check {
      width: 22px; height: 22px;
      border-radius: 50%;
      background: var(--bbg-blue);
      color: var(--bbg-white);
      display: inline-flex; align-items: center; justify-content: center;
      flex-shrink: 0; margin-top: 1px;
    }
    .cbs-tabs__check--alt {
      background: var(--bbg-white);
      color: var(--bbg-blue);
      box-shadow: inset 0 0 0 1.5px var(--bbg-blue);
    }
    .cbs-tabs__check svg { width: 12px; height: 12px; }
    .cbs-tabs__note {
      margin: var(--space-5) auto 0;
      max-width: 60ch;
      text-align: center;
      color: var(--bbg-text);
      font-size: 1rem;
      line-height: 1.6;
    }
    @media (max-width: 1024px) {
      .cbs-tabs__nav { grid-template-columns: 1fr; }
      .cbs-tabs__tab { border-right: none; border-bottom: 1px solid var(--bbg-border); }
      .cbs-tabs__indicator { display: none; }
      .cbs-tabs__panel-grid { grid-template-columns: 1fr; }
    }

    /* ---------- QUOTE ---------- */
    .cbs-quote {
      margin: var(--space-7) auto 0;
      max-width: 760px;
      padding: var(--space-5) var(--space-6);
      background: linear-gradient(135deg, var(--bbg-blue-light) 0%, rgba(38, 154, 218, 0.10) 100%);
      border-left: 4px solid var(--bbg-blue);
      border-radius: 0 var(--radius-md) var(--radius-md) 0;
    }
    .cbs-quote p {
      margin: 0;
      font-size: 1.0625rem;
      line-height: 1.55;
      color: var(--bbg-ink);
      font-weight: 500;
      font-style: italic;
    }

    /* ---------- FAQ ---------- */
    .cbs-faq {
      display: flex; flex-direction: column;
      gap: var(--space-3);
    }
    .cbs-faq__item {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-md);
      overflow: hidden;
      transition: border-color 240ms ease, box-shadow 240ms ease, transform 280ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .cbs-faq__item:hover {
      transform: translateX(2px);
      border-color: rgba(38, 154, 218, 0.4);
    }
    .cbs-faq__item[open] {
      border-color: var(--bbg-blue);
      box-shadow: 0 8px 22px -14px rgba(38, 154, 218, 0.4);
    }
    .cbs-faq__q {
      cursor: pointer;
      padding: var(--space-5);
      display: flex; align-items: center;
      justify-content: space-between;
      gap: var(--space-4);
      font-weight: 600;
      font-size: 1.0625rem; line-height: 1.3;
      color: var(--bbg-ink);
      list-style: none;
      transition: color var(--dur) var(--ease);
    }
    .cbs-faq__q::-webkit-details-marker { display: none; }
    .cbs-faq__q::marker { display: none; }
    .cbs-faq__item:hover .cbs-faq__q { color: var(--bbg-blue); }
    .cbs-faq__icon {
      flex-shrink: 0;
      width: 28px; height: 28px;
      display: inline-flex;
      align-items: center; justify-content: center;
      border-radius: 50%;
      background: var(--bbg-surface);
      color: var(--bbg-text-muted);
      transition: transform 320ms cubic-bezier(0.16, 1, 0.3, 1),
                  color var(--dur) var(--ease),
                  background var(--dur) var(--ease);
    }
    .cbs-faq__item[open] .cbs-faq__icon {
      transform: rotate(180deg);
      color: var(--bbg-white);
      background: var(--bbg-blue);
    }
    .cbs-faq__a {
      padding: 0 var(--space-5) var(--space-5);
      color: var(--bbg-text);
      line-height: 1.65;
      font-size: 1rem;
      animation: cbs-faq-in 320ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    @keyframes cbs-faq-in {
      from { opacity: 0; transform: translateY(-4px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .cbs-faq__a p { margin: 0; }

    /* ---------- FAQ FILTER ---------- */
    .cbs-faq-filter {
      display: flex; flex-wrap: wrap;
      gap: 0.5rem; justify-content: center;
      margin-bottom: var(--space-6);
      overflow-x: auto;
    }
    .cbs-faq-chip {
      display: inline-flex; align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 0.95rem;
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: 999px;
      cursor: pointer;
      font-size: 0.875rem;
      font-weight: 600;
      color: var(--bbg-text);
      transition: all 220ms ease;
      font-family: inherit;
      white-space: nowrap;
    }
    .cbs-faq-chip:hover { border-color: var(--bbg-blue); color: var(--bbg-blue); }
    .cbs-faq-chip.is-active {
      background: var(--bbg-blue);
      border-color: var(--bbg-blue);
      color: var(--bbg-white);
    }
    .cbs-faq-chip__count {
      font-size: 0.72rem; font-weight: 700;
      padding: 0.15em 0.5em;
      border-radius: 999px;
      background: var(--bbg-surface);
      color: var(--bbg-text-muted);
      transition: all 220ms ease;
    }
    .cbs-faq-chip:hover .cbs-faq-chip__count { background: var(--bbg-blue-light); color: var(--bbg-blue); }
    .cbs-faq-chip.is-active .cbs-faq-chip__count {
      background: rgba(255, 255, 255, 0.2);
      color: var(--bbg-white);
    }
    .cbs-faq__cat-tag {
      display: inline-block;
      font-size: 0.68rem; font-weight: 700;
      letter-spacing: 0.08em; text-transform: uppercase;
      color: var(--bbg-blue);
      background: var(--bbg-blue-light);
      padding: 0.25em 0.6em;
      border-radius: 4px;
      margin-right: var(--space-3);
      flex-shrink: 0;
    }
    .cbs-faq__q-text { flex-grow: 1; }
    .cbs-faq__item.is-hidden { display: none; }

    /* ---------- FINAL CTA ---------- */
    .cbs-cta {
      padding: clamp(var(--space-6), 4vw + 1rem, var(--space-8)) var(--space-6);
      background: linear-gradient(135deg, var(--bbg-blue) 0%, var(--bbg-blue-dark) 100%);
      color: var(--bbg-white);
      border-radius: var(--radius-lg);
      text-align: center;
      position: relative; overflow: hidden;
    }
    .cbs-cta::before {
      content: ""; position: absolute;
      top: -40%; right: -10%;
      width: 420px; height: 420px;
      background: radial-gradient(circle at center, rgba(255,255,255,0.18) 0%, rgba(255,255,255,0) 65%);
      pointer-events: none;
    }
    .cbs-cta .eyebrow--on-dark {
      display: inline-block;
      margin-bottom: var(--space-4);
      color: rgba(255, 255, 255, 0.9);
    }
    .cbs-cta__title {
      font-size: clamp(1.75rem, 1.5vw + 1rem, 2.5rem);
      line-height: 1.15;
      color: var(--bbg-white);
      margin: 0 0 var(--space-4);
      letter-spacing: -0.015em;
    }
    .cbs-cta__lead {
      color: rgba(255, 255, 255, 0.92);
      font-size: 1.0625rem;
      max-width: 60ch;
      margin: 0 auto var(--space-5);
      line-height: 1.55;
    }
    .cbs-cta__actions {
      display: flex; flex-wrap: wrap;
      gap: var(--space-3);
      justify-content: center;
      position: relative;
    }
    .cbs-cta__btn-primary {
      background: var(--bbg-white);
      color: var(--bbg-blue-dark);
      border: 1px solid var(--bbg-white);
    }
    .cbs-cta__btn-primary:hover {
      background: rgba(255, 255, 255, 0.92);
      color: var(--bbg-blue-dark);
    }
    .cbs-cta__btn-ghost {
      background: transparent;
      border: 1px solid rgba(255, 255, 255, 0.5);
      color: var(--bbg-white);
    }
    .cbs-cta__btn-ghost:hover {
      background: rgba(255, 255, 255, 0.12);
      border-color: var(--bbg-white);
      color: var(--bbg-white);
    }
  </style>

  <script is:inline>
    (() => {
      const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      // 1. Reveal-on-scroll
      const revealEls = document.querySelectorAll('.cbs-reveal');
      if (revealEls.length) {
        if (reduced || !('IntersectionObserver' in window)) {
          revealEls.forEach((el) => el.classList.add('is-in'));
        } else {
          const io = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add('is-in');
                io.unobserve(entry.target);
              }
            });
          }, { threshold: 0.12, rootMargin: '0px 0px -8% 0px' });
          revealEls.forEach((el) => io.observe(el));
        }
      }

      // 2. Animated counters
      const counters = document.querySelectorAll('[data-counter]');
      if (counters.length && !reduced && 'IntersectionObserver' in window) {
        const animate = (el) => {
          const target = parseFloat(el.dataset.counter || '0');
          const suffix = el.dataset.suffix || '';
          const duration = 1400;
          const start = performance.now();
          const tick = (now) => {
            const t = Math.min(1, (now - start) / duration);
            const eased = 1 - Math.pow(1 - t, 3);
            const value = Math.round(target * eased);
            el.textContent = value + suffix;
            if (t < 1) requestAnimationFrame(tick);
          };
          requestAnimationFrame(tick);
        };
        const counterIo = new IntersectionObserver((entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              animate(entry.target);
              counterIo.unobserve(entry.target);
            }
          });
        }, { threshold: 0.5 });
        counters.forEach((el) => counterIo.observe(el));
      } else {
        counters.forEach((el) => {
          el.textContent = (el.dataset.counter || '0') + (el.dataset.suffix || '');
        });
      }

      // 3. Hero parallax + scroll progress bar
      const parallaxEl = document.querySelector('[data-parallax]');
      const progressEl = document.querySelector('.cbs-progress__bar');
      const heroEl = document.querySelector('[data-hero]');
      let ticking = false;
      const onScroll = () => {
        if (ticking) return;
        ticking = true;
        requestAnimationFrame(() => {
          const scrollY = window.scrollY || window.pageYOffset;
          if (parallaxEl && heroEl && !reduced) {
            const heroH = heroEl.offsetHeight;
            if (scrollY < heroH) {
              parallaxEl.style.setProperty('--parallax-y', (scrollY * 0.18) + 'px');
            }
          }
          if (progressEl) {
            const docH = document.documentElement.scrollHeight - window.innerHeight;
            const pct = docH > 0 ? Math.min(100, (scrollY / docH) * 100) : 0;
            progressEl.style.setProperty('--progress', pct + '%');
          }
          ticking = false;
        });
      };
      window.addEventListener('scroll', onScroll, { passive: true });
      onScroll();

      // 4. Tabs
      const openTabByHash = () => {
        const hash = window.location.hash.slice(1);
        if (!hash) return;
        document.querySelectorAll('[data-tabs]').forEach((root) => {
          const matchingTab = root.querySelector(`[data-tab-id="${hash}"]`);
          if (matchingTab) matchingTab.click();
        });
      };

      document.querySelectorAll('[data-tabs]').forEach((tabsRoot) => {
        const tabs = Array.from(tabsRoot.querySelectorAll('.cbs-tabs__tab'));
        const panels = Array.from(tabsRoot.querySelectorAll('.cbs-tabs__panel'));
        const indicator = tabsRoot.querySelector('.cbs-tabs__indicator');

        const setActive = (tab) => {
          tabs.forEach((t) => {
            const active = t === tab;
            t.classList.toggle('is-active', active);
            t.setAttribute('aria-selected', active ? 'true' : 'false');
          });
          const targetId = tab.getAttribute('data-tab-target');
          panels.forEach((p) => {
            const active = p.id === targetId;
            p.classList.toggle('is-active', active);
            if (active) p.removeAttribute('hidden');
            else p.setAttribute('hidden', '');
          });
          if (indicator) {
            const idx = tabs.indexOf(tab);
            const w = 100 / tabs.length;
            indicator.style.width = w + '%';
            indicator.style.transform = `translateX(${idx * 100}%)`;
          }
        };

        tabs.forEach((tab) => {
          tab.addEventListener('click', () => setActive(tab));
          tab.addEventListener('keydown', (e) => {
            const idx = tabs.indexOf(tab);
            if (e.key === 'ArrowRight') {
              e.preventDefault();
              const next = tabs[(idx + 1) % tabs.length];
              next.focus(); setActive(next);
            } else if (e.key === 'ArrowLeft') {
              e.preventDefault();
              const prev = tabs[(idx - 1 + tabs.length) % tabs.length];
              prev.focus(); setActive(prev);
            }
          });
        });

        const initialActive = tabs.find((t) => t.classList.contains('is-active')) || tabs[0];
        if (initialActive) setActive(initialActive);
      });

      openTabByHash();
      window.addEventListener('hashchange', openTabByHash);

      // 5. Smooth scroll for anchors
      document.querySelectorAll('a[href^="#"]').forEach((link) => {
        link.addEventListener('click', (e) => {
          const id = link.getAttribute('href');
          if (!id || id === '#') return;
          const target = document.querySelector(id);
          if (!target) return;
          e.preventDefault();
          target.scrollIntoView({ behavior: reduced ? 'auto' : 'smooth', block: 'start' });
          const tabId = id.slice(1);
          document.querySelectorAll('[data-tabs]').forEach((root) => {
            const matchingTab = root.querySelector(`[data-tab-id="${tabId}"]`);
            if (matchingTab) matchingTab.click();
          });
          history.replaceState(null, '', id);
        });
      });

      // 6. FAQ accordion - close others on open
      const faqItems = document.querySelectorAll('.cbs-faq__item');
      faqItems.forEach((item) => {
        item.addEventListener('toggle', () => {
          if (item.open) {
            faqItems.forEach((other) => {
              if (other !== item && other.open) other.open = false;
            });
          }
        });
      });

      // 7. FAQ category filter
      const faqFilter = document.querySelector('[data-faq-filter]');
      const faqList = document.querySelector('[data-faq-list]');
      if (faqFilter && faqList) {
        const chips = Array.from(faqFilter.querySelectorAll('.cbs-faq-chip'));
        const items = Array.from(faqList.querySelectorAll('.cbs-faq__item'));
        chips.forEach((chip) => {
          chip.addEventListener('click', () => {
            const cat = chip.dataset.faqCat;
            chips.forEach((c) => {
              const on = c === chip;
              c.classList.toggle('is-active', on);
              c.setAttribute('aria-selected', on ? 'true' : 'false');
            });
            items.forEach((item) => {
              const matches = cat === 'Todas' || item.dataset.faqCat === cat;
              item.classList.toggle('is-hidden', !matches);
            });
          });
        });
      }

      // Truth cards: cursor tilt + scroll-driven image parallax
      const truths = document.querySelectorAll('[data-truth-tilt]');
      if (truths.length && !reduced) {
        truths.forEach((card) => {
          const onMove = (e) => {
            const rect = card.getBoundingClientRect();
            const x = (e.clientX - rect.left) / rect.width;
            const y = (e.clientY - rect.top) / rect.height;
            const ry = (x - 0.5) * 5;
            const rx = (0.5 - y) * 4;
            card.style.setProperty('--ry', ry.toFixed(2) + 'deg');
            card.style.setProperty('--rx', rx.toFixed(2) + 'deg');
            card.setAttribute('data-tilt-active', '');
          };
          const onLeave = () => {
            card.style.setProperty('--ry', '0deg');
            card.style.setProperty('--rx', '0deg');
            card.removeAttribute('data-tilt-active');
          };
          card.addEventListener('mousemove', onMove);
          card.addEventListener('mouseleave', onLeave);
        });

        const parallaxImgs = document.querySelectorAll('[data-truth-parallax]');
        let truthsTicking = false;
        const updateTruthParallax = () => {
          parallaxImgs.forEach((img) => {
            const frame = img.parentElement;
            if (!frame) return;
            const rect = frame.getBoundingClientRect();
            const vh = window.innerHeight;
            if (rect.bottom < 0 || rect.top > vh) return;
            const center = rect.top + rect.height / 2;
            const offset = (center - vh / 2) / vh;
            const shift = Math.max(-16, Math.min(16, -offset * 22));
            img.style.setProperty('--img-y', shift.toFixed(1) + 'px');
          });
        };
        const onTruthsScroll = () => {
          if (truthsTicking) return;
          truthsTicking = true;
          requestAnimationFrame(() => {
            updateTruthParallax();
            truthsTicking = false;
          });
        };
        window.addEventListener('scroll', onTruthsScroll, { passive: true });
        window.addEventListener('resize', onTruthsScroll, { passive: true });
        updateTruthParallax();
      }
    })();
  </script>
</Layout>
