---
import Layout from '../../layouts/Layout.astro';

const heroStats = [
  { num: '10,000', suffix: '+', label: 'creators in our own KOC database' },
  { num: '7', suffix: '', label: 'platforms run end to end' },
  { num: 'One team', suffix: '', label: 'Casting, briefing, production and reporting' },
];

const whyPoints = [
  {
    lead: 'The funnel is one screen, not five steps.',
    body: 'Chinese shoppers don’t leave the app to decide. They scroll, search, watch a livestream and buy, all in the same place. Treat the influencer as a billboard and the budget is gone before the first post goes up.',
  },
  {
    lead: 'The numbers are bigger than people expect.',
    body: 'Livestream commerce alone runs into the hundreds of billions of dollars a year, and the channel keeps growing at a double-digit rate.',
  },
  {
    lead: 'And brands still underspend it.',
    body: 'Influencer investment in Asia trails the US by an order of magnitude, even though the channel does more of the selling. The opportunity is sitting right there.',
  },
];

const whyQuotes = [
  {
    image: '/Images/grow-in-china/influencers-kols/why-funnel-onescreen.png',
    alt: 'A Chinese woman scrolling her phone on a sofa, a just-opened delivery parcel beside her, the one-screen funnel made literal',
    quote: 'China’s livestreaming e-commerce market reached roughly $807 billion in 2024, and is projected to grow at around 18 percent a year through 2026.',
    cite: 'U.S. Department of Agriculture, Shanghai ATO',
    citeMeta: 'Selling in China: The Rise of Livestreaming E-commerce, 2025',
  },
  {
    image: '/Images/grow-in-china/influencers-kols/why-koc-mentions.png',
    alt: 'A phone screen showing a grid of small creator posts for one product category, a real research moment in a domestic setting',
    quote: 'Across Asia, influencer spend is just 0.75 percent of social commerce GMV, against 6.8 percent in the US.',
    cite: 'Totem and Rakuten Insight',
    citeMeta: 'Influencer Marketing in Asia, 2025',
  },
];

const howSteps = [
  {
    icon: 'compass',
    title: 'Find the right people',
    body: 'We start with your goal and your margin, not a name on a list. Then we read the data: who is watching, and what they go on to buy. We keep our own database of KOCs we have used and trust.',
  },
  {
    icon: 'pen',
    title: 'Write a brief that travels',
    body: 'The brief is the whole game. We agree the message and the must-haves with you, then leave the execution to the creator. The ones who sound scripted are the ones nobody watches.',
  },
  {
    icon: 'gear',
    title: 'Run it end to end',
    body: 'Contracts, content, posting schedule, livestream coordination. You approve the plan. We move budget the moment something underperforms, not weeks later in a post-mortem.',
  },
  {
    icon: 'link',
    title: 'Connect content to commerce',
    body: 'A post you can’t buy from is half a campaign. We link content straight to your store on Douyin, Tmall or JD and track what each creator drove. On Xiaohongshu the sale often lands elsewhere, so we follow it across platforms.',
  },
];

const benefits = [
  {
    icon: 'handshake',
    lead: 'Direct relationships, better rates.',
    body: 'We work with KOLs and MCNs we know personally, not a marketplace. Priority access to in-demand creators, at rates below open-market.',
  },
  {
    icon: 'creator',
    lead: 'A KOC network built over years.',
    body: 'Our own database, more than 10,000 creators deep. It puts your product in front of the right niche communities at a scale a brand cannot reach alone.',
  },
  {
    icon: 'chart',
    lead: 'Selection backed by data, not a gut call.',
    body: 'Every creator on a shortlist is screened first: audience composition, real engagement, fake-follower checks.',
  },
  {
    icon: 'clipboard',
    lead: 'Reporting that ties spend to sales.',
    body: 'Creator by creator, across platforms. You know which ones sold while the campaign is still live, while it can still be fixed.',
  },
];

const platforms = [
  {
    name: 'Xiaohongshu',
    cn: '小红书',
    desc: 'Where research and purchase happen in the same scroll. A strong note sends shoppers straight to your store. Our own KOC and KOL database lives here.',
  },
  {
    name: 'Douyin',
    cn: '抖音',
    desc: 'Short video and livestream commerce, run on an interest algorithm. We are an official Douyin marketing partner.',
  },
  {
    name: 'WeChat',
    cn: '微信',
    desc: 'Articles, private traffic, long-form trust and CRM. More than ten years on the platform.',
  },
  {
    name: 'Weibo',
    cn: '微博',
    desc: 'Reach and conversation. Still strong for launches and buzz.',
  },
  {
    name: 'Bilibili',
    cn: '',
    desc: 'Long-form, younger, deeply engaged. Underused by foreign brands.',
  },
  {
    name: 'Kuaishou',
    cn: '快手',
    desc: 'Livestream commerce with real pull in lower-tier cities.',
  },
  {
    name: 'Tmall & JD',
    cn: '天猫 / 京东',
    desc: 'The platforms where the sale closes. We connect KOL content straight to your store and run influencer campaigns inside both.',
  },
];

const faqs = [
  { cat: 'Strategy', q: 'How is this different from influencer marketing back home?', a: 'In the West, influencers mostly create awareness and the purchase happens later, somewhere else. In China it all happens on the same app, often in the same session. Creators are tied directly to sales, not just reach. And because brands here still underspend the channel relative to the US, it is more of an open door than a crowded room.' },
  { cat: 'Creators', q: 'How do I find the right influencer?', a: 'Start with your audience, not the influencer. Work out who you need to reach, then check that against a creator’s real follower base, not their headline number. Douyin runs on an interest algorithm, so content fit usually beats follower count. An agency with real data and direct relationships makes this faster and cheaper than guessing your way through it.' },
  { cat: 'Cost', q: 'How much does a KOL actually cost?', a: 'It ranges a lot. On Xiaohongshu, a nano KOC post can run a few hundred RMB; a mid-tier KOL post sits in the low tens of thousands; a top-tier name can pass 100,000 RMB for a single post. Douyin video tends to come in 20 to 30 percent lower for comparable reach. And top livestream KOLs take a sales commission on top, often 40 to 50 percent. Whatever number you get quoted, treat it as a starting point, not a rate card.' },
  { cat: 'Cost', q: 'What budget do I need to run something real?', a: 'To genuinely test a platform, plan for 100,000 to 300,000 RMB over two to three months. That covers a KOC seeding base, a few mid-tier KOLs, content production and some paid amplification. You can run a seeding pilot below 50,000 RMB, but you will not get a clean read on whether the channel sells for you.' },
  { cat: 'Creators', q: 'KOL or KOC?', a: 'Usually both. KOLs give you reach and a credibility signal. KOCs, the everyday creators with small, trusted followings, build the search traction that matters when a shopper looks your product up. Find a dozen honest mentions and they buy. Find one paid post and they keep scrolling. We size the mix to your goal and your budget, and that mix is rarely the same twice.' },
  { cat: 'Strategy', q: 'Which platform is best?', a: 'The one where your buyers already are. Xiaohongshu suits considered purchases in beauty, lifestyle and wellness. Douyin is built for video and livestream selling at scale. Weibo still works for launches. WeChat is for depth and retention. Most brands need two or three, not one, and definitely not all of them at once.' },
  { cat: 'Working with us', q: 'How do I get a KOL to actually sell, not just rack up views?', a: 'Build the product into the content so it does not read as an ad, then make it buyable on the spot. Livestreams, honest reviews, unboxings, follower-only discount codes. And the link from post to product page has to be there. A campaign with no path to purchase is just awareness with a bigger invoice.' },
  { cat: 'Operations', q: 'Do I need a China-registered entity?', a: 'For the official collaboration systems, usually yes. Xiaohongshu’s Pugongying and Weibo’s official tools generally want a registered business in China. You can still run campaigns through an agency that holds the relationships, which is how most foreign brands start before their own entity is set up.' },
  { cat: 'Operations', q: 'How long until we are live?', a: 'Brief to first posts, three to five weeks. Shortlisting and negotiation take one to two weeks, contracting a few days, content and review another one to two. Livestream slots with in-demand hosts often need booking further out. Rushing the brief is where campaigns quietly lose their quality.' },
  { cat: 'Creators', q: 'How do I know an influencer’s followers are real?', a: 'Look past the count at the shape of the engagement. Comment quality, save and share rates, whether the audience’s city and age profile matches the content. Sudden follower spikes, generic one-word comments, engagement that does not track with views, those are the red flags. We screen for this before anyone reaches a shortlist, because inflated accounts are common.' },
  { cat: 'Operations', q: 'What should the contract spell out?', a: 'Be specific. Number and type of posts, platform, posting window, whether the content stays up and for how long, usage rights if you want to repurpose it, KPIs, and the commission structure where it applies. Vague contracts are where the disputes start. And get proof of performance before final payment written in.' },
  { cat: 'Cost', q: 'Can I reuse the content in my own ads?', a: 'Only if the contract grants usage rights, and that usually costs extra. Plenty of brands forget to negotiate it, then find they cannot legally boost a post that did well. Decide upfront whether you want amplification rights. Bolting them on later is slower and more expensive.' },
  { cat: 'Strategy', q: 'What results can I realistically expect?', a: 'Depends on category, budget and platform, so be wary of anyone who promises a fixed number. A healthy seeding campaign builds search visibility and a base of genuine mentions. A livestream drives sales in a window. Match the KPI to the format: reach and saves for seeding, conversion and GMV for livestream. And judge the program over a quarter, not off one post.' },
  { cat: 'Strategy', q: 'How do you measure ROI when the sale happens somewhere else?', a: 'This is the Xiaohongshu problem. A note builds intent, then the shopper buys on Tmall or JD a few days later. We track it through branded search lift, traffic and add-to-cart movement on your store, and discount codes unique to each creator. Intent shows up first, sales after. So we measure across platforms, not inside one.' },
  { cat: 'Operations', q: 'What if a campaign is underperforming halfway through?', a: 'We move budget. If a creator isn’t converting, spend shifts to the ones that are. Or we adjust the angle. Or change the format entirely. None of that works without live reporting, which is exactly why we set it up that way from day one. The flexibility has to be in the plan, and in the contracts, before you need it.' },
  { cat: 'Creators', q: 'Is livestreaming worth it for us?', a: 'Depends on your category and your margin. Livestream drives the highest engagement and the most direct conversion, but top hosts take that 40 to 50 percent commission, and the format rewards products that demo well. For a lot of brands, a store-led livestream beats renting a celebrity, you keep control of the message and the margin. If it does not fit, we will say so.' },
  { cat: 'Operations', q: 'How do I keep it from looking like an obvious ad?', a: 'Brief the creator on the message and the must-haves, then leave the execution alone. The scripted ones are the ones nobody watches, and on Xiaohongshu hard-selling backfires fast. Native, experience-based content that names the product honestly beats a polished ad read almost every time.' },
  { cat: 'Operations', q: 'What compliance risks should I know about?', a: 'China’s advertising law is strict on superlatives and unverified claims. "Best," "number one," unproven health or efficacy statements, those can trigger real penalties. And influencer collaborations increasingly have to run through official platforms for disclosure. We brief creators on what they cannot say, and we review content before it goes live.' },
  { cat: 'Strategy', q: 'Can a small brand succeed here, or is this just for the big names?', a: 'A small brand can absolutely work here, and often does better through KOCs than KOLs. Because Douyin and Xiaohongshu reward content fit over follower count, a relevant product with good content can build traction without a celebrity budget. Start with a focused KOC seeding base, prove the product lands, then scale.' },
  { cat: 'Working with us', q: 'Should I work with creators directly, through an MCN, or through an agency?', a: 'Direct works if you have the China team, the language, the contracts and the time to vet accounts. An MCN gives you its roster, but remember it represents its creators, not you. An agency works for your side of the table. It selects across the whole market against your goals, handles the contracts and the compliance, and ties the reporting back to your sales. That last part is the job we do.' },
];

const faqCategories = ['All', 'Strategy', 'Cost', 'Creators', 'Operations', 'Working with us'];

const icons: Record<string, string> = {
  compass: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><polygon points="15 9 13 13 9 15 11 11 15 9"/></svg>`,
  pen: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M3 21l3.5-1 11-11-2.5-2.5-11 11z"/><path d="M14 5l2.5-2.5a1.4 1.4 0 0 1 2 0l1 1a1.4 1.4 0 0 1 0 2L17 8"/></svg>`,
  gear: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.6 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.6a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg>`,
  link: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7.07 0l3-3a5 5 0 0 0-7.07-7.07L11.17 5"/><path d="M14 11a5 5 0 0 0-7.07 0l-3 3a5 5 0 0 0 7.07 7.07L12.83 19"/></svg>`,
  handshake: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M2 12l4-4 5 5-1 1-3-3-4 4z"/><path d="M22 12l-4-4-5 5 1 1 3-3 4 4z"/><path d="M9 14l3-3 3 3-3 3z"/></svg>`,
  creator: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="4"/><path d="M4 21c0-4 4-7 8-7s8 3 8 7"/></svg>`,
  chart: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M3 20h18"/><rect x="5" y="13" width="3" height="7"/><rect x="11" y="8" width="3" height="12"/><rect x="17" y="4" width="3" height="16"/></svg>`,
  clipboard: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><rect x="6" y="4" width="12" height="17" rx="2"/><path d="M9 4V3a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v1"/><line x1="9" y1="10" x2="15" y2="10"/><line x1="9" y1="14" x2="15" y2="14"/><line x1="9" y1="18" x2="13" y2="18"/></svg>`,
};

const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((f) => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a },
  })),
};

const serviceJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  serviceType: 'KOL and influencer marketing in China',
  provider: {
    '@type': 'Organization',
    name: 'Beyond Border Group',
    url: 'https://beyondbordergroup.com',
  },
  areaServed: { '@type': 'Country', name: 'China' },
};
---

<Layout
  title="China KOL Agency: Influencer Marketing & Sales"
  description="We run KOL and KOC campaigns in China that sell, not just get views. Data-driven creator selection across Douyin, Xiaohongshu, Tmall and JD."
>
  <script type="application/ld+json" set:html={JSON.stringify(faqJsonLd)} is:inline />
  <script type="application/ld+json" set:html={JSON.stringify(serviceJsonLd)} is:inline />

  <div class="kol-progress" aria-hidden="true"><div class="kol-progress__bar"></div></div>
  <article class="kol">

    {/* ---------- HERO ---------- */}
    <section class="kol-hero" data-hero>
      <div class="kol-hero__bg" aria-hidden="true" data-parallax>
        <img src="/Images/grow-in-china/influencers-kols/hero-kol-livestream.png" alt="A young Chinese livestream host on a small home set, ring light glowing, holding a skincare bottle mid-pitch" />
      </div>
      <div class="kol-hero__overlay" aria-hidden="true"></div>
      <div class="kol-hero__grain" aria-hidden="true"></div>

      <div class="container kol-hero__inner">
        <nav class="kol-hero__crumbs" aria-label="Breadcrumb">
          <a href="/">Home</a>
          <span aria-hidden="true">/</span>
          <a href="/#services">Grow in China</a>
        </nav>
        <span class="eyebrow eyebrow--on-dark kol-reveal">Influencers &amp; KOLs</span>
        <h1 class="kol-hero__title kol-reveal" style="--reveal-delay: 80ms;">
          <span class="kol-hero__title-row">In the West, an influencer gets you noticed.</span>
          <span class="kol-hero__title-row kol-hero__title-row--accent">In China, they close the sale.</span>
        </h1>
        <p class="kol-hero__lead kol-reveal" style="--reveal-delay: 200ms;">
          On Instagram, the creator sits at the top of the funnel. In China, discovery,
          research and checkout happen on one screen, in the same minute. The KOL is not
          your awareness layer. They are standing at the register. So we pick creators on
          data, we brief them properly, and what we get paid is tied to what actually sold.
        </p>
        <div class="kol-hero__actions kol-reveal" style="--reveal-delay: 280ms;">
          <a href="/contact" class="btn btn--primary kol-magnetic">Talk to us about your campaign</a>
          <a href="/insights" class="btn btn--ghost-light kol-magnetic">Read our China insights</a>
        </div>

        <div class="kol-hero__meta kol-reveal" style="--reveal-delay: 360ms;">
          {heroStats.map((s) => (
            <div class="kol-hero__meta-item">
              <span class="kol-hero__meta-num">{s.num}{s.suffix}</span>
              <span class="kol-hero__meta-label">{s.label}</span>
            </div>
          ))}
        </div>
      </div>

      <a href="#why" class="kol-hero__scroll" aria-label="Scroll to next section">
        <span class="kol-hero__scroll-line"></span>
        <span class="kol-hero__scroll-text">Scroll</span>
      </a>
    </section>

    {/* ---------- WHY CHINA IS A DIFFERENT GAME ---------- */}
    <section id="why" class="section">
      <div class="container">
        <header class="section-header section-header--center kol-reveal">
          <span class="eyebrow">Why it&rsquo;s different</span>
          <h2>Why China is a different game</h2>
          <p class="section-header__lead">
            Most brands show up with the Western playbook and lose money for a quarter
            before they work out why. Three things it gets wrong.
          </p>
        </header>

        <ul class="kol-points kol-reveal">
          {whyPoints.map((p, i) => (
            <li class="kol-point" style={`--reveal-delay: ${i * 80}ms;`}>
              <span class="kol-point__mark" aria-hidden="true"></span>
              <div>
                <h3>{p.lead}</h3>
                <p>{p.body}</p>
              </div>
            </li>
          ))}
        </ul>

        <div class="kol-quotes">
          {whyQuotes.map((q, i) => (
            <figure class={`kol-quote-card kol-reveal`} style={`--reveal-delay: ${i * 120}ms;`}>
              <div class="kol-quote-card__media">
                <img src={q.image} alt={q.alt} loading="lazy" decoding="async" />
              </div>
              <blockquote class="kol-quote-card__body">
                <p>{q.quote}</p>
                <footer>
                  <cite>{q.cite}</cite>
                  <span class="kol-quote-card__meta">{q.citeMeta}</span>
                </footer>
              </blockquote>
            </figure>
          ))}
        </div>

        <p class="kol-aftertext kol-reveal">
          One last shift. The era of renting a single giant name is fading. Brand-led and
          store-led livestreams now drive most of Douyin&rsquo;s commerce, and top KOLs take
          a 40 to 50 percent cut. When a shopper looks you up, you want a dozen honest
          mentions. Not one paid celebrity post.
        </p>
      </div>
    </section>

    {/* ---------- SO HERE IS HOW WE RUN IT ---------- */}
    <section id="how" class="section section--alt">
      <div class="container">
        <header class="section-header section-header--center kol-reveal">
          <span class="eyebrow">What we do</span>
          <h2>So here is how we run it</h2>
          <p class="section-header__lead">
            One operator across casting, briefing, production and reporting. The plan that
            goes into the deck is the one that runs on the platform on Monday.
          </p>
        </header>

        <div class="kol-grid kol-grid--4">
          {howSteps.map((s, i) => (
            <article class="kol-card kol-reveal" style={`--reveal-delay: ${i * 60}ms;`}>
              <span class="kol-card__icon" aria-hidden="true" set:html={icons[s.icon]} />
              <h3 class="kol-card__title">{s.title}</h3>
              <p class="kol-card__body">{s.body}</p>
            </article>
          ))}
        </div>

        <blockquote class="kol-pull kol-reveal">
          <p>A million followers means nothing if half of them will never buy your category. We read audience composition before we read follower count. Every time.</p>
        </blockquote>
      </div>
    </section>

    {/* ---------- WHAT IS AN MCN ---------- */}
    <section id="mcn" class="section">
      <div class="container container--narrow">
        <header class="section-header kol-reveal">
          <span class="eyebrow">MCN</span>
          <h2>What is an MCN, and why it matters</h2>
        </header>

        <div class="kol-prose kol-reveal">
          <p>
            An MCN (multi-channel network) is an agency that signs creators and runs
            everything for them: content, contracts, brand deals, livestream operations.
          </p>
          <p>
            In China, MCNs run the creator economy. Most working KOLs and KOCs are signed
            to one. So when you book a creator, you are negotiating with their MCN, not the
            person, and the MCN works for the creator, not for you.
          </p>
          <p>
            We deal with MCNs every day, and we work around them when that serves you
            better. You want someone on your side of the table.
          </p>
        </div>

        <figure class="kol-figure kol-reveal">
          <img
            src="/Images/grow-in-china/influencers-kols/mcn-creator-signing.png"
            alt="A Chinese creator and an MCN manager reviewing a contract on a laptop in a plain office, fluorescent light overhead"
            loading="lazy"
            decoding="async"
          />
        </figure>
      </div>
    </section>

    {/* ---------- WHAT YOU ACTUALLY GET ---------- */}
    <section id="get" class="section section--alt">
      <div class="container">
        <header class="section-header section-header--center kol-reveal">
          <span class="eyebrow">What you get</span>
          <h2>What you actually get</h2>
          <p class="section-header__lead">
            Direct access, data-led selection, and reporting that links every yuan of spend
            back to a sale you can name.
          </p>
        </header>

        <div class="kol-grid kol-grid--4">
          {benefits.map((b, i) => (
            <article class="kol-card kol-card--benefit kol-reveal" style={`--reveal-delay: ${i * 60}ms;`}>
              <span class="kol-card__icon" aria-hidden="true" set:html={icons[b.icon]} />
              <h3 class="kol-card__lead">{b.lead}</h3>
              <p class="kol-card__body">{b.body}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    {/* ---------- PLATFORMS WE RUN ---------- */}
    <section id="platforms" class="section">
      <div class="container">
        <header class="section-header section-header--center kol-reveal">
          <span class="eyebrow">Platforms</span>
          <h2>Platforms we run</h2>
          <p class="section-header__lead">
            Seven platforms covered end to end. We pick the two or three that fit your
            category, not all of them at once.
          </p>
        </header>

        <div class="kol-platforms">
          {platforms.map((p, i) => (
            <article class="kol-platform kol-reveal" style={`--reveal-delay: ${i * 50}ms;`}>
              <header class="kol-platform__head">
                <h3 class="kol-platform__name">{p.name}</h3>
                {p.cn && <span class="kol-platform__cn" lang="zh">{p.cn}</span>}
              </header>
              <p class="kol-platform__desc">{p.desc}</p>
            </article>
          ))}
        </div>
      </div>
    </section>

    {/* ---------- FAQ ---------- */}
    <section id="faq" class="section section--alt">
      <div class="container container--narrow">
        <header class="section-header section-header--center kol-reveal">
          <span class="eyebrow">FAQ</span>
          <h2>Questions brands actually ask us</h2>
          <p class="section-header__lead">
            Twenty questions we hear on the first call. Filter by topic.
          </p>
        </header>

        <div class="kol-faq-filter kol-reveal" data-faq-filter role="tablist" aria-label="Filter FAQs by category">
          {faqCategories.map((c, i) => (
            <button
              type="button"
              class={`kol-faq-chip${i === 0 ? ' is-active' : ''}`}
              role="tab"
              aria-selected={i === 0 ? 'true' : 'false'}
              data-faq-cat={c}
            >
              <span class="kol-faq-chip__label">{c}</span>
              <span class="kol-faq-chip__count">{c === 'All' ? faqs.length : faqs.filter((f) => f.cat === c).length}</span>
            </button>
          ))}
        </div>

        <div class="kol-faq" data-faq-list>
          {faqs.map((f, i) => (
            <details
              class="kol-faq__item kol-reveal"
              data-faq-cat={f.cat}
              open={i === 0}
              style={`--reveal-delay: ${Math.min(i, 6) * 40}ms;`}
            >
              <summary class="kol-faq__q">
                <span class="kol-faq__cat-tag">{f.cat}</span>
                <span class="kol-faq__q-text">{f.q}</span>
                <span class="kol-faq__icon" aria-hidden="true">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
                </span>
              </summary>
              <div class="kol-faq__a">
                <p>{f.a}</p>
              </div>
            </details>
          ))}
        </div>
      </div>
    </section>

    {/* ---------- FINAL CTA ---------- */}
    <section class="section section--tight">
      <div class="container container--narrow">
        <div class="kol-cta">
          <span class="eyebrow eyebrow--on-dark">Let&rsquo;s talk</span>
          <h2 class="kol-cta__title">One thing before you go</h2>
          <p class="kol-cta__lead">
            The brands that win in China are not the ones with the biggest influencer
            budget. They are the ones that started while the channel was still underpriced.
            That window is open right now. It will not stay open forever. We consult, and
            we run what we recommend. If you want a KOL and KOC program built on data and
            judged on sales, get in touch.
          </p>
          <div class="kol-cta__actions">
            <a href="/contact" class="btn kol-cta__btn-primary">Contact us</a>
          </div>
        </div>
      </div>
    </section>
  </article>

  <style is:global>
    .kol .container--narrow { max-width: 880px; }

    /* ---------- REVEAL + MOTION ---------- */
    @media (prefers-reduced-motion: no-preference) {
      .kol-reveal {
        opacity: 0;
        transform: translateY(16px);
        transition:
          opacity 700ms cubic-bezier(0.16, 1, 0.3, 1) var(--reveal-delay, 0ms),
          transform 700ms cubic-bezier(0.16, 1, 0.3, 1) var(--reveal-delay, 0ms);
      }
      .kol-reveal.is-in { opacity: 1; transform: translateY(0); }
      .kol-magnetic {
        will-change: transform;
        transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1),
                    box-shadow 220ms ease, background-color 200ms ease, color 200ms ease;
      }
      .kol-magnetic:hover { transform: translateY(-2px); }
    }

    /* ---------- SCROLL PROGRESS BAR ---------- */
    .kol-progress {
      position: fixed; top: 0; left: 0; right: 0;
      height: 3px; z-index: 1000;
      background: transparent; pointer-events: none;
    }
    .kol-progress__bar {
      height: 100%; width: var(--progress, 0%);
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 60%, #9DDBF5 100%);
      box-shadow: 0 0 12px rgba(38, 154, 218, 0.55);
      transition: width 80ms linear;
    }

    /* ---------- HERO ---------- */
    .kol-hero {
      position: relative;
      min-height: clamp(360px, 48vh, 500px);
      display: flex; align-items: flex-start;
      padding: clamp(8px, 1.25vh, 16px) 0 clamp(48px, 7vh, 80px);
      color: var(--bbg-white);
      overflow: hidden; isolation: isolate;
    }
    body:has(.kol-hero) .kol-hero { padding-top: clamp(112px, 10vh, 144px); }
    @media (max-width: 1024px) {
      body:has(.kol-hero) .kol-hero { padding-top: clamp(96px, 9vh, 116px); }
    }
    .kol-hero__bg { position: absolute; inset: -10% 0 0; z-index: -3; }
    .kol-hero__bg img {
      width: 100%; height: 110%;
      object-fit: cover; object-position: center;
      transform: translate3d(0, var(--parallax-y, 0px), 0) scale(1.04);
      transition: transform 80ms linear;
      filter: saturate(0.95);
    }
    .kol-hero__overlay {
      position: absolute; inset: 0; z-index: -2;
      background:
        linear-gradient(110deg,
          rgba(8, 18, 28, 0.92) 0%,
          rgba(8, 18, 28, 0.72) 42%,
          rgba(8, 18, 28, 0.32) 100%),
        radial-gradient(120% 80% at 0% 100%,
          rgba(38, 154, 218, 0.28) 0%,
          rgba(38, 154, 218, 0) 60%);
    }
    .kol-hero__grain {
      position: absolute; inset: 0; z-index: -1;
      background-image: radial-gradient(rgba(255, 255, 255, 0.06) 1px, transparent 1px);
      background-size: 3px 3px;
      mix-blend-mode: overlay; opacity: 0.45;
      pointer-events: none;
    }
    .kol-hero__inner { max-width: 980px; position: relative; }
    .kol-hero__crumbs {
      margin-bottom: var(--space-4);
      font-size: var(--fs-small);
      display: flex; gap: 0.5rem; align-items: center;
      color: rgba(255, 255, 255, 0.7);
    }
    .kol-hero__crumbs a { color: rgba(255, 255, 255, 0.85); font-weight: 500; }
    .kol-hero__crumbs a:hover { color: var(--bbg-white); }
    .kol-hero__inner .eyebrow--on-dark {
      display: inline-block;
      margin-bottom: var(--space-3);
      color: #9DDBF5;
    }
    .kol-hero__title {
      font-size: clamp(2rem, 3.4vw + 1rem, 3.75rem);
      line-height: 1.06;
      letter-spacing: -0.025em;
      color: var(--bbg-white);
      margin: 0 0 var(--space-4);
      max-width: 24ch;
      font-weight: 700;
      display: flex; flex-direction: column;
      gap: 0.05em;
    }
    .kol-hero__title-row { display: block; }
    .kol-hero__title-row--accent {
      color: #9DDBF5; font-weight: 500;
      font-style: italic; letter-spacing: -0.018em;
    }
    .kol-hero__lead {
      font-size: clamp(1rem, 0.5vw + 1rem, 1.1875rem);
      line-height: 1.55;
      color: rgba(255, 255, 255, 0.86);
      max-width: 62ch;
      margin: 0 0 var(--space-5);
    }
    .kol-hero__actions { display: flex; flex-wrap: wrap; gap: var(--space-3); margin-bottom: var(--space-6); }

    .kol-hero__meta {
      display: flex; flex-wrap: nowrap;
      gap: clamp(var(--space-3), 3vw, var(--space-6));
      padding-top: var(--space-5);
      border-top: 1px solid rgba(255, 255, 255, 0.14);
      max-width: none;
    }
    .kol-hero__meta-item {
      display: flex; flex-direction: column;
      gap: 0.2rem;
      min-width: 0;
      flex: 1 1 0;
    }
    @media (max-width: 720px) {
      .kol-hero__meta { flex-wrap: wrap; }
      .kol-hero__meta-item { flex: 1 1 auto; }
    }
    .kol-hero__meta-num {
      font-size: clamp(1.625rem, 1.2vw + 1rem, 2.125rem);
      font-weight: 700; letter-spacing: -0.02em;
      line-height: 1; color: var(--bbg-white);
      font-feature-settings: "tnum";
    }
    .kol-hero__meta-label {
      font-size: 0.85rem;
      color: rgba(255, 255, 255, 0.66);
      letter-spacing: 0.02em;
    }

    .kol-hero__scroll {
      position: absolute;
      bottom: var(--space-5);
      right: clamp(var(--space-5), 4vw, var(--space-7));
      display: flex; flex-direction: column;
      align-items: center; gap: 0.5rem;
      color: rgba(255, 255, 255, 0.7);
      font-size: 0.7rem; letter-spacing: 0.18em;
      text-transform: uppercase; font-weight: 600;
      transition: color 200ms ease;
    }
    .kol-hero__scroll:hover { color: var(--bbg-white); }
    .kol-hero__scroll-line {
      width: 1px; height: 40px;
      background: linear-gradient(180deg, transparent 0%, rgba(255, 255, 255, 0.6) 100%);
      position: relative; overflow: hidden;
    }
    .kol-hero__scroll-line::after {
      content: ""; position: absolute;
      top: -100%; left: 0; right: 0;
      height: 50%;
      background: linear-gradient(180deg, transparent 0%, var(--bbg-white) 100%);
      animation: kol-scroll-tick 1.8s ease-in-out infinite;
    }
    @keyframes kol-scroll-tick { 0% { top: -50%; } 100% { top: 100%; } }
    @media (max-width: 720px) {
      .kol-hero__scroll { display: none; }
      .kol-hero__meta { gap: var(--space-4); }
    }

    /* ---------- WHY POINTS ---------- */
    .kol-points {
      list-style: none;
      padding: 0;
      margin: var(--space-6) auto 0;
      max-width: 880px;
      display: grid;
      gap: var(--space-5);
    }
    .kol-point {
      display: grid;
      grid-template-columns: auto 1fr;
      gap: var(--space-5);
      align-items: start;
      padding: var(--space-5) 0;
      border-top: 1px solid var(--bbg-border);
    }
    .kol-point:last-child { border-bottom: 1px solid var(--bbg-border); }
    .kol-point__mark {
      display: block;
      width: 6px;
      height: 28px;
      border-radius: 3px;
      background: linear-gradient(180deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      margin-top: 0.6rem;
    }
    .kol-point h3 {
      font-size: clamp(1.125rem, 0.6vw + 1rem, 1.375rem);
      line-height: 1.3;
      margin: 0 0 var(--space-3);
      letter-spacing: -0.005em;
      color: var(--bbg-ink);
    }
    .kol-point p {
      font-size: 1.0625rem;
      line-height: 1.65;
      color: var(--bbg-text);
      margin: 0;
    }
    @media (max-width: 640px) {
      .kol-point { grid-template-columns: 1fr; gap: var(--space-2); }
      .kol-point__mark { width: 36px; height: 4px; margin-top: 0; }
    }

    /* ---------- QUOTE CARDS (stats) ---------- */
    .kol-quotes {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-5);
      margin-top: var(--space-7);
    }
    .kol-quote-card {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      overflow: hidden;
      margin: 0;
      display: flex; flex-direction: column;
      transition: border-color var(--dur) var(--ease), box-shadow var(--dur) var(--ease);
    }
    .kol-quote-card:hover {
      border-color: var(--bbg-blue);
      box-shadow: 0 16px 32px -22px rgba(38, 154, 218, 0.4);
    }
    .kol-quote-card__media {
      aspect-ratio: 16 / 9;
      overflow: hidden;
      background: var(--bbg-border);
    }
    .kol-quote-card__media img {
      width: 100%; height: 100%;
      object-fit: cover; display: block;
      transition: transform 600ms var(--ease);
    }
    .kol-quote-card:hover .kol-quote-card__media img { transform: scale(1.03); }
    .kol-quote-card__body {
      padding: var(--space-6);
      margin: 0;
      border-left: 4px solid var(--bbg-blue);
      background: linear-gradient(180deg, rgba(232, 244, 252, 0.4) 0%, rgba(232, 244, 252, 0) 100%);
    }
    .kol-quote-card__body p {
      margin: 0 0 var(--space-4);
      font-size: 1.0625rem;
      line-height: 1.55;
      color: var(--bbg-ink);
      font-weight: 500;
      font-style: italic;
    }
    .kol-quote-card__body footer {
      display: flex; flex-direction: column;
      gap: 0.2rem;
      padding-top: var(--space-3);
      border-top: 1px solid var(--bbg-border);
    }
    .kol-quote-card__body cite {
      font-style: normal;
      font-weight: 600;
      font-size: 0.875rem;
      color: var(--bbg-ink);
    }
    .kol-quote-card__meta {
      font-size: 0.8rem;
      color: var(--bbg-text-muted);
      line-height: 1.4;
    }
    @media (max-width: 900px) {
      .kol-quotes { grid-template-columns: 1fr; }
    }

    .kol-aftertext {
      max-width: 760px;
      margin: var(--space-7) auto 0;
      font-size: 1.0625rem;
      line-height: 1.7;
      color: var(--bbg-text);
      text-align: center;
    }

    /* ---------- 4-CARD GRID ---------- */
    .kol-grid {
      display: grid;
      gap: var(--space-5);
    }
    .kol-grid--4 {
      grid-template-columns: repeat(4, 1fr);
    }
    @media (max-width: 1024px) {
      .kol-grid--4 { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 600px) {
      .kol-grid--4 { grid-template-columns: 1fr; }
    }
    .kol-card {
      position: relative;
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      padding: var(--space-6);
      display: flex; flex-direction: column;
      gap: var(--space-3);
      transition:
        transform 320ms cubic-bezier(0.16, 1, 0.3, 1),
        border-color 260ms ease,
        box-shadow 260ms ease;
      overflow: hidden;
    }
    .kol-card::before {
      content: "";
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      transform: scaleX(0);
      transform-origin: left center;
      transition: transform 360ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .kol-card:hover {
      transform: translateY(-4px);
      border-color: var(--bbg-blue);
      box-shadow: 0 16px 32px -20px rgba(38, 154, 218, 0.4);
    }
    .kol-card:hover::before { transform: scaleX(1); }
    .kol-card__icon {
      display: inline-flex;
      width: 44px; height: 44px;
      align-items: center; justify-content: center;
      border-radius: 12px;
      background: linear-gradient(135deg, var(--bbg-blue-light) 0%, rgba(38, 154, 218, 0.18) 100%);
      color: var(--bbg-blue);
      margin-bottom: var(--space-2);
    }
    .kol-card__icon svg { width: 22px; height: 22px; }
    .kol-card__title {
      font-size: 1.125rem;
      line-height: 1.25;
      margin: 0;
      color: var(--bbg-ink);
      letter-spacing: -0.005em;
    }
    .kol-card__lead {
      font-size: 1.0625rem;
      line-height: 1.3;
      margin: 0;
      color: var(--bbg-ink);
      letter-spacing: -0.005em;
    }
    .kol-card__body {
      font-size: 0.95rem;
      line-height: 1.6;
      color: var(--bbg-text);
      margin: 0;
    }

    /* ---------- PULL QUOTE ---------- */
    .kol-pull {
      margin: var(--space-7) auto 0;
      max-width: 820px;
      padding: var(--space-6) var(--space-7);
      background: linear-gradient(135deg, var(--bbg-blue-light) 0%, rgba(38, 154, 218, 0.10) 100%);
      border-left: 4px solid var(--bbg-blue);
      border-radius: 0 var(--radius-md) var(--radius-md) 0;
    }
    .kol-pull p {
      margin: 0;
      font-size: clamp(1.0625rem, 0.4vw + 1rem, 1.25rem);
      line-height: 1.5;
      color: var(--bbg-ink);
      font-weight: 500;
      font-style: italic;
    }

    /* ---------- PROSE ---------- */
    .kol-prose {
      margin-top: var(--space-5);
    }
    .kol-prose p {
      font-size: 1.0625rem;
      line-height: 1.75;
      color: var(--bbg-text);
      margin: 0 0 var(--space-4);
    }
    .kol-prose p:last-child { margin-bottom: 0; }

    .kol-figure {
      margin: var(--space-7) 0 0;
      border-radius: var(--radius-lg);
      overflow: hidden;
      border: 1px solid var(--bbg-border);
      aspect-ratio: 16 / 9;
      background: var(--bbg-border);
    }
    .kol-figure img {
      width: 100%; height: 100%;
      object-fit: cover; display: block;
    }

    /* ---------- PLATFORMS ---------- */
    .kol-platforms {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: var(--space-4);
    }
    .kol-platforms > :nth-child(5),
    .kol-platforms > :nth-child(6),
    .kol-platforms > :nth-child(7) {
      grid-column: span 1;
    }
    .kol-platforms > :last-child {
      grid-column: span 2;
    }
    @media (max-width: 1024px) {
      .kol-platforms { grid-template-columns: repeat(2, 1fr); }
      .kol-platforms > :last-child { grid-column: span 2; }
    }
    @media (max-width: 600px) {
      .kol-platforms { grid-template-columns: 1fr; }
      .kol-platforms > :last-child { grid-column: span 1; }
    }
    .kol-platform {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-md);
      padding: var(--space-5);
      transition: border-color 240ms ease, box-shadow 240ms ease, transform 280ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .kol-platform:hover {
      transform: translateY(-3px);
      border-color: var(--bbg-blue);
      box-shadow: 0 14px 28px -22px rgba(38, 154, 218, 0.4);
    }
    .kol-platform__head {
      display: flex;
      align-items: baseline;
      gap: var(--space-3);
      margin-bottom: var(--space-3);
      flex-wrap: wrap;
    }
    .kol-platform__name {
      font-size: 1.0625rem;
      margin: 0;
      letter-spacing: -0.005em;
      color: var(--bbg-ink);
    }
    .kol-platform__cn {
      font-size: 0.95rem;
      color: var(--bbg-blue);
      font-weight: 500;
    }
    .kol-platform__desc {
      font-size: 0.92rem;
      line-height: 1.55;
      color: var(--bbg-text);
      margin: 0;
    }

    /* ---------- FAQ ---------- */
    .kol-faq {
      display: flex; flex-direction: column;
      gap: var(--space-3);
    }
    .kol-faq__item {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-md);
      overflow: hidden;
      transition: border-color 240ms ease, box-shadow 240ms ease, transform 280ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .kol-faq__item:hover {
      transform: translateX(2px);
      border-color: rgba(38, 154, 218, 0.4);
    }
    .kol-faq__item[open] {
      border-color: var(--bbg-blue);
      box-shadow: 0 8px 22px -14px rgba(38, 154, 218, 0.4);
    }
    .kol-faq__q {
      cursor: pointer;
      padding: var(--space-5);
      display: flex; align-items: center;
      justify-content: space-between;
      gap: var(--space-4);
      font-weight: 600;
      font-size: 1.0625rem; line-height: 1.3;
      color: var(--bbg-ink);
      list-style: none;
      transition: color var(--dur) var(--ease);
    }
    .kol-faq__q::-webkit-details-marker { display: none; }
    .kol-faq__q::marker { display: none; }
    .kol-faq__item:hover .kol-faq__q { color: var(--bbg-blue); }
    .kol-faq__icon {
      flex-shrink: 0;
      width: 28px; height: 28px;
      display: inline-flex;
      align-items: center; justify-content: center;
      border-radius: 50%;
      background: var(--bbg-surface);
      color: var(--bbg-text-muted);
      transition: transform 320ms cubic-bezier(0.16, 1, 0.3, 1),
                  color var(--dur) var(--ease),
                  background var(--dur) var(--ease);
    }
    .kol-faq__item[open] .kol-faq__icon {
      transform: rotate(180deg);
      color: var(--bbg-white);
      background: var(--bbg-blue);
    }
    .kol-faq__a {
      padding: 0 var(--space-5) var(--space-5);
      color: var(--bbg-text);
      line-height: 1.65;
      font-size: 1rem;
      animation: kol-faq-in 320ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    @keyframes kol-faq-in {
      from { opacity: 0; transform: translateY(-4px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .kol-faq__a p { margin: 0; }

    .kol-faq-filter {
      display: flex; flex-wrap: wrap;
      gap: 0.5rem; justify-content: center;
      margin-bottom: var(--space-6);
      overflow-x: auto;
    }
    .kol-faq-chip {
      display: inline-flex; align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 0.95rem;
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: 999px;
      cursor: pointer;
      font-size: 0.875rem;
      font-weight: 600;
      color: var(--bbg-text);
      transition: all 220ms ease;
      font-family: inherit;
      white-space: nowrap;
    }
    .kol-faq-chip:hover { border-color: var(--bbg-blue); color: var(--bbg-blue); }
    .kol-faq-chip.is-active {
      background: var(--bbg-blue);
      border-color: var(--bbg-blue);
      color: var(--bbg-white);
    }
    .kol-faq-chip__count {
      font-size: 0.72rem; font-weight: 700;
      padding: 0.15em 0.5em;
      border-radius: 999px;
      background: var(--bbg-surface);
      color: var(--bbg-text-muted);
      transition: all 220ms ease;
    }
    .kol-faq-chip:hover .kol-faq-chip__count { background: var(--bbg-blue-light); color: var(--bbg-blue); }
    .kol-faq-chip.is-active .kol-faq-chip__count {
      background: rgba(255, 255, 255, 0.2);
      color: var(--bbg-white);
    }
    .kol-faq__cat-tag {
      display: inline-block;
      font-size: 0.68rem; font-weight: 700;
      letter-spacing: 0.08em; text-transform: uppercase;
      color: var(--bbg-blue);
      background: var(--bbg-blue-light);
      padding: 0.25em 0.6em;
      border-radius: 4px;
      margin-right: var(--space-3);
      flex-shrink: 0;
    }
    .kol-faq__q-text { flex-grow: 1; }
    .kol-faq__item.is-hidden { display: none; }

    /* ---------- FINAL CTA ---------- */
    .kol-cta {
      padding: clamp(var(--space-6), 4vw + 1rem, var(--space-8)) var(--space-6);
      background: linear-gradient(135deg, var(--bbg-blue) 0%, var(--bbg-blue-dark) 100%);
      color: var(--bbg-white);
      border-radius: var(--radius-lg);
      text-align: center;
      position: relative; overflow: hidden;
    }
    .kol-cta::before {
      content: ""; position: absolute;
      top: -40%; right: -10%;
      width: 420px; height: 420px;
      background: radial-gradient(circle at center, rgba(255,255,255,0.18) 0%, rgba(255,255,255,0) 65%);
      pointer-events: none;
    }
    .kol-cta .eyebrow--on-dark {
      display: inline-block;
      margin-bottom: var(--space-4);
      color: rgba(255, 255, 255, 0.9);
    }
    .kol-cta__title {
      font-size: clamp(1.75rem, 1.5vw + 1rem, 2.5rem);
      line-height: 1.15;
      color: var(--bbg-white);
      margin: 0 0 var(--space-4);
      letter-spacing: -0.015em;
    }
    .kol-cta__lead {
      color: rgba(255, 255, 255, 0.92);
      font-size: 1.0625rem;
      max-width: 60ch;
      margin: 0 auto var(--space-5);
      line-height: 1.6;
    }
    .kol-cta__actions {
      display: flex; flex-wrap: wrap;
      gap: var(--space-3);
      justify-content: center;
      position: relative;
    }
    .kol-cta__btn-primary {
      background: var(--bbg-white);
      color: var(--bbg-blue-dark);
      border: 1px solid var(--bbg-white);
    }
    .kol-cta__btn-primary:hover {
      background: rgba(255, 255, 255, 0.92);
      color: var(--bbg-blue-dark);
    }
  </style>

  <script is:inline>
    (() => {
      const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      // Reveal-on-scroll
      const revealEls = document.querySelectorAll('.kol-reveal');
      if (revealEls.length) {
        if (reduced || !('IntersectionObserver' in window)) {
          revealEls.forEach((el) => el.classList.add('is-in'));
        } else {
          const io = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add('is-in');
                io.unobserve(entry.target);
              }
            });
          }, { threshold: 0.12, rootMargin: '0px 0px -8% 0px' });
          revealEls.forEach((el) => io.observe(el));
        }
      }

      // Hero parallax + scroll progress
      const parallaxEl = document.querySelector('[data-parallax]');
      const progressEl = document.querySelector('.kol-progress__bar');
      const heroEl = document.querySelector('[data-hero]');
      let ticking = false;
      const onScroll = () => {
        if (ticking) return;
        ticking = true;
        requestAnimationFrame(() => {
          const scrollY = window.scrollY || window.pageYOffset;
          if (parallaxEl && heroEl && !reduced) {
            const heroH = heroEl.offsetHeight;
            if (scrollY < heroH) {
              parallaxEl.style.setProperty('--parallax-y', (scrollY * 0.18) + 'px');
            }
          }
          if (progressEl) {
            const docH = document.documentElement.scrollHeight - window.innerHeight;
            const pct = docH > 0 ? Math.min(100, (scrollY / docH) * 100) : 0;
            progressEl.style.setProperty('--progress', pct + '%');
          }
          ticking = false;
        });
      };
      window.addEventListener('scroll', onScroll, { passive: true });
      onScroll();

      // Smooth scroll for in-page anchors
      document.querySelectorAll('a[href^="#"]').forEach((link) => {
        link.addEventListener('click', (e) => {
          const id = link.getAttribute('href');
          if (!id || id === '#') return;
          const target = document.querySelector(id);
          if (!target) return;
          e.preventDefault();
          target.scrollIntoView({ behavior: reduced ? 'auto' : 'smooth', block: 'start' });
          history.replaceState(null, '', id);
        });
      });

      // FAQ accordion - close others on open
      const faqItems = document.querySelectorAll('.kol-faq__item');
      faqItems.forEach((item) => {
        item.addEventListener('toggle', () => {
          if (item.open) {
            faqItems.forEach((other) => {
              if (other !== item && other.open) other.open = false;
            });
          }
        });
      });

      // FAQ category filter
      const faqFilter = document.querySelector('[data-faq-filter]');
      const faqList = document.querySelector('[data-faq-list]');
      if (faqFilter && faqList) {
        const chips = Array.from(faqFilter.querySelectorAll('.kol-faq-chip'));
        const items = Array.from(faqList.querySelectorAll('.kol-faq__item'));
        chips.forEach((chip) => {
          chip.addEventListener('click', () => {
            const cat = chip.dataset.faqCat;
            chips.forEach((c) => {
              const on = c === chip;
              c.classList.toggle('is-active', on);
              c.setAttribute('aria-selected', on ? 'true' : 'false');
            });
            items.forEach((item) => {
              const matches = cat === 'All' || item.dataset.faqCat === cat;
              item.classList.toggle('is-hidden', !matches);
            });
          });
        });
      }
    })();
  </script>
</Layout>
