---
import Layout from '../../layouts/Layout.astro';

const promises = [
  {
    title: 'Senior operators on your account, every day',
    desc: 'Cyril or Liyan in your kickoff, your monthly review and your war room. Not just at pitch.',
  },
  {
    title: 'One team across store, media, content and livestream',
    desc: 'Not four agencies, four briefs and four invoices. One owner of the P&L.',
  },
  {
    title: 'Transparent fees, no platform kickbacks',
    desc: 'We are paid by you. Not by Alimama. Not by JD. Not by KOL agencies. Your margin stays your margin.',
  },
];

const models = [
  {
    name: 'TP model',
    tag: 'You own the inventory, we run the store',
    desc: 'You keep the stock on your books and the customer relationship in your name. We act as your Tmall Partner, JD Partner or Douyin EC partner. Operations, content, media, livestream, customer service: all run by us, all under your brand.',
    when: 'You want the China P&L on your own books, you want to build long-term brand equity in China, and you have the working capital to fund inventory in the bonded warehouse.',
    owns: [
      { label: 'Inventory', value: 'Yours, in a bonded warehouse' },
      { label: 'Store license', value: 'Yours' },
      { label: 'Customer data', value: 'Yours' },
      { label: 'Content, media, contracts', value: 'Yours' },
      { label: 'Media spend', value: '100% funded by you' },
      { label: 'Revenue', value: 'Direct-to-consumer GMV, on your books' },
    ],
    yourSide: [
      'Full retail margin on every sale',
      'Direct visibility into customer data and CRM',
      'Brand control across the entire storefront',
      'Monthly P&L review with the senior leads',
    ],
    ourSide: [
      'Daily store operations, merchandising, listing updates',
      'Paid media across Alimama, JD media and Douyin Qianchuan',
      'Livestream, KOL and KOC campaigns within the eCommerce platforms',
      'Mandarin customer service and review moderation',
      'Platform compliance, takedowns and penalty negotiation',
    ],
    image: '/Images/grow-in-china/decision-tp-unboxing.png',
    imageAlt: 'A Chinese student in a tier-2 city apartment unboxing a cross-border parcel with imported products on the floor',
  },
  {
    name: 'Distributor model',
    tag: 'We own the inventory, we run the store',
    desc: 'We buy your product at wholesale and own the stock in China. The store is operated under our license, the inventory sits on our balance sheet, and we carry the commercial risk. You sell to us, we sell to the consumer.',
    when: 'You already have brand awareness in China, you want China revenue without China complexity, you do not want to fund inventory or carry stock risk, and you want a single accountable partner buying your product and selling it through. New-to-market brands rarely fit this model.',
    owns: [
      { label: 'Inventory', value: 'Ours' },
      { label: 'Store license', value: 'Ours' },
      { label: 'Customer data', value: 'Ours' },
      { label: 'Content, media, contracts', value: 'Ours' },
      { label: 'Media spend', value: 'Brand budget + BBG co-invest' },
      { label: 'Revenue', value: 'Wholesale invoice, recognised as B2B' },
    ],
    yourSide: [
      'Wholesale revenue with no inventory risk',
      'A single B2B invoice instead of a complex direct-to-consumer P&L',
      'No working capital tied up in China stock',
      'Aggregated performance reporting and category insight',
    ],
    ourSide: [
      'Inventory purchase, financing and warehousing',
      'Everything in the TP model, plus the commercial risk',
      'Pricing strategy across platforms within agreed brand guardrails',
      'Returns, refunds, customer service liability',
    ],
    image: '/Images/grow-in-china/decision-distributor-mother.png',
    imageAlt: 'A young Chinese mother on a sofa watching a livestream commerce broadcast on her tablet while her daughter plays nearby',
    highlight: false,
  },
];

const compareRows = [
  { label: 'Who owns the inventory', tp: 'You', dist: 'BBG' },
  { label: 'Who owns the store and the data', tp: 'You', dist: 'BBG' },
  { label: 'Working capital you put in', tp: 'High', dist: 'None' },
  { label: 'Revenue you book', tp: 'Retail GMV minus fees', dist: 'Wholesale price' },
  { label: 'Margin upside', tp: 'Full retail margin', dist: 'Capped at wholesale' },
  { label: 'Risk you carry', tp: 'Stock and demand', dist: 'None on inventory' },
  { label: 'Media and promotion costs', tp: '100% on you', dist: 'Brand budget + BBG co-invest' },
  { label: 'Brand equity in China', tp: 'Yours, fully', dist: 'Yours, built through us' },
  { label: 'Best for', tp: 'Brands building long-term China P&L', dist: 'Brands already known in China' },
];

const costCards = [
  {
    icon: 'wallet',
    title: 'What you pay us',
    sections: [
      {
        heading: 'TP model',
        body: 'Monthly retainer starting from $7,500 plus a 10% revenue share on GMV, with movement up or down depending on category and platforms covered. Media and content production billed at pass-through.',
      },
      {
        heading: 'Distributor model',
        body: 'No retainer, no revenue share. Our margin sits in the retail price. Wholesale terms depend on volume commitment, exclusivity and the campaign calendar.',
      },
    ],
    footer: 'We rarely take on new-to-market brands in the distributor model. The brand-building lift from zero is too costly unless the brand commits a serious launch fund. For most new entrants, TP is the right answer.',
  },
  {
    icon: 'platform-fee',
    title: 'What you pay the platforms',
    sections: [
      {
        heading: '',
        body: 'Platform fees on Tmall Global and JD Worldwide are quoted off the official Chinese-language fee schedule, not estimated. Three buckets to plan for:',
      },
    ],
    bullets: [
      'Annual platform fee. Flagship stores on Tmall Global run a fixed annual fee plus a refundable deposit. JD Worldwide is similar with different brackets.',
      'Transaction commission. Roughly 2% to 5% on each sale, depending on category. Beauty and supplements sit at the high end. Hard goods at the low end.',
      'Payment gateway. Alipay and WeChat Pay each take a small percentage on every transaction.',
    ],
    footer: 'These are the same numbers every brand pays. We do not mark them up.',
  },
  {
    icon: 'megaphone',
    title: 'What you put behind the brand',
    sections: [
      {
        heading: '',
        body: 'The variable line, and usually the biggest one in year one. Who funds it depends on the model.',
      },
      {
        heading: 'TP model',
        body: 'You fund 100% of media, content and promotion. We execute against your approved budget. Every yuan flows through your platform account, fully visible to you.',
      },
      {
        heading: 'Distributor model',
        body: 'The brand provides a marketing budget for the year, and we deploy it against a co-agreed plan. BBG often co-invests from retail margin on top, particularly on always-on platform media and promotional mechanics for 618 and 11.11.',
      },
    ],
    footer: 'Typical ranges, regardless of who pays: paid media from $120,000 across the first twelve months. Content production billed at cost plus a small handling fee. KOL and KOC booked through us, invoiced by the talent agency. We do not take referral commissions on KOL spend, ever.',
  },
];

const steps = [
  {
    icon: 'compass',
    phase: 'Phase one',
    duration: '2 to 3 weeks',
    title: 'Audit and growth diagnosis',
    desc: 'We pull the store apart and rebuild the picture across traffic, conversion, retention, content and media efficiency. You walk away with three ranked growth levers, the numbers behind them, and a 90-day priority plan.',
  },
  {
    icon: 'palette',
    phase: 'Phase two',
    duration: '4 to 6 weeks',
    title: 'Store and content rebuild',
    desc: 'New PDPs, new banners, new video, a working livestream room. The store starts looking like it sells in 2026, not 2021, and the content survives platform algorithm changes.',
  },
  {
    icon: 'rocket',
    phase: 'Phase three',
    duration: '4 to 8 weeks',
    title: 'Media and traffic activation',
    desc: 'Paid search, paid display, paid livestream, KOL and KOC. We turn on the taps across Alimama, JD media and Douyin Qianchuan, with RED seeding feeding the upper funnel. Every yuan tied to conversion.',
  },
  {
    icon: 'trend',
    phase: 'Phase four',
    duration: 'Always on, annual reset',
    title: 'Operations and scale',
    desc: 'Daily operations, weekly optimisation, monthly business review, quarterly campaign planning. We run the calendar (618, 99, 11.11, Double 12, Chinese New Year) and reset the priority list with a fresh audit every twelve months.',
  },
];

const sisterPackages = [
  {
    title: 'Always-on social',
    desc: 'Daily WeChat, RED, Douyin and Weibo run alongside your flagship store. For brands whose store is live and needs constant content fuel.',
  },
  {
    title: 'Launch sprint',
    desc: 'A three-month intensive across all four channels around a Tmall Global or JD Worldwide launch. For the opening wave.',
  },
  {
    title: 'Festival drive',
    desc: 'Campaign-specific social activation for 618, 11.11, Double 12 or Chinese New Year. For brands where the calendar moment matters more than the always-on cadence.',
  },
];

const contextCards = [
  {
    image: '/Images/grow-in-china/truth-comparing-products.png',
    alt: 'A Chinese man in his late 30s comparing two imported product boxes while looking at a Chinese eCommerce app in a quiet cafe',
    title: 'The team that wrote the plan stays on the store',
    body: 'Most agencies hand strategy to one team and execution to another, then disappear into monthly reporting. We keep strategy, planning and operations under one roof, and the senior who wrote the plan sits in on the weekly ops meeting. The strategy does not drift, and the operations team is not guessing at intent.',
  },
  {
    image: '/Images/enter-china/team.png',
    alt: 'The Beyond Border Group eCommerce operations team working in an open office',
    title: 'Operators first, strategists second',
    body: 'Our senior team ran stores at Publicis Commerce China for global beauty, FMCG and lifestyle brands. They have lost a Double 11 and won the next one. When they tell you what will work in March, it is because they were inside the platform in February.',
  },
];

const faqs = [
  { cat: 'Operations', q: 'What does running a Tmall Global store actually involve day to day?', a: 'More than people expect. Daily store updates, banner swaps, promotion setup, inventory sync with the bonded warehouse, Mandarin customer service, order checks, returns, review moderation, livestream prep. A flagship store left untouched for a week loses ranking. Leave it untouched for a month and it loses category position. The work is steady, not glamorous, and it is what separates the stores that grow from the ones that just exist.' },
  { cat: 'Operations', q: 'How fast can you take over an existing store?', a: 'Two to four weeks for an audit and clean handover. Longer if the previous partner is uncooperative (it happens). We pull everything first: backend access, media accounts, customer data, content library, supplier contracts. Then we map what is missing. We run two weeks in parallel before cutting over, so you do not lose a campaign window in the transition.' },
  { cat: 'Operations', q: 'Do you handle customer service in Mandarin?', a: 'Yes. Native Mandarin team working platform hours (9 a.m. to 11 p.m. local time, longer during 11.11). Tmall Global, JD Worldwide and Douyin EC Global all measure response time and bake it into your store score. We hold response time under 30 seconds and we keep ratings above platform average.' },
  { cat: 'Operations', q: 'How do you handle logistics and bonded warehouse?', a: 'We coordinate with your existing bonded warehouse provider, or we introduce one if you do not have one. Most Tmall Global brands run through Cainiao bonded warehouses. For JD Worldwide, JD’s own bonded network is usually the faster route. We track stock, file customs, handle returns and chase delays. Logistics is not glamorous. It is just the difference between a 4.9 store rating and a 4.5.' },
  { cat: 'Operations', q: 'What happens when an SKU sells out mid-campaign?', a: 'We flag it before it happens. Stock reorder triggers are set in the dashboard. If you cannot replenish in time, we shift the campaign weight to a sister SKU, redirect paid media, and protect the store rating with a clear holding page. The worst outcome is a silent stockout, and we make sure that does not happen.' },
  { cat: 'Operations', q: 'How do you handle returns and fake reviews?', a: 'Returns are managed by the bonded warehouse partner under our coordination. Return rates typically run 8% to 15% in beauty, 15% to 25% in fashion, under 5% in F&B and supplements. Fake reviews and review-bombing attacks happen, especially after a viral moment. We monitor daily, file removal requests with the platform, and seed genuine customer reviews through our KOC network.' },
  { cat: 'Platforms', q: 'Which platform should I run first?', a: 'Depends on category, brand maturity and content readiness. Tmall Global is the default for beauty, supplements, F&B and lifestyle, where brand trust matters most. JD Worldwide wins on electronics, home, male-skewing categories and consumables that benefit from JD logistics. Douyin EC Global is the right call when content is your strength and you can produce livestream every day. We pick the right one in Phase one, not by default.' },
  { cat: 'Platforms', q: 'Can we run Tmall Global and JD Worldwide at the same time?', a: 'Yes, and most brands at scale do. Tmall Global usually carries the brand flagship. JD Worldwide picks up logistics-sensitive categories and male shoppers. Be ready for double the operations workload, double the campaign cadence, and a clear pricing strategy across both. We run multi-platform accounts every day.' },
  { cat: 'Platforms', q: 'What about Douyin EC Global?', a: 'Growing fast. Douyin closed the gap on Tmall Global across beauty, F&B and lifestyle in 2025 and 2026, because livestream is the format where conversion sits highest. The catch is content production: daily livestream, weekly creative refresh, constant short video. Brands without content discipline burn money on Douyin. Brands that have it, win.' },
  { cat: 'Platforms', q: 'Where does RED (Xiaohongshu) fit?', a: 'Discovery, not transaction. RED is where Chinese consumers research before buying on Tmall Global, JD Worldwide or Douyin EC Global. We use it for KOC seeding, brand reputation and demand creation. In-app commerce on RED is still small for cross-border. The standard play is RED for influence and Tmall Global or JD Worldwide for conversion, though some categories now flip that order.' },
  { cat: 'Performance', q: 'What growth should I expect in year one?', a: 'Depends on category, starting point and budget. New flagship stores on Tmall Global or JD Worldwide typically land their first full year between $400,000 and the low single-digit millions in GMV, depending on media spend and category. Relaunched stores with an existing baseline tend to grow 30% to 80% in the first twelve months under our operations. Anyone quoting a single number without seeing your category and budget is selling, not advising.' },
  { cat: 'Performance', q: 'When do we see results?', a: 'First metrics shift in week six (conversion, traffic mix, content engagement). Revenue shifts usually land in month three or four. The first big campaign window after takeover (618, 99, 11.11) is where the real number prints. Cross-border in China rewards patience, not impatience.' },
  { cat: 'Performance', q: 'How do you report?', a: 'Live dashboard you can open at any time (traffic, GMV, ROAS, conversion, repeat rate). A weekly written summary covering what moved and what is next. A monthly senior business review on video with Cyril or Liyan. A quarterly planning session. No PDFs that arrive a week late. We share the same dashboard we use internally.' },
  { cat: 'Compliance', q: 'What if my product cannot be sold through cross-border?', a: 'We tell you before we sign. Some products do not qualify (restricted categories, certain medical claims, formulation issues). If yours is one, we say so in week one, and we either help you reposition or recommend a different route to market. We do not run stores for products that will get pulled six months in.' },
  { cat: 'Compliance', q: 'What about platform takedowns or penalties?', a: 'We handle them. A penalty notice on Tmall Global or JD Worldwide has to be answered within 24 to 72 hours depending on severity. Our ops team handles the response, the rectification plan and the platform negotiation. Penalties happen, even at top brands. What matters is how fast you recover.' },
  { cat: 'Contract', q: 'What is the minimum engagement length?', a: 'Twelve months for the TP model. Twenty-four months for the distributor model. Cross-border stores need time to ramp, especially through the first major campaign window, and an inventory commitment needs a longer runway to recover. Shorter engagements are possible for project-based work (618 sprint, 11.11 sprint, launch sprint) but not for always-on operations. Anyone promising always-on results inside three months is signing you up for a poor outcome.' },
  { cat: 'Contract', q: 'Who owns the store license, customer data and content?', a: 'Depends entirely on the model. In the TP model, you own everything: store license, customer data, content library, media accounts and supplier contracts. We run operations under your brand and your assets. Nothing transfers at exit because nothing was ever ours. In the distributor model, we own everything: the store, the inventory, the customer data, the content and the media accounts. You sell to us, we sell to the consumer. Brand IP and trademarks stay yours, naturally, but the China commercial infrastructure sits on our side.' },
  { cat: 'Team', q: 'Who runs my account day to day?', a: 'A dedicated senior account director, a store operations lead, a content producer and a media buyer. They are full-time on China cross-border, not splitting their hours across five clients. Cyril or Liyan join the monthly review and step in on commercial calls.' },
  { cat: 'Team', q: 'Where is the team based?', a: 'Native team based in mainland China, working platform hours. Senior leadership splits time between China, Hong Kong and Paris. You get one inbox, one Slack channel, and a working day that overlaps with both China and Europe.' },
  { cat: 'Team', q: 'Do you work with my global agency or in-house team?', a: 'Yes. We slot into existing brand frameworks and we share what we learn back to your global team. We do not try to wedge ourselves into work that is already covered elsewhere. The brief from week one is to run the China stores, not to redo the global plan.' },
];

const faqCategories = ['All', 'Operations', 'Platforms', 'Performance', 'Compliance', 'Contract', 'Team'];

const icons: Record<string, string> = {
  compass: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><polygon points="16 8 13 13 8 16 11 11 16 8"/></svg>`,
  palette: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3a9 9 0 1 0 0 18c1 0 2-1 2-2s-1-2-2-2h-1a2 2 0 0 1 0-4h3a5 5 0 0 0 5-5 5 5 0 0 0-5-5z"/><circle cx="7.5" cy="11" r="1"/><circle cx="9.5" cy="7.5" r="1"/><circle cx="14" cy="6.5" r="1"/><circle cx="17.5" cy="9.5" r="1"/></svg>`,
  rocket: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M14 4c4 0 6 2 6 6-4 0-6 2-6 6-4 0-6-2-6-6 4 0 6-2 6-6z"/><circle cx="14" cy="10" r="1.5"/><path d="M8 16l-3 3M11 19l-2 2M5 13l-2 2"/></svg>`,
  trend: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><polyline points="3 17 9 11 13 15 21 7"/><polyline points="15 7 21 7 21 13"/></svg>`,
  wallet: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M3 7h16a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><path d="M3 7V5a2 2 0 0 1 2-2h11l3 4"/><circle cx="16.5" cy="13.5" r="1.25" fill="currentColor"/></svg>`,
  'platform-fee': `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="5" width="18" height="14" rx="2"/><path d="M3 10h18"/><path d="M7 15h4"/><path d="M14 15h3"/></svg>`,
  megaphone: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M3 11v2a2 2 0 0 0 2 2h2l8 4V5L7 9H5a2 2 0 0 0-2 2z"/><path d="M19 8a4 4 0 0 1 0 8"/></svg>`,
  spark: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3v4M12 17v4M3 12h4M17 12h4M6 6l3 3M15 15l3 3M18 6l-3 3M9 15l-3 3"/></svg>`,
  scroll: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M4 5a2 2 0 0 1 2-2h11a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V5z"/><path d="M8 7h7M8 11h7M8 15h4"/></svg>`,
};
---

<Layout
  title="Cross-Border eCommerce Agency in China | BBG"
  description="We run Tmall Global, JD Worldwide and Douyin EC Global stores for foreign brands. Daily ops, content, paid media, livestream. One senior team."
>
  <div class="cbs-progress" aria-hidden="true"><div class="cbs-progress__bar"></div></div>
  <article class="cbs">

    {/* ---------- HERO ---------- */}
    <section class="cbs-hero" data-hero>
      <div class="cbs-hero__bg" aria-hidden="true" data-parallax>
        <img src="/Images/grow-in-china/hero-livestream-shopper.png" alt="A young Chinese woman scrolling a livestream commerce broadcast on her phone at night during a Chinese shopping festival" />
      </div>
      <div class="cbs-hero__overlay" aria-hidden="true"></div>
      <div class="cbs-hero__grain" aria-hidden="true"></div>

      <div class="container cbs-hero__inner">
        <nav class="cbs-hero__crumbs" aria-label="Breadcrumb">
          <a href="/">Home</a>
          <span aria-hidden="true">/</span>
          <a href="/#services">Grow in China</a>
        </nav>
        <span class="eyebrow eyebrow--on-dark cbs-reveal">Cross-Border eCommerce</span>
        <h1 class="cbs-hero__title cbs-reveal" style="--reveal-delay: 80ms;">
          <span class="cbs-hero__title-row">Opening the store takes weeks.</span>
          <span class="cbs-hero__title-row cbs-hero__title-row--accent">Growing it takes years.</span>
          <span class="cbs-hero__title-row cbs-hero__title-row--highlight">We do the years.</span>
        </h1>
        <p class="cbs-hero__lead cbs-reveal" style="--reveal-delay: 200ms;">
          Most foreign brands have a Tmall Global or JD Worldwide store. Few have one
          that grows. We run flagship stores every day, with one senior lead on call.
        </p>
        <div class="cbs-hero__actions cbs-reveal" style="--reveal-delay: 280ms;">
          <a href="/contact" class="btn btn--primary cbs-magnetic">Talk to us</a>
          <a href="#how-we-work" class="btn btn--ghost-light cbs-magnetic">See how we work</a>
        </div>

        <div class="cbs-hero__meta cbs-reveal" style="--reveal-delay: 360ms;">
          <div class="cbs-hero__meta-item">
            <span class="cbs-hero__meta-num" data-counter="15" data-suffix="+">0</span>
            <span class="cbs-hero__meta-label">years running Chinese eCommerce stores</span>
          </div>
          <div class="cbs-hero__meta-item">
            <span class="cbs-hero__meta-num" data-counter="50" data-suffix="+">0</span>
            <span class="cbs-hero__meta-label">flagship stores operated end to end</span>
          </div>
          <div class="cbs-hero__meta-item">
            <span class="cbs-hero__meta-num">Daily</span>
            <span class="cbs-hero__meta-label">Store ops, content, media, livestream</span>
          </div>
        </div>
      </div>

      <a href="#two-truths" class="cbs-hero__scroll" aria-label="Scroll to next section">
        <span class="cbs-hero__scroll-line"></span>
        <span class="cbs-hero__scroll-text">Scroll</span>
      </a>
    </section>

    {/* ---------- TWO TRUTHS ---------- */}
    <section id="two-truths" class="section">
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">Our approach</span>
          <h2>Two things to know before we touch your store</h2>
          <p class="section-header__lead">
            A cross-border store in China is a daily product, not a quarterly project.
            Here is how that shapes the way we work.
          </p>
        </header>

        <div class="cbs-context">
          {contextCards.map((c, i) => (
            <article class={`cbs-context__card${i === 1 ? ' cbs-context__card--alt' : ''} cbs-reveal`} style={`--reveal-delay: ${i * 120}ms;`}>
              <figure class="cbs-context__media">
                <img src={c.image} alt={c.alt} loading="lazy" decoding="async" />
              </figure>
              <div class="cbs-context__body">
                <h3>{c.title}</h3>
                <p>{c.body}</p>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>

    {/* ---------- DECISION POINT (Two models) ---------- */}
    <section class="section section--alt cbs-models-section">
      <div class="cbs-models-section__bg" aria-hidden="true">
        <img src="/Images/enter-china/four-ways-tiles.png" alt="" loading="lazy" decoding="async" />
      </div>
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">Decision point</span>
          <h2>Who owns the inventory. Two ways we run your store.</h2>
          <p class="section-header__lead">
            The first commercial question on any cross-border store is who owns the stock
            in the bonded warehouse. That answer shapes the contract, the margin and how
            you book China revenue. We work in two models. Pick the one that fits how you
            want China on your books.
          </p>
        </header>

        <div class="cbs-tabs cbs-tabs--two cbs-reveal" data-tabs>
          <div class="cbs-tabs__nav" role="tablist">
            {models.map((m, i) => (
              <button
                type="button"
                class={`cbs-tabs__tab${i === 0 ? ' is-active' : ''}`}
                role="tab"
                aria-selected={i === 0 ? 'true' : 'false'}
                aria-controls={`model-panel-${i}`}
                id={`model-tab-${i}`}
                data-tab-target={`model-panel-${i}`}
              >
                <span class="cbs-tabs__tab-label">{m.name}</span>
                <span class="cbs-tabs__tab-tag">{m.tag}</span>
                {m.highlight && <span class="cbs-tabs__tab-pop">Most common</span>}
              </button>
            ))}
            <span class="cbs-tabs__indicator" aria-hidden="true"></span>
          </div>

          <div class="cbs-tabs__panels">
            {models.map((m, i) => (
              <div
                class={`cbs-tabs__panel${i === 0 ? ' is-active' : ''}`}
                role="tabpanel"
                id={`model-panel-${i}`}
                aria-labelledby={`model-tab-${i}`}
                hidden={i !== 0}
              >
                <div class="cbs-tabs__panel-grid cbs-tabs__panel-grid--with-image">
                  <div class="cbs-tabs__lead">
                    <span class="cbs-tabs__pillset">
                      <span class="cbs-tabs__pill">{m.tag}</span>
                    </span>
                    <h3 class="cbs-tabs__title">{m.name}</h3>
                    <p class="cbs-tabs__desc">{m.desc}</p>
                    <div class="cbs-tabs__when">
                      <span class="cbs-tabs__when-label">Best when</span>
                      <p>{m.when}</p>
                    </div>

                    <div class="cbs-owns">
                      <span class="cbs-owns__label">Who owns what</span>
                      <ul class="cbs-owns__list">
                        {m.owns.map((o) => (
                          <li>
                            <span class="cbs-owns__key">{o.label}</span>
                            <span class="cbs-owns__val">{o.value}</span>
                          </li>
                        ))}
                      </ul>
                    </div>

                    <a href="/contact" class="btn btn--primary cbs-magnetic">Talk about the {m.name}</a>
                  </div>

                  <div class="cbs-tabs__body">
                    <figure class="cbs-tabs__image">
                      <img src={m.image} alt={m.imageAlt} loading="lazy" decoding="async" />
                    </figure>

                    <div class="cbs-tabs__list-wrap">
                      <span class="cbs-tabs__list-label">You receive</span>
                      <ul class="cbs-tabs__list">
                        {m.yourSide.map((b) => (
                          <li>
                            <span class="cbs-tabs__check" aria-hidden="true">
                              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="5 12 10 17 19 8"/></svg>
                            </span>
                            <span>{b}</span>
                          </li>
                        ))}
                      </ul>
                    </div>

                    <div class="cbs-tabs__list-wrap cbs-tabs__list-wrap--alt">
                      <span class="cbs-tabs__list-label">We handle</span>
                      <ul class="cbs-tabs__list">
                        {m.ourSide.map((b) => (
                          <li>
                            <span class="cbs-tabs__check cbs-tabs__check--alt" aria-hidden="true">
                              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="5 12 10 17 19 8"/></svg>
                            </span>
                            <span>{b}</span>
                          </li>
                        ))}
                      </ul>
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* ---------- HOW TO PICK: Comparison Table ---------- */}
        <div class="cbs-compare cbs-reveal">
          <div class="cbs-compare__intro">
            <span class="eyebrow">How to pick</span>
            <h3>One question, two answers</h3>
            <p>Do you want to own the customer relationship in China, or do you want a clean wholesale exit? That is what decides the model.</p>
          </div>
          <div class="cbs-compare__table-wrap">
            <table class="cbs-compare__table">
              <thead>
                <tr>
                  <th scope="col" class="cbs-compare__row-head-col"></th>
                  <th scope="col" class="cbs-compare__head cbs-compare__head--tp">
                    <span class="cbs-compare__head-label">TP model</span>
                    <span class="cbs-compare__head-sub">You own the inventory</span>
                  </th>
                  <th scope="col" class="cbs-compare__head cbs-compare__head--dist">
                    <span class="cbs-compare__head-label">Distributor model</span>
                    <span class="cbs-compare__head-sub">We own the inventory</span>
                  </th>
                </tr>
              </thead>
              <tbody>
                {compareRows.map((r) => (
                  <tr>
                    <th scope="row" class="cbs-compare__row-label">{r.label}</th>
                    <td class="cbs-compare__cell cbs-compare__cell--tp">{r.tp}</td>
                    <td class="cbs-compare__cell cbs-compare__cell--dist">{r.dist}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
          <p class="cbs-compare__note">
            The model that fits depends on two things: how big you want China to be in
            your global revenue, and how much cash flow you can put behind that ambition.
            Brands with high ambition and the working capital to fund inventory and media
            usually pick TP. Brands wanting steady China revenue without tying up cash
            usually pick distributor. Some brands run both in parallel across different
            categories. None of this is a maturity ladder.
          </p>
        </div>
      </div>
    </section>

    {/* ---------- CATEGORY NOTE ---------- */}
    <section class="section">
      <div class="container">
        <div class="cbs-value">
          <div class="cbs-value__copy">
            <span class="eyebrow">Category matters</span>
            <h2>One country, ten playbooks</h2>
            <p>
              Beauty needs RED seeding plus a Tmall Global flagship. Supplements run on
              JD Worldwide. Premium F&B wins on Douyin livestream. Electronics belong on
              JD. Each category has its own platform logic.
            </p>
            <p>
              Which model fits depends on what you sell. The cross-border platforms do
              not reward one playbook.
            </p>
          </div>

          <ul class="cbs-promise">
            <li>
              <span class="eyebrow">Where each category lands</span>
            </li>
            <li class="cbs-cat-row"><span class="cbs-cat-row__cat">Beauty</span><span class="cbs-cat-row__home">RED seeding + Tmall Global flagship</span></li>
            <li class="cbs-cat-row"><span class="cbs-cat-row__cat">Supplements</span><span class="cbs-cat-row__home">JD Worldwide for trust signal</span></li>
            <li class="cbs-cat-row"><span class="cbs-cat-row__cat">Premium F&B</span><span class="cbs-cat-row__home">Douyin EC Global livestream</span></li>
            <li class="cbs-cat-row"><span class="cbs-cat-row__cat">Fashion</span><span class="cbs-cat-row__home">Tmall Global + RED influence</span></li>
            <li class="cbs-cat-row"><span class="cbs-cat-row__cat">Electronics</span><span class="cbs-cat-row__home">JD Worldwide</span></li>
            <li class="cbs-cat-row"><span class="cbs-cat-row__cat">Pet, home, lifestyle</span><span class="cbs-cat-row__home">Platform mix per category</span></li>
          </ul>
        </div>
      </div>
    </section>

    {/* ---------- COST ---------- */}
    <section class="section section--alt">
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">What it costs</span>
          <h2>What you will actually spend</h2>
          <p class="section-header__lead">
            Three lines on every cross-border P&L: what you pay us, what you pay the
            platforms, and what you put behind the brand. We show all three before we sign
            anything.
          </p>
        </header>

        <div class="cbs-cost-grid">
          {costCards.map((c, i) => (
            <article class="cbs-cost-card cbs-reveal" style={`--reveal-delay: ${i * 80}ms;`}>
              <span class="cbs-cost-card__icon" aria-hidden="true" set:html={icons[c.icon]} />
              <h3 class="cbs-cost-card__title">{c.title}</h3>
              {c.sections.map((s) => (
                <div class="cbs-cost-card__block">
                  {s.heading && <span class="cbs-cost-card__sub">{s.heading}</span>}
                  <p>{s.body}</p>
                </div>
              ))}
              {c.bullets && (
                <ul class="cbs-cost-card__bullets">
                  {c.bullets.map((b) => (
                    <li>
                      <span class="cbs-cost-card__bullet" aria-hidden="true"></span>
                      <span>{b}</span>
                    </li>
                  ))}
                </ul>
              )}
              <p class="cbs-cost-card__footer">{c.footer}</p>
            </article>
          ))}
        </div>

        <blockquote class="cbs-quote cbs-reveal">
          <p>Your margin stays your margin. We are paid by you (TP) or by the retail price (distributor). Never by Alimama, JD, or KOL agencies.</p>
        </blockquote>

        <p class="cbs-cost-note">We model the exact numbers and the split for your category in Phase one of the engagement, before any commitment.</p>
      </div>
    </section>

    {/* ---------- HOW WE WORK (vertical timeline) ---------- */}
    <section id="how-we-work" class="section cbs-journey-section">
      <div class="cbs-journey-section__bg" aria-hidden="true">
        <img src="/Images/enter-china/journey-path.png" alt="" loading="lazy" decoding="async" />
      </div>
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">How we work</span>
          <h2>Four phases from store to scale</h2>
          <p class="section-header__lead">
            A structured engagement that moves you from "we have a store" to a store that
            grows month on month. Phases one to three are sequential. Phase four runs on a
            loop, and we revisit phase one inside it every twelve months.
          </p>
        </header>

        <div class="cbs-journey" data-journey>
          <div class="cbs-journey__rail" aria-hidden="true">
            <div class="cbs-journey__rail-track"></div>
            <div class="cbs-journey__rail-fill" data-journey-fill></div>
          </div>
          <ol class="cbs-journey__list">
            {steps.map((s, i) => (
              <li class="cbs-journey__item cbs-reveal" data-journey-item style={`--reveal-delay: ${i * 80}ms;`}>
                <span class="cbs-journey__dot" aria-hidden="true">
                  <span class="cbs-journey__dot-inner"></span>
                </span>
                <span class="cbs-journey__phase">{s.phase} <span class="cbs-journey__duration">({s.duration})</span></span>
                <div class="cbs-journey__card">
                  <span class="cbs-journey__icon" aria-hidden="true" set:html={icons[s.icon]} />
                  <div>
                    <h3 class="cbs-journey__title">{s.title}</h3>
                    <p class="cbs-journey__desc">{s.desc}</p>
                  </div>
                </div>
              </li>
            ))}
          </ol>
        </div>
      </div>
    </section>

    {/* ---------- SISTER PRODUCT: TheRedScroll ---------- */}
    <section class="section section--alt">
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">If you want to add social commerce</span>
          <h2>Powered by TheRedScroll</h2>
          <p class="section-header__lead">
            Our cross-border stores convert better when content is live across the China
            social stack. TheRedScroll is our sister agency for daily WeChat, RED, Douyin
            and Weibo operations. Built to feed your store with attention, not just
            impressions.
          </p>
        </header>

        <div class="cbs-sister cbs-reveal">
          <div class="cbs-sister__hero">
            <span class="cbs-sister__chip">Sister product</span>
            <h3 class="cbs-sister__title">Run your store and your social on the same rhythm.</h3>
            <p class="cbs-sister__desc">
              Cross-border stops working when the content goes quiet. TheRedScroll keeps
              the off-platform channels active every day, while we run the stores. Same
              group, same standards, separate brand.
            </p>
            <ul class="cbs-sister__stats">
              <li>
                <span class="cbs-sister__stat-num">25+</span>
                <span class="cbs-sister__stat-label">years of Chinese social experience inside the group</span>
              </li>
              <li>
                <span class="cbs-sister__stat-num">4</span>
                <span class="cbs-sister__stat-label">channels covered: WeChat, RED, Douyin, Weibo</span>
              </li>
              <li>
                <span class="cbs-sister__stat-num">In-house</span>
                <span class="cbs-sister__stat-label">native team, content shot in studio</span>
              </li>
            </ul>
            <div class="cbs-sister__actions">
              <a href="https://theredscroll.com" target="_blank" rel="noopener" class="btn cbs-sister__btn-primary">Visit TheRedScroll</a>
              <a href="/contact" class="btn cbs-sister__btn-ghost">Brief us on social commerce</a>
            </div>
          </div>

          <div class="cbs-sister__packs">
            {sisterPackages.map((p) => (
              <article class="cbs-sister__pack">
                <span class="cbs-sister__pack-icon" aria-hidden="true" set:html={icons.scroll} />
                <h4>{p.title}</h4>
                <p>{p.desc}</p>
              </article>
            ))}
          </div>
        </div>
      </div>
    </section>

    {/* ---------- WHY BRANDS CHOOSE US ---------- */}
    <section class="section">
      <div class="container">
        <div class="cbs-value">
          <div class="cbs-value__copy">
            <span class="eyebrow">Why brands choose us</span>
            <h2>Most agencies hand you an account manager. We hand you an operator.</h2>
            <p>
              Our team ran eCommerce at Publicis Commerce China for global brands across
              Tmall, JD, Douyin and RED. We run what we recommend.
            </p>
            <blockquote class="cbs-quote cbs-quote--inline">
              <p>The senior who writes the plan is on the call when the store hits a problem.</p>
            </blockquote>
            <p>
              We test what platforms reward every week. We book the KOLs we recommend.
              We shoot the content that sells, in our own studio.
            </p>
            <a href="/about#team" class="btn btn--secondary cbs-magnetic">Meet the team</a>
          </div>

          <ul class="cbs-promise">
            <li>
              <span class="eyebrow">What you can expect</span>
            </li>
            {promises.map((p) => (
              <li class="cbs-promise__item">
                <span class="cbs-promise__check" aria-hidden="true">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="5 12 10 17 19 8"/></svg>
                </span>
                <div>
                  <h4>{p.title}</h4>
                  <p>{p.desc}</p>
                </div>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </section>

    {/* ---------- FAQ ---------- */}
    <section class="section section--alt">
      <div class="container container--narrow">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">FAQ</span>
          <h2>Twenty questions, answered straight</h2>
          <p class="section-header__lead">
            The practical questions we field from brand managers, eCommerce directors and
            founders running cross-border in China. Filter by topic.
          </p>
        </header>

        <div class="cbs-faq-filter cbs-reveal" data-faq-filter role="tablist" aria-label="Filter FAQs by category">
          {faqCategories.map((c, i) => (
            <button
              type="button"
              class={`cbs-faq-chip${i === 0 ? ' is-active' : ''}`}
              role="tab"
              aria-selected={i === 0 ? 'true' : 'false'}
              data-faq-cat={c}
            >
              <span class="cbs-faq-chip__label">{c}</span>
              <span class="cbs-faq-chip__count">{c === 'All' ? faqs.length : faqs.filter((f) => f.cat === c).length}</span>
            </button>
          ))}
        </div>

        <div class="cbs-faq" data-faq-list>
          {faqs.map((f, i) => (
            <details
              class="cbs-faq__item cbs-reveal"
              data-faq-cat={f.cat}
              open={i === 0}
              style={`--reveal-delay: ${Math.min(i, 6) * 40}ms;`}
            >
              <summary class="cbs-faq__q">
                <span class="cbs-faq__cat-tag">{f.cat}</span>
                <span class="cbs-faq__q-text">{f.q}</span>
                <span class="cbs-faq__icon" aria-hidden="true">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
                </span>
              </summary>
              <div class="cbs-faq__a">
                <p>{f.a}</p>
              </div>
            </details>
          ))}
        </div>
      </div>
    </section>

    {/* ---------- FINAL CTA ---------- */}
    <section class="section section--tight">
      <div class="container container--narrow">
        <div class="cbs-cta">
          <span class="eyebrow eyebrow--on-dark">Let's talk</span>
          <h2 class="cbs-cta__title">Running a store today, or about to open one?</h2>
          <p class="cbs-cta__lead">
            Tell us where the store is today and what it needs to be doing six months from
            now. A senior member of the team replies within one working day. No funnel.
            No auto-responder.
          </p>
          <div class="cbs-cta__actions">
            <a href="/contact" class="btn cbs-cta__btn-primary">Start the conversation</a>
            <a href="/work" class="btn cbs-cta__btn-ghost">See our case studies</a>
          </div>
        </div>
      </div>
    </section>
  </article>

  <style is:global>
    /* ---------- Container narrow helper ---------- */
    .cbs .container--narrow { max-width: 880px; }

    /* ---------- REVEAL + MOTION ---------- */
    @media (prefers-reduced-motion: no-preference) {
      .cbs-reveal {
        opacity: 0;
        transform: translateY(16px);
        transition:
          opacity 700ms cubic-bezier(0.16, 1, 0.3, 1) var(--reveal-delay, 0ms),
          transform 700ms cubic-bezier(0.16, 1, 0.3, 1) var(--reveal-delay, 0ms);
      }
      .cbs-reveal.is-in { opacity: 1; transform: translateY(0); }
      .cbs-magnetic {
        will-change: transform;
        transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1),
                    box-shadow 220ms ease, background-color 200ms ease, color 200ms ease;
      }
    }

    /* ---------- SCROLL PROGRESS BAR ---------- */
    .cbs-progress {
      position: fixed; top: 0; left: 0; right: 0;
      height: 3px; z-index: 1000;
      background: transparent; pointer-events: none;
    }
    .cbs-progress__bar {
      height: 100%; width: var(--progress, 0%);
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 60%, #9DDBF5 100%);
      box-shadow: 0 0 12px rgba(38, 154, 218, 0.55);
      transition: width 80ms linear;
    }

    /* ---------- HERO ---------- */
    .cbs-hero {
      position: relative;
      min-height: clamp(360px, 48vh, 500px);
      display: flex; align-items: flex-start;
      padding: clamp(8px, 1.25vh, 16px) 0 clamp(48px, 7vh, 80px);
      color: var(--bbg-white);
      overflow: hidden; isolation: isolate;
    }
    body:has(.cbs-hero) .cbs-hero { padding-top: clamp(112px, 10vh, 144px); }
    @media (max-width: 1024px) {
      body:has(.cbs-hero) .cbs-hero { padding-top: clamp(96px, 9vh, 116px); }
    }
    .cbs-hero__bg { position: absolute; inset: -10% 0 0; z-index: -3; }
    .cbs-hero__bg img {
      width: 100%; height: 110%;
      object-fit: cover; object-position: center;
      transform: translate3d(0, var(--parallax-y, 0px), 0) scale(1.04);
      transition: transform 80ms linear;
      filter: saturate(0.95);
    }
    .cbs-hero__overlay {
      position: absolute; inset: 0; z-index: -2;
      background:
        linear-gradient(110deg,
          rgba(8, 18, 28, 0.92) 0%,
          rgba(8, 18, 28, 0.72) 42%,
          rgba(8, 18, 28, 0.32) 100%),
        radial-gradient(120% 80% at 0% 100%,
          rgba(38, 154, 218, 0.28) 0%,
          rgba(38, 154, 218, 0) 60%);
    }
    .cbs-hero__grain {
      position: absolute; inset: 0; z-index: -1;
      background-image: radial-gradient(rgba(255, 255, 255, 0.06) 1px, transparent 1px);
      background-size: 3px 3px;
      mix-blend-mode: overlay; opacity: 0.45;
      pointer-events: none;
    }
    .cbs-hero__inner { max-width: 980px; position: relative; }
    .cbs-hero__crumbs {
      margin-bottom: var(--space-4);
      font-size: var(--fs-small);
      display: flex; gap: 0.5rem; align-items: center;
      color: rgba(255, 255, 255, 0.7);
    }
    .cbs-hero__crumbs a { color: rgba(255, 255, 255, 0.85); font-weight: 500; }
    .cbs-hero__crumbs a:hover { color: var(--bbg-white); }
    .cbs-hero__inner .eyebrow--on-dark {
      display: inline-block;
      margin-bottom: var(--space-3);
      color: #9DDBF5;
    }
    .cbs-hero__title {
      font-size: clamp(2rem, 3.4vw + 1rem, 3.75rem);
      line-height: 1.06;
      letter-spacing: -0.025em;
      color: var(--bbg-white);
      margin: 0 0 var(--space-4);
      max-width: 22ch;
      font-weight: 700;
      display: flex; flex-direction: column;
      gap: 0.05em;
    }
    .cbs-hero__title-row { display: block; }
    .cbs-hero__title-row--accent {
      color: #9DDBF5; font-weight: 500;
      font-style: italic; letter-spacing: -0.018em;
    }
    .cbs-hero__title-row--highlight {
      position: relative; display: inline-block;
      align-self: flex-start; padding: 0 0.15em;
    }
    .cbs-hero__title-row--highlight::before {
      content: ""; position: absolute;
      left: 0; right: 0; bottom: 0.04em;
      height: 0.18em;
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      border-radius: 2px;
      transform-origin: left center;
      animation: cbs-underline 900ms cubic-bezier(0.16, 1, 0.3, 1) 600ms both;
      z-index: -1;
    }
    @keyframes cbs-underline { from { transform: scaleX(0); } to { transform: scaleX(1); } }
    .cbs-hero__lead {
      font-size: clamp(1rem, 0.5vw + 1rem, 1.1875rem);
      line-height: 1.55;
      color: rgba(255, 255, 255, 0.86);
      max-width: 58ch;
      margin: 0 0 var(--space-5);
    }
    .cbs-hero__actions { display: flex; flex-wrap: wrap; gap: var(--space-3); margin-bottom: var(--space-6); }
    .cbs-magnetic:hover { transform: translateY(-2px); }

    .cbs-hero__meta {
      display: flex; flex-wrap: nowrap;
      gap: clamp(var(--space-3), 3vw, var(--space-6));
      padding-top: var(--space-5);
      border-top: 1px solid rgba(255, 255, 255, 0.14);
      max-width: none;
    }
    .cbs-hero__meta-item {
      display: flex; flex-direction: column;
      gap: 0.2rem;
      min-width: 0;
      flex: 1 1 0;
    }
    @media (max-width: 720px) {
      .cbs-hero__meta { flex-wrap: wrap; }
      .cbs-hero__meta-item { flex: 1 1 auto; }
    }
    .cbs-hero__meta-num {
      font-size: clamp(1.625rem, 1.2vw + 1rem, 2.125rem);
      font-weight: 700; letter-spacing: -0.02em;
      line-height: 1; color: var(--bbg-white);
      font-feature-settings: "tnum";
    }
    .cbs-hero__meta-label {
      font-size: 0.85rem;
      color: rgba(255, 255, 255, 0.66);
      letter-spacing: 0.02em;
    }

    .cbs-hero__scroll {
      position: absolute;
      bottom: var(--space-5);
      right: clamp(var(--space-5), 4vw, var(--space-7));
      display: flex; flex-direction: column;
      align-items: center; gap: 0.5rem;
      color: rgba(255, 255, 255, 0.7);
      font-size: 0.7rem; letter-spacing: 0.18em;
      text-transform: uppercase; font-weight: 600;
      transition: color 200ms ease;
    }
    .cbs-hero__scroll:hover { color: var(--bbg-white); }
    .cbs-hero__scroll-line {
      width: 1px; height: 40px;
      background: linear-gradient(180deg, transparent 0%, rgba(255, 255, 255, 0.6) 100%);
      position: relative; overflow: hidden;
    }
    .cbs-hero__scroll-line::after {
      content: ""; position: absolute;
      top: -100%; left: 0; right: 0;
      height: 50%;
      background: linear-gradient(180deg, transparent 0%, var(--bbg-white) 100%);
      animation: cbs-scroll-tick 1.8s ease-in-out infinite;
    }
    @keyframes cbs-scroll-tick { 0% { top: -50%; } 100% { top: 100%; } }
    @media (max-width: 720px) {
      .cbs-hero__scroll { display: none; }
      .cbs-hero__meta { gap: var(--space-4); }
    }

    /* ---------- CONTEXT (two truths) ---------- */
    .cbs-context {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: var(--space-5);
    }
    .cbs-context__card {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      overflow: hidden;
      display: flex; flex-direction: column;
      transition: border-color var(--dur) var(--ease), box-shadow var(--dur) var(--ease);
    }
    .cbs-context__card:hover {
      border-color: var(--bbg-blue);
      box-shadow: var(--shadow-md);
    }
    .cbs-context__card--alt {
      background: var(--bbg-blue-light);
      border-color: transparent;
    }
    .cbs-context__media {
      margin: 0; aspect-ratio: 16 / 9;
      overflow: hidden; background: var(--bbg-border);
    }
    .cbs-context__media img {
      width: 100%; height: 100%;
      object-fit: cover; display: block;
      transition: transform 600ms var(--ease);
    }
    .cbs-context__card:hover .cbs-context__media img { transform: scale(1.03); }
    .cbs-context__body { padding: var(--space-6) var(--space-7) var(--space-7); }
    .cbs-context__card h3 {
      font-size: clamp(1.25rem, 1vw + 0.75rem, 1.625rem);
      line-height: 1.25; margin: 0 0 var(--space-4);
      letter-spacing: -0.01em;
    }
    .cbs-context__card p {
      color: var(--bbg-text); line-height: 1.7;
      font-size: 1.0625rem; margin: 0;
    }
    @media (max-width: 900px) {
      .cbs-context { grid-template-columns: 1fr; }
      .cbs-context__body { padding: var(--space-5) var(--space-6) var(--space-6); }
    }

    /* ---------- VALUE + PROMISES ---------- */
    .cbs-value {
      display: grid;
      grid-template-columns: 1.1fr 1fr;
      gap: var(--space-8);
      align-items: start;
    }
    .cbs-value__copy h2 {
      font-size: clamp(1.75rem, 2vw + 1rem, 2.5rem);
      line-height: 1.15; letter-spacing: -0.015em;
      margin: var(--space-3) 0 var(--space-4);
    }
    .cbs-value__copy p {
      color: var(--bbg-text); line-height: 1.7;
      font-size: 1.0625rem; margin: 0 0 var(--space-4);
    }
    .cbs-value__copy .btn { margin-top: var(--space-4); }
    .cbs-promise {
      list-style: none; padding: var(--space-6); margin: 0;
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      display: flex; flex-direction: column;
      gap: var(--space-5);
    }
    .cbs-promise > li:first-child .eyebrow { margin-bottom: 0; }
    .cbs-promise__item {
      display: grid;
      grid-template-columns: 32px 1fr;
      gap: var(--space-4);
      align-items: start;
    }
    .cbs-promise__check {
      width: 32px; height: 32px;
      border-radius: 50%;
      background: var(--bbg-blue-light);
      color: var(--bbg-blue);
      display: inline-flex; align-items: center; justify-content: center;
      flex-shrink: 0;
    }
    .cbs-promise__check svg { width: 16px; height: 16px; }
    .cbs-promise__item h4 {
      font-size: 1.125rem; margin: 0 0 var(--space-2);
      letter-spacing: -0.005em; color: var(--bbg-ink);
    }
    .cbs-promise__item p {
      font-size: 0.975rem; line-height: 1.55;
      color: var(--bbg-text); margin: 0;
    }

    /* Category rows inside promise */
    .cbs-cat-row {
      display: grid;
      grid-template-columns: minmax(100px, auto) 1fr;
      gap: var(--space-4);
      padding: var(--space-3) 0;
      border-bottom: 1px solid var(--bbg-border);
      align-items: baseline;
    }
    .cbs-cat-row:last-child { border-bottom: none; }
    .cbs-cat-row__cat {
      font-weight: 700; color: var(--bbg-ink);
      font-size: 0.95rem;
    }
    .cbs-cat-row__home {
      font-size: 0.95rem;
      color: var(--bbg-text);
      line-height: 1.5;
    }

    @media (max-width: 900px) {
      .cbs-value { grid-template-columns: 1fr; gap: var(--space-6); }
    }

    /* ---------- BUSINESS MODELS BG ---------- */
    .cbs-models-section { position: relative; isolation: isolate; overflow: hidden; }
    .cbs-models-section__bg {
      position: absolute;
      top: 0; right: -8%;
      width: 55%; height: 100%;
      z-index: -1;
      opacity: 0.18;
      mask-image: radial-gradient(ellipse at right center, black 0%, transparent 65%);
      -webkit-mask-image: radial-gradient(ellipse at right center, black 0%, transparent 65%);
      pointer-events: none;
    }
    .cbs-models-section__bg img {
      width: 100%; height: 100%;
      object-fit: cover; object-position: center;
      filter: saturate(0.6);
    }
    @media (max-width: 900px) { .cbs-models-section__bg { display: none; } }

    /* ---------- BUSINESS MODELS TABS ---------- */
    .cbs-tabs {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 16px 48px -28px rgba(8, 18, 28, 0.18);
    }
    .cbs-tabs__nav {
      position: relative;
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 0;
      background: linear-gradient(180deg, var(--bbg-surface) 0%, var(--bbg-white) 100%);
      border-bottom: 1px solid var(--bbg-border);
    }
    .cbs-tabs--two .cbs-tabs__nav { grid-template-columns: repeat(2, 1fr); }
    .cbs-tabs__tab {
      position: relative; background: transparent; border: none;
      padding: var(--space-5) var(--space-4);
      text-align: left; cursor: pointer;
      display: flex; flex-direction: column;
      gap: 0.35rem;
      color: var(--bbg-text-muted);
      transition: color 240ms ease, background 240ms ease;
      border-right: 1px solid var(--bbg-border);
    }
    .cbs-tabs__tab:last-child { border-right: none; }
    .cbs-tabs__tab:hover { color: var(--bbg-ink); background: rgba(38, 154, 218, 0.04); }
    .cbs-tabs__tab.is-active { color: var(--bbg-ink); background: var(--bbg-white); }
    .cbs-tabs__tab-label {
      font-size: 1.0625rem; font-weight: 700;
      letter-spacing: -0.005em; line-height: 1.2;
    }
    .cbs-tabs__tab-tag {
      font-size: 0.78rem; color: var(--bbg-text-muted);
      line-height: 1.35;
    }
    .cbs-tabs__tab.is-active .cbs-tabs__tab-tag { color: var(--bbg-blue); }
    .cbs-tabs__tab-pop {
      position: absolute;
      top: var(--space-3); right: var(--space-3);
      font-size: 0.65rem; font-weight: 700;
      letter-spacing: 0.08em; text-transform: uppercase;
      color: var(--bbg-blue);
      background: var(--bbg-blue-light);
      padding: 2px 8px;
      border-radius: 999px;
    }
    .cbs-tabs__indicator {
      position: absolute;
      bottom: -1px; left: 0;
      width: 25%; height: 3px;
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      border-radius: 2px 2px 0 0;
      transform: translateX(0);
      transition: transform 360ms cubic-bezier(0.16, 1, 0.3, 1);
      pointer-events: none;
    }
    .cbs-tabs--two .cbs-tabs__indicator { width: 50%; }
    .cbs-tabs__panels { background: var(--bbg-white); }
    .cbs-tabs__panel {
      padding: clamp(var(--space-5), 4vw + 1rem, var(--space-7));
      animation: cbs-tab-in 360ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    @keyframes cbs-tab-in {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .cbs-tabs__panel[hidden] { display: none; }
    .cbs-tabs__panel-grid {
      display: grid;
      grid-template-columns: 1.1fr 1fr;
      gap: clamp(var(--space-5), 3vw, var(--space-7));
      align-items: start;
    }
    .cbs-tabs__lead { display: flex; flex-direction: column; gap: var(--space-4); }
    .cbs-tabs__pillset { display: flex; flex-wrap: wrap; gap: 0.5rem; }
    .cbs-tabs__pill {
      font-size: 0.78rem; font-weight: 600;
      padding: 0.35em 0.85em;
      border-radius: 999px;
      background: var(--bbg-blue-light);
      color: var(--bbg-blue);
      letter-spacing: 0.01em;
    }
    .cbs-tabs__title {
      font-size: clamp(1.625rem, 1.8vw + 1rem, 2.25rem);
      line-height: 1.1; margin: 0;
      letter-spacing: -0.015em;
    }
    .cbs-tabs__desc {
      font-size: 1rem; line-height: 1.6;
      color: var(--bbg-text); margin: 0;
    }
    .cbs-tabs__when {
      padding: var(--space-4);
      background: var(--bbg-surface);
      border-radius: var(--radius-md);
      border-left: 3px solid var(--bbg-blue);
    }
    .cbs-tabs__when-label {
      display: block;
      font-size: 0.72rem; font-weight: 700;
      letter-spacing: 0.1em; text-transform: uppercase;
      color: var(--bbg-blue);
      margin-bottom: 0.35rem;
    }
    .cbs-tabs__when p { margin: 0; font-size: 0.95rem; line-height: 1.55; color: var(--bbg-text); }
    .cbs-tabs__body { display: flex; flex-direction: column; gap: var(--space-4); }
    .cbs-tabs__image {
      margin: 0;
      aspect-ratio: 16 / 10;
      overflow: hidden;
      border-radius: var(--radius-md);
      background: var(--bbg-border);
    }
    .cbs-tabs__image img {
      width: 100%; height: 100%;
      object-fit: cover; display: block;
    }
    .cbs-tabs__list-wrap {
      background: var(--bbg-surface);
      border-radius: var(--radius-md);
      padding: var(--space-5);
    }
    .cbs-tabs__list-wrap--alt {
      background: var(--bbg-blue-light);
    }
    .cbs-tabs__list-label {
      display: block;
      font-size: 0.72rem; font-weight: 700;
      letter-spacing: 0.1em; text-transform: uppercase;
      color: var(--bbg-text-muted);
      margin-bottom: var(--space-3);
    }
    .cbs-tabs__list-wrap--alt .cbs-tabs__list-label { color: var(--bbg-blue-dark); }
    .cbs-tabs__list {
      list-style: none; padding: 0; margin: 0;
      display: flex; flex-direction: column;
      gap: var(--space-3);
    }
    .cbs-tabs__list li {
      display: grid;
      grid-template-columns: 24px 1fr;
      gap: 0.75rem;
      align-items: start;
      font-size: 0.95rem; line-height: 1.5;
      color: var(--bbg-text);
    }
    .cbs-tabs__check {
      width: 22px; height: 22px;
      border-radius: 50%;
      background: var(--bbg-blue);
      color: var(--bbg-white);
      display: inline-flex; align-items: center; justify-content: center;
      flex-shrink: 0; margin-top: 1px;
    }
    .cbs-tabs__check--alt {
      background: var(--bbg-white);
      color: var(--bbg-blue);
      box-shadow: inset 0 0 0 1.5px var(--bbg-blue);
    }
    .cbs-tabs__check svg { width: 12px; height: 12px; }

    .cbs-owns {
      padding: var(--space-4);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-md);
      background: var(--bbg-white);
    }
    .cbs-owns__label {
      display: block;
      font-size: 0.72rem; font-weight: 700;
      letter-spacing: 0.1em; text-transform: uppercase;
      color: var(--bbg-text-muted);
      margin-bottom: var(--space-3);
    }
    .cbs-owns__list {
      list-style: none; padding: 0; margin: 0;
      display: flex; flex-direction: column;
      gap: 0.6rem;
    }
    .cbs-owns__list li {
      display: grid;
      grid-template-columns: minmax(150px, auto) 1fr;
      gap: var(--space-4);
      align-items: baseline;
      padding-bottom: 0.6rem;
      border-bottom: 1px dashed var(--bbg-border);
      font-size: 0.9rem;
      line-height: 1.45;
    }
    .cbs-owns__list li:last-child { border-bottom: none; padding-bottom: 0; }
    .cbs-owns__key { color: var(--bbg-text-muted); font-weight: 600; }
    .cbs-owns__val { color: var(--bbg-ink); }

    @media (max-width: 900px) {
      .cbs-tabs--two .cbs-tabs__nav { grid-template-columns: 1fr; }
      .cbs-tabs__tab { border-right: none; border-bottom: 1px solid var(--bbg-border); }
      .cbs-tabs--two .cbs-tabs__indicator { display: none; }
      .cbs-tabs__panel-grid { grid-template-columns: 1fr; }
    }

    /* ---------- COMPARISON TABLE ---------- */
    .cbs-compare {
      margin-top: var(--space-8);
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      padding: clamp(var(--space-5), 3vw + 1rem, var(--space-7));
      box-shadow: 0 1px 0 rgba(0, 0, 0, 0.02);
    }
    .cbs-compare__intro { text-align: center; margin-bottom: var(--space-6); }
    .cbs-compare__intro h3 {
      font-size: clamp(1.375rem, 1.2vw + 1rem, 1.75rem);
      margin: var(--space-2) 0 var(--space-3);
      letter-spacing: -0.01em;
    }
    .cbs-compare__intro p {
      color: var(--bbg-text);
      max-width: 60ch; margin: 0 auto;
      line-height: 1.6; font-size: 1rem;
    }
    .cbs-compare__table-wrap { overflow-x: auto; }
    .cbs-compare__table {
      width: 100%; border-collapse: separate;
      border-spacing: 0;
      min-width: 640px;
    }
    .cbs-compare__head {
      background: var(--bbg-surface);
      padding: var(--space-4);
      text-align: left;
      vertical-align: top;
      border-bottom: 1px solid var(--bbg-border);
    }
    .cbs-compare__head--tp { border-bottom-color: var(--bbg-blue); }
    .cbs-compare__head--dist { border-bottom-color: var(--bbg-blue-dark); }
    .cbs-compare__head-label {
      display: block;
      font-size: 1rem; font-weight: 700;
      color: var(--bbg-ink);
      letter-spacing: -0.005em;
    }
    .cbs-compare__head-sub {
      display: block;
      font-size: 0.78rem;
      color: var(--bbg-text-muted);
      margin-top: 0.25rem;
    }
    .cbs-compare__row-head-col {
      width: 38%;
      background: var(--bbg-surface);
      border-bottom: 1px solid var(--bbg-border);
    }
    .cbs-compare__row-label {
      text-align: left;
      padding: var(--space-4);
      font-size: 0.92rem;
      font-weight: 600;
      color: var(--bbg-text-muted);
      letter-spacing: 0.01em;
      border-bottom: 1px solid var(--bbg-border);
      background: var(--bbg-white);
    }
    .cbs-compare__cell {
      padding: var(--space-4);
      font-size: 0.95rem;
      color: var(--bbg-ink);
      border-bottom: 1px solid var(--bbg-border);
      vertical-align: top;
    }
    .cbs-compare__cell--tp {
      background: rgba(38, 154, 218, 0.04);
    }
    .cbs-compare__cell--dist {
      background: var(--bbg-white);
    }
    .cbs-compare__table tbody tr:last-child .cbs-compare__row-label,
    .cbs-compare__table tbody tr:last-child .cbs-compare__cell {
      border-bottom: none;
    }
    .cbs-compare__note {
      margin: var(--space-5) auto 0;
      max-width: 70ch;
      text-align: center;
      color: var(--bbg-text);
      line-height: 1.65;
      font-size: 0.975rem;
    }

    /* ---------- COST GRID ---------- */
    .cbs-cost-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: var(--space-5);
    }
    .cbs-cost-card {
      position: relative;
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: 20px;
      padding: var(--space-6);
      display: flex; flex-direction: column;
      gap: var(--space-3);
      overflow: hidden; isolation: isolate;
      transition:
        transform 360ms cubic-bezier(0.16, 1, 0.3, 1),
        border-color 280ms ease,
        box-shadow 280ms ease;
    }
    .cbs-cost-card::before {
      content: ""; position: absolute;
      top: 0; left: 0; width: 100%; height: 3px;
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      transform: scaleX(0.35);
      transform-origin: left center;
      transition: transform 420ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .cbs-cost-card:hover {
      transform: translateY(-4px);
      border-color: var(--bbg-blue);
      box-shadow: 0 16px 32px -20px rgba(38, 154, 218, 0.4);
    }
    .cbs-cost-card:hover::before { transform: scaleX(1); }
    .cbs-cost-card__icon {
      display: inline-flex;
      width: 48px; height: 48px;
      align-items: center; justify-content: center;
      border-radius: 14px;
      background: linear-gradient(135deg, var(--bbg-blue-light) 0%, rgba(38, 154, 218, 0.18) 100%);
      color: var(--bbg-blue);
      margin-bottom: var(--space-3);
    }
    .cbs-cost-card__icon svg { width: 22px; height: 22px; }
    .cbs-cost-card__title {
      font-size: 1.25rem; line-height: 1.2;
      margin: 0; letter-spacing: -0.005em;
    }
    .cbs-cost-card__block { display: flex; flex-direction: column; gap: 0.4rem; }
    .cbs-cost-card__sub {
      font-size: 0.7rem; font-weight: 700;
      letter-spacing: 0.1em; text-transform: uppercase;
      color: var(--bbg-blue);
    }
    .cbs-cost-card__block p {
      font-size: 0.95rem; line-height: 1.55;
      color: var(--bbg-text); margin: 0;
    }
    .cbs-cost-card__bullets {
      list-style: none; padding: var(--space-3) 0;
      margin: 0;
      display: flex; flex-direction: column;
      gap: 0.55rem;
      border-top: 1px dashed var(--bbg-border);
      border-bottom: 1px dashed var(--bbg-border);
    }
    .cbs-cost-card__bullets li {
      display: grid;
      grid-template-columns: 14px 1fr;
      gap: 0.6rem;
      align-items: start;
      font-size: 0.9rem;
      line-height: 1.5;
      color: var(--bbg-text);
    }
    .cbs-cost-card__bullet {
      width: 6px; height: 6px;
      border-radius: 50%;
      background: var(--bbg-blue);
      box-shadow: 0 0 0 3px var(--bbg-blue-light);
      margin: 6px 0 0 4px;
    }
    .cbs-cost-card__footer {
      font-size: 0.85rem; line-height: 1.55;
      color: var(--bbg-text-muted);
      margin: 0;
      padding-top: var(--space-2);
    }
    @media (max-width: 960px) { .cbs-cost-grid { grid-template-columns: 1fr; } }

    /* ---------- QUOTE BLOCK ---------- */
    .cbs-quote {
      margin: var(--space-6) auto 0;
      max-width: 760px;
      padding: var(--space-5) var(--space-6);
      background: linear-gradient(135deg, var(--bbg-blue-light) 0%, rgba(38, 154, 218, 0.10) 100%);
      border-left: 4px solid var(--bbg-blue);
      border-radius: 0 var(--radius-md) var(--radius-md) 0;
    }
    .cbs-quote p {
      margin: 0;
      font-size: 1.0625rem;
      line-height: 1.55;
      color: var(--bbg-ink);
      font-weight: 500;
      font-style: italic;
    }
    .cbs-quote--inline {
      margin: var(--space-2) 0;
      max-width: none;
      padding: var(--space-4) var(--space-5);
    }
    .cbs-cost-note {
      margin: var(--space-5) auto 0;
      max-width: 60ch;
      text-align: center;
      font-size: 0.95rem;
      color: var(--bbg-text-muted);
      line-height: 1.6;
    }

    /* ---------- JOURNEY (vertical timeline) ---------- */
    .cbs-journey-section { position: relative; isolation: isolate; overflow: hidden; }
    .cbs-journey-section__bg {
      position: absolute;
      top: 0; left: 0;
      width: 100%; height: 100%;
      z-index: -2;
      opacity: 0.10;
      pointer-events: none;
    }
    .cbs-journey-section__bg::after {
      content: ""; position: absolute; inset: 0;
      background: linear-gradient(180deg, var(--bbg-white) 0%, rgba(255, 255, 255, 0.4) 40%, rgba(255, 255, 255, 0.7) 100%);
    }
    .cbs-journey-section__bg img {
      width: 100%; height: 100%;
      object-fit: cover; object-position: center;
      filter: saturate(0.55);
    }
    .cbs-journey {
      position: relative;
      max-width: 880px;
      margin: 0 auto;
      padding-left: 56px;
    }
    .cbs-journey__rail {
      position: absolute;
      top: 0; bottom: 0;
      left: 22px; width: 2px;
    }
    .cbs-journey__rail-track {
      position: absolute; inset: 0;
      background: var(--bbg-border);
      border-radius: 2px;
    }
    .cbs-journey__rail-fill {
      position: absolute;
      top: 0; left: 0; width: 100%;
      height: var(--journey-progress, 0%);
      background: linear-gradient(180deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      border-radius: 2px;
      transition: height 120ms linear;
      box-shadow: 0 0 8px rgba(38, 154, 218, 0.4);
    }
    .cbs-journey__list {
      list-style: none; padding: 0; margin: 0;
      display: flex; flex-direction: column;
      gap: var(--space-7);
    }
    .cbs-journey__item { position: relative; }
    .cbs-journey__dot {
      position: absolute;
      left: -56px; top: 0;
      width: 44px; height: 44px;
      border-radius: 50%;
      background: var(--bbg-white);
      border: 2px solid var(--bbg-border);
      display: flex; align-items: center; justify-content: center;
      z-index: 2;
      transition: border-color 320ms ease;
    }
    .cbs-journey__dot-inner {
      width: 14px; height: 14px;
      border-radius: 50%;
      background: var(--bbg-border);
      transition: background 320ms ease, transform 320ms ease;
    }
    .cbs-journey__item.is-active .cbs-journey__dot { border-color: var(--bbg-blue); }
    .cbs-journey__item.is-active .cbs-journey__dot-inner {
      background: var(--bbg-blue);
      transform: scale(1.1);
      box-shadow: 0 0 0 4px var(--bbg-blue-light);
    }
    .cbs-journey__phase {
      display: inline-block;
      font-size: 0.72rem; font-weight: 700;
      letter-spacing: 0.1em; text-transform: uppercase;
      color: var(--bbg-text-muted);
      margin-bottom: var(--space-2);
      transition: color 320ms ease;
    }
    .cbs-journey__duration {
      font-weight: 500;
      color: var(--bbg-text-muted);
      letter-spacing: 0.04em;
      text-transform: none;
    }
    .cbs-journey__item.is-active .cbs-journey__phase { color: var(--bbg-blue); }
    .cbs-journey__card {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      padding: var(--space-5) var(--space-6);
      display: flex; align-items: flex-start;
      gap: var(--space-4);
      transition: border-color 320ms ease, box-shadow 320ms ease, transform 320ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .cbs-journey__item:hover .cbs-journey__card,
    .cbs-journey__item.is-active .cbs-journey__card {
      border-color: var(--bbg-blue);
      box-shadow: 0 18px 38px -22px rgba(38, 154, 218, 0.4);
      transform: translateX(4px);
    }
    .cbs-journey__icon {
      display: inline-flex;
      width: 48px; height: 48px;
      align-items: center; justify-content: center;
      border-radius: 14px;
      background: var(--bbg-blue-light);
      color: var(--bbg-blue);
      flex-shrink: 0;
    }
    .cbs-journey__icon svg { width: 24px; height: 24px; }
    .cbs-journey__title {
      font-size: 1.25rem; line-height: 1.25;
      margin: 0 0 0.4rem;
      letter-spacing: -0.005em;
    }
    .cbs-journey__desc {
      font-size: 0.95rem; line-height: 1.55;
      color: var(--bbg-text); margin: 0;
    }
    @media (max-width: 600px) {
      .cbs-journey { padding-left: 40px; }
      .cbs-journey__rail { left: 16px; }
      .cbs-journey__dot { left: -40px; width: 32px; height: 32px; }
      .cbs-journey__dot-inner { width: 10px; height: 10px; }
      .cbs-journey__card { flex-direction: column; gap: var(--space-3); }
    }

    /* ---------- SISTER PRODUCT ---------- */
    .cbs-sister {
      display: grid;
      grid-template-columns: 1.1fr 1fr;
      gap: var(--space-6);
      align-items: stretch;
    }
    .cbs-sister__hero {
      position: relative;
      padding: clamp(var(--space-6), 4vw, var(--space-8));
      background: linear-gradient(160deg, #1a0f1f 0%, #2a1530 60%, #401a45 100%);
      color: var(--bbg-white);
      border-radius: 24px;
      overflow: hidden;
      isolation: isolate;
      display: flex; flex-direction: column;
      gap: var(--space-4);
    }
    .cbs-sister__hero::before {
      content: "";
      position: absolute; inset: 0; z-index: -1;
      background:
        radial-gradient(60% 80% at 100% 0%, rgba(255, 90, 130, 0.25) 0%, transparent 60%),
        radial-gradient(40% 60% at 0% 100%, rgba(255, 60, 100, 0.18) 0%, transparent 60%);
    }
    .cbs-sister__chip {
      display: inline-block;
      align-self: flex-start;
      font-size: 0.7rem; font-weight: 700;
      letter-spacing: 0.12em; text-transform: uppercase;
      color: #FFC2D1;
      background: rgba(255, 90, 130, 0.18);
      padding: 0.4em 0.85em;
      border-radius: 999px;
      border: 1px solid rgba(255, 194, 209, 0.3);
    }
    .cbs-sister__title {
      font-size: clamp(1.5rem, 1.6vw + 1rem, 2.125rem);
      line-height: 1.15;
      margin: 0;
      letter-spacing: -0.015em;
      color: var(--bbg-white);
    }
    .cbs-sister__desc {
      font-size: 1rem; line-height: 1.6;
      color: rgba(255, 255, 255, 0.82);
      margin: 0;
    }
    .cbs-sister__stats {
      list-style: none; padding: var(--space-4) 0 0;
      margin: 0;
      display: flex; flex-direction: column;
      gap: var(--space-3);
      border-top: 1px solid rgba(255, 255, 255, 0.14);
    }
    .cbs-sister__stats li {
      display: grid;
      grid-template-columns: minmax(96px, auto) 1fr;
      gap: var(--space-4);
      align-items: baseline;
    }
    .cbs-sister__stat-num {
      font-size: 1.25rem;
      font-weight: 700;
      color: #FFC2D1;
      letter-spacing: -0.01em;
      font-feature-settings: "tnum";
    }
    .cbs-sister__stat-label {
      font-size: 0.9rem;
      color: rgba(255, 255, 255, 0.78);
      line-height: 1.5;
    }
    .cbs-sister__actions {
      display: flex; flex-wrap: wrap; gap: var(--space-3);
      margin-top: var(--space-3);
    }
    .cbs-sister__btn-primary {
      background: var(--bbg-white);
      color: #2a1530;
      border: 1px solid var(--bbg-white);
    }
    .cbs-sister__btn-primary:hover {
      background: #FFC2D1; color: #2a1530;
    }
    .cbs-sister__btn-ghost {
      background: transparent;
      border: 1px solid rgba(255, 255, 255, 0.5);
      color: var(--bbg-white);
    }
    .cbs-sister__btn-ghost:hover {
      background: rgba(255, 255, 255, 0.12);
      border-color: var(--bbg-white);
      color: var(--bbg-white);
    }
    .cbs-sister__packs {
      display: flex; flex-direction: column;
      gap: var(--space-4);
    }
    .cbs-sister__pack {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      padding: var(--space-5);
      display: flex; flex-direction: column;
      gap: var(--space-2);
      transition: border-color 280ms ease, box-shadow 280ms ease, transform 280ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .cbs-sister__pack:hover {
      transform: translateY(-3px);
      border-color: var(--bbg-blue);
      box-shadow: 0 14px 28px -18px rgba(38, 154, 218, 0.35);
    }
    .cbs-sister__pack-icon {
      display: inline-flex;
      width: 36px; height: 36px;
      align-items: center; justify-content: center;
      border-radius: 10px;
      background: var(--bbg-blue-light);
      color: var(--bbg-blue);
      margin-bottom: 0.4rem;
    }
    .cbs-sister__pack-icon svg { width: 18px; height: 18px; }
    .cbs-sister__pack h4 {
      font-size: 1.0625rem;
      margin: 0;
      letter-spacing: -0.005em;
      color: var(--bbg-ink);
    }
    .cbs-sister__pack p {
      font-size: 0.92rem;
      line-height: 1.55;
      color: var(--bbg-text);
      margin: 0;
    }
    @media (max-width: 900px) {
      .cbs-sister { grid-template-columns: 1fr; }
    }

    /* ---------- FAQ ---------- */
    .cbs-faq {
      display: flex; flex-direction: column;
      gap: var(--space-3);
    }
    .cbs-faq__item {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-md);
      overflow: hidden;
      transition: border-color 240ms ease, box-shadow 240ms ease, transform 280ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .cbs-faq__item:hover {
      transform: translateX(2px);
      border-color: rgba(38, 154, 218, 0.4);
    }
    .cbs-faq__item[open] {
      border-color: var(--bbg-blue);
      box-shadow: 0 8px 22px -14px rgba(38, 154, 218, 0.4);
    }
    .cbs-faq__q {
      cursor: pointer;
      padding: var(--space-5);
      display: flex; align-items: center;
      justify-content: space-between;
      gap: var(--space-4);
      font-weight: 600;
      font-size: 1.0625rem; line-height: 1.3;
      color: var(--bbg-ink);
      list-style: none;
      transition: color var(--dur) var(--ease);
    }
    .cbs-faq__q::-webkit-details-marker { display: none; }
    .cbs-faq__q::marker { display: none; }
    .cbs-faq__item:hover .cbs-faq__q { color: var(--bbg-blue); }
    .cbs-faq__icon {
      flex-shrink: 0;
      width: 28px; height: 28px;
      display: inline-flex;
      align-items: center; justify-content: center;
      border-radius: 50%;
      background: var(--bbg-surface);
      color: var(--bbg-text-muted);
      transition: transform 320ms cubic-bezier(0.16, 1, 0.3, 1),
                  color var(--dur) var(--ease),
                  background var(--dur) var(--ease);
    }
    .cbs-faq__item[open] .cbs-faq__icon {
      transform: rotate(180deg);
      color: var(--bbg-white);
      background: var(--bbg-blue);
    }
    .cbs-faq__a {
      padding: 0 var(--space-5) var(--space-5);
      color: var(--bbg-text);
      line-height: 1.65;
      font-size: 1rem;
      animation: cbs-faq-in 320ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    @keyframes cbs-faq-in {
      from { opacity: 0; transform: translateY(-4px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .cbs-faq__a p { margin: 0; }

    /* ---------- FAQ FILTER ---------- */
    .cbs-faq-filter {
      display: flex; flex-wrap: wrap;
      gap: 0.5rem; justify-content: center;
      margin-bottom: var(--space-6);
    }
    .cbs-faq-chip {
      display: inline-flex; align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 0.95rem;
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: 999px;
      cursor: pointer;
      font-size: 0.875rem;
      font-weight: 600;
      color: var(--bbg-text);
      transition: all 220ms ease;
      font-family: inherit;
    }
    .cbs-faq-chip:hover { border-color: var(--bbg-blue); color: var(--bbg-blue); }
    .cbs-faq-chip.is-active {
      background: var(--bbg-blue);
      border-color: var(--bbg-blue);
      color: var(--bbg-white);
    }
    .cbs-faq-chip__count {
      font-size: 0.72rem; font-weight: 700;
      padding: 0.15em 0.5em;
      border-radius: 999px;
      background: var(--bbg-surface);
      color: var(--bbg-text-muted);
      transition: all 220ms ease;
    }
    .cbs-faq-chip:hover .cbs-faq-chip__count { background: var(--bbg-blue-light); color: var(--bbg-blue); }
    .cbs-faq-chip.is-active .cbs-faq-chip__count {
      background: rgba(255, 255, 255, 0.2);
      color: var(--bbg-white);
    }
    .cbs-faq__cat-tag {
      display: inline-block;
      font-size: 0.68rem; font-weight: 700;
      letter-spacing: 0.08em; text-transform: uppercase;
      color: var(--bbg-blue);
      background: var(--bbg-blue-light);
      padding: 0.25em 0.6em;
      border-radius: 4px;
      margin-right: var(--space-3);
      flex-shrink: 0;
    }
    .cbs-faq__q-text { flex-grow: 1; }
    .cbs-faq__item.is-hidden { display: none; }

    /* ---------- FINAL CTA ---------- */
    .cbs-cta {
      padding: clamp(var(--space-6), 4vw + 1rem, var(--space-8)) var(--space-6);
      background: linear-gradient(135deg, var(--bbg-blue) 0%, var(--bbg-blue-dark) 100%);
      color: var(--bbg-white);
      border-radius: var(--radius-lg);
      text-align: center;
      position: relative; overflow: hidden;
    }
    .cbs-cta::before {
      content: ""; position: absolute;
      top: -40%; right: -10%;
      width: 420px; height: 420px;
      background: radial-gradient(circle at center, rgba(255,255,255,0.18) 0%, rgba(255,255,255,0) 65%);
      pointer-events: none;
    }
    .cbs-cta .eyebrow--on-dark {
      display: inline-block;
      margin-bottom: var(--space-4);
      color: rgba(255, 255, 255, 0.9);
    }
    .cbs-cta__title {
      font-size: clamp(1.75rem, 1.5vw + 1rem, 2.5rem);
      line-height: 1.15;
      color: var(--bbg-white);
      margin: 0 0 var(--space-4);
      letter-spacing: -0.015em;
    }
    .cbs-cta__lead {
      color: rgba(255, 255, 255, 0.92);
      font-size: 1.0625rem;
      max-width: 52ch;
      margin: 0 auto var(--space-5);
      line-height: 1.55;
    }
    .cbs-cta__actions {
      display: flex; flex-wrap: wrap;
      gap: var(--space-3);
      justify-content: center;
      position: relative;
    }
    .cbs-cta__btn-primary {
      background: var(--bbg-white);
      color: var(--bbg-blue-dark);
      border: 1px solid var(--bbg-white);
    }
    .cbs-cta__btn-primary:hover {
      background: rgba(255, 255, 255, 0.92);
      color: var(--bbg-blue-dark);
    }
    .cbs-cta__btn-ghost {
      background: transparent;
      border: 1px solid rgba(255, 255, 255, 0.5);
      color: var(--bbg-white);
    }
    .cbs-cta__btn-ghost:hover {
      background: rgba(255, 255, 255, 0.12);
      border-color: var(--bbg-white);
      color: var(--bbg-white);
    }
  </style>

  <script is:inline>
    (() => {
      const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      // 1. Reveal-on-scroll
      const revealEls = document.querySelectorAll('.cbs-reveal');
      if (revealEls.length) {
        if (reduced || !('IntersectionObserver' in window)) {
          revealEls.forEach((el) => el.classList.add('is-in'));
        } else {
          const io = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add('is-in');
                io.unobserve(entry.target);
              }
            });
          }, { threshold: 0.12, rootMargin: '0px 0px -8% 0px' });
          revealEls.forEach((el) => io.observe(el));
        }
      }

      // 2. Animated number counters in the hero meta strip
      const counters = document.querySelectorAll('[data-counter]');
      if (counters.length && !reduced && 'IntersectionObserver' in window) {
        const animate = (el) => {
          const target = parseFloat(el.dataset.counter || '0');
          const suffix = el.dataset.suffix || '';
          const duration = 1400;
          const start = performance.now();
          const tick = (now) => {
            const t = Math.min(1, (now - start) / duration);
            const eased = 1 - Math.pow(1 - t, 3);
            const value = Math.round(target * eased);
            el.textContent = value + suffix;
            if (t < 1) requestAnimationFrame(tick);
          };
          requestAnimationFrame(tick);
        };
        const counterIo = new IntersectionObserver((entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              animate(entry.target);
              counterIo.unobserve(entry.target);
            }
          });
        }, { threshold: 0.5 });
        counters.forEach((el) => counterIo.observe(el));
      } else {
        counters.forEach((el) => {
          el.textContent = (el.dataset.counter || '0') + (el.dataset.suffix || '');
        });
      }

      // 3. Hero parallax + scroll progress bar
      const parallaxEl = document.querySelector('[data-parallax]');
      const progressEl = document.querySelector('.cbs-progress__bar');
      const heroEl = document.querySelector('[data-hero]');
      let ticking = false;
      const onScroll = () => {
        if (ticking) return;
        ticking = true;
        requestAnimationFrame(() => {
          const scrollY = window.scrollY || window.pageYOffset;
          if (parallaxEl && heroEl && !reduced) {
            const heroH = heroEl.offsetHeight;
            if (scrollY < heroH) {
              parallaxEl.style.setProperty('--parallax-y', (scrollY * 0.18) + 'px');
            }
          }
          if (progressEl) {
            const docH = document.documentElement.scrollHeight - window.innerHeight;
            const pct = docH > 0 ? Math.min(100, (scrollY / docH) * 100) : 0;
            progressEl.style.setProperty('--progress', pct + '%');
          }
          ticking = false;
        });
      };
      window.addEventListener('scroll', onScroll, { passive: true });
      onScroll();

      // 4. Smooth scroll for anchors
      document.querySelectorAll('a[href^="#"]').forEach((link) => {
        link.addEventListener('click', (e) => {
          const id = link.getAttribute('href');
          if (!id || id === '#') return;
          const target = document.querySelector(id);
          if (!target) return;
          e.preventDefault();
          target.scrollIntoView({ behavior: reduced ? 'auto' : 'smooth', block: 'start' });
        });
      });

      // 5. FAQ accordion - close others on open
      const faqItems = document.querySelectorAll('.cbs-faq__item');
      faqItems.forEach((item) => {
        item.addEventListener('toggle', () => {
          if (item.open) {
            faqItems.forEach((other) => {
              if (other !== item && other.open) other.open = false;
            });
          }
        });
      });

      // 6. Business model tabs
      const tabsRoot = document.querySelector('[data-tabs]');
      if (tabsRoot) {
        const tabs = Array.from(tabsRoot.querySelectorAll('.cbs-tabs__tab'));
        const panels = Array.from(tabsRoot.querySelectorAll('.cbs-tabs__panel'));
        const indicator = tabsRoot.querySelector('.cbs-tabs__indicator');

        const setActive = (tab) => {
          tabs.forEach((t) => {
            const active = t === tab;
            t.classList.toggle('is-active', active);
            t.setAttribute('aria-selected', active ? 'true' : 'false');
          });
          const targetId = tab.getAttribute('data-tab-target');
          panels.forEach((p) => {
            const active = p.id === targetId;
            p.classList.toggle('is-active', active);
            if (active) p.removeAttribute('hidden');
            else p.setAttribute('hidden', '');
          });
          if (indicator) {
            const idx = tabs.indexOf(tab);
            const w = 100 / tabs.length;
            indicator.style.width = w + '%';
            indicator.style.transform = `translateX(${idx * 100}%)`;
          }
        };

        tabs.forEach((tab) => {
          tab.addEventListener('click', () => setActive(tab));
          tab.addEventListener('keydown', (e) => {
            const idx = tabs.indexOf(tab);
            if (e.key === 'ArrowRight') {
              e.preventDefault();
              const next = tabs[(idx + 1) % tabs.length];
              next.focus(); setActive(next);
            } else if (e.key === 'ArrowLeft') {
              e.preventDefault();
              const prev = tabs[(idx - 1 + tabs.length) % tabs.length];
              prev.focus(); setActive(prev);
            }
          });
        });

        const initialActive = tabs.find((t) => t.classList.contains('is-active')) || tabs[0];
        if (initialActive) setActive(initialActive);
      }

      // 7. Journey vertical timeline scroll progress + active state
      const journey = document.querySelector('[data-journey]');
      if (journey) {
        const fill = journey.querySelector('[data-journey-fill]');
        const items = Array.from(journey.querySelectorAll('[data-journey-item]'));
        const updateJourney = () => {
          const rect = journey.getBoundingClientRect();
          const vh = window.innerHeight;
          const total = rect.height;
          const visible = Math.max(0, Math.min(total, vh * 0.55 - rect.top));
          const pct = Math.max(0, Math.min(100, (visible / total) * 100));
          if (fill) fill.style.setProperty('--journey-progress', pct + '%');
          items.forEach((item) => {
            const r = item.getBoundingClientRect();
            const center = r.top + r.height / 2;
            const inActive = center < vh * 0.6 && center > 0;
            item.classList.toggle('is-active', inActive);
          });
        };
        let journeyTicking = false;
        const onJourneyScroll = () => {
          if (journeyTicking) return;
          journeyTicking = true;
          requestAnimationFrame(() => {
            updateJourney();
            journeyTicking = false;
          });
        };
        window.addEventListener('scroll', onJourneyScroll, { passive: true });
        window.addEventListener('resize', onJourneyScroll, { passive: true });
        updateJourney();
      }

      // 8. FAQ category filter
      const faqFilter = document.querySelector('[data-faq-filter]');
      const faqList = document.querySelector('[data-faq-list]');
      if (faqFilter && faqList) {
        const chips = Array.from(faqFilter.querySelectorAll('.cbs-faq-chip'));
        const items = Array.from(faqList.querySelectorAll('.cbs-faq__item'));
        chips.forEach((chip) => {
          chip.addEventListener('click', () => {
            const cat = chip.dataset.faqCat;
            chips.forEach((c) => {
              const on = c === chip;
              c.classList.toggle('is-active', on);
              c.setAttribute('aria-selected', on ? 'true' : 'false');
            });
            items.forEach((item) => {
              const matches = cat === 'All' || item.dataset.faqCat === cat;
              item.classList.toggle('is-hidden', !matches);
            });
          });
        });
      }
    })();
  </script>
</Layout>
