---
import Layout from '../../layouts/Layout.astro';

const promises = [
  {
    title: 'Senior operators run the account',
    desc: 'Strategy and execution by the same people. No junior handoff once the contract is signed.',
  },
  {
    title: 'Phased commitment',
    desc: 'Start with one platform. Add the next when the first one works. No 12-month all-in deals.',
  },
  {
    title: 'Independent creator sourcing',
    desc: 'We do not take kickbacks from creators or agencies. Your interests sit on our side of the table.',
  },
];

const services = [
  {
    icon: 'store',
    title: 'Platform operations',
    body: 'We run your stores and accounts on Douyin, RedNote (Xiaohongshu), WeChat and Weibo, plus WeChat Channels and Douyin Mall. Daily posting, content, customer service, ad management.',
    href: '#decision-point',
  },
  {
    icon: 'creator',
    title: 'Creator and KOL programs',
    body: 'A vetted roster across beauty, food and beverage, home, fashion, mother and baby, tech and lifestyle. We negotiate, brief, produce, and measure. And we will tell you when a creator is overpriced.',
    href: '#kol',
  },
  {
    icon: 'video',
    title: 'Livestream commerce',
    body: 'Store-led livestreams on Douyin and Taobao Live, three to seven sessions a week. We staff the room, write the script, design the set, run the ads.',
    href: '#livestream',
  },
  {
    icon: 'film',
    title: 'Content production',
    body: 'Short videos, RedNote notes, livestream sets, product visuals. Shot in our Shanghai studio or on location, in Mandarin, with talent that fits the brand.',
    href: '/grow-in-china/production-studio',
  },
  {
    icon: 'chart',
    title: 'Performance and ad operations',
    body: 'Douyin Qianchuan, RedNote effect ads, WeChat Moments, Weibo. We allocate, test, cut what is not working and put more behind what is. Weekly reports stay simple.',
    href: '#how-we-work',
  },
  {
    icon: 'clipboard',
    title: 'Measurement and reporting',
    body: 'Sales by SKU, by creator, by livestream, by ad. GMV, conversion, repeat rate. The numbers that actually tell you what to do tomorrow.',
    href: '#measurement',
  },
];

const models = [
  {
    name: 'DP model',
    tag: 'You own the inventory, we run the store',
    desc: 'You keep the stock on your books and the customer relationship in your name. We act as your DP (Designated Partner) on Douyin EC and RedNote. Content, livestream, paid media, customer service, KOL and KOC programs: all run by us, all under your brand.',
    when: 'You want the China P&L on your own books, you want to build long-term brand equity on Douyin and RedNote, and you have the working capital to fund inventory plus media spend.',
    owns: [
      { label: 'Inventory', value: 'Yours, in a bonded or domestic warehouse' },
      { label: 'Store license', value: 'Yours' },
      { label: 'Customer data', value: 'Yours' },
      { label: 'Content, accounts, KOL contracts', value: 'Yours' },
      { label: 'Media and promotion spend', value: '100% funded by you' },
      { label: 'Revenue', value: 'Direct-to-consumer GMV, on your books' },
    ],
    yourSide: [
      'Full retail margin on every sale',
      'Direct visibility into customer data and CRM',
      'Brand control across the storefront, the feed and every livestream',
      'Monthly P&L review with the senior leads',
    ],
    ourSide: [
      'Daily store operations, listing updates, short-video calendar',
      'Livestream production, three to seven sessions a week, set, host and script included',
      'Paid media across Douyin Qianchuan and RedNote effect ads',
      'KOL and KOC seeding, negotiation, briefing and reporting',
      'Mandarin customer service and review moderation',
      'Platform compliance, content vetting, penalty negotiation',
    ],
    image: '/Images/grow-in-china/social-commerce/route-dp-unboxing.png',
    imageAlt: 'A young Chinese woman opening a Douyin parcel on the floor of her apartment, the livestream she bought from still playing on her phone',
    highlight: true,
  },
  {
    name: 'Distributor model',
    tag: 'We own the inventory, we run the store',
    desc: 'We buy your product at wholesale and own the stock in China. The Douyin and RedNote stores are run under our license, the inventory sits on our balance sheet, and we carry the commercial risk. You sell to us, we sell to the consumer.',
    when: 'You are already known in China, want the Douyin and RedNote revenue without the operational complexity, and would rather not tie up cash in inventory. New-to-market brands rarely fit this model.',
    owns: [
      { label: 'Inventory', value: 'Ours' },
      { label: 'Store license', value: 'Ours' },
      { label: 'Customer data', value: 'Ours' },
      { label: 'Content, accounts, KOL contracts', value: 'Ours' },
      { label: 'Media and promotion spend', value: 'Brand budget + BBG co-invest' },
      { label: 'Revenue', value: 'Wholesale invoice, recognised as B2B' },
    ],
    yourSide: [
      'Wholesale revenue with no inventory risk',
      'A single B2B invoice instead of a direct-to-consumer P&L',
      'No working capital tied up in China stock',
      'Aggregated performance reporting and category insight',
    ],
    ourSide: [
      'Inventory purchase, financing and warehousing',
      'Everything in the DP model, plus the commercial risk',
      'Pricing strategy on Douyin and RedNote within agreed brand guardrails',
      'Returns, refunds, customer service liability',
    ],
    image: '/Images/grow-in-china/social-commerce/route-distributor-livingroom.png',
    imageAlt: 'A Chinese mother watching a Douyin livestream on her tablet while her daughter plays nearby on the rug',
    highlight: false,
  },
];

const compareRows = [
  { label: 'Who owns the inventory', tp: 'You', dist: 'BBG' },
  { label: 'Who owns the store and the data', tp: 'You', dist: 'BBG' },
  { label: 'Working capital you put in', tp: 'High', dist: 'None' },
  { label: 'Revenue you book', tp: 'Retail GMV minus fees', dist: 'Wholesale price' },
  { label: 'Margin upside', tp: 'Full retail margin', dist: 'Capped at wholesale' },
  { label: 'Risk you carry', tp: 'Stock and demand', dist: 'None on inventory' },
  { label: 'Media and promotion costs', tp: '100% on you', dist: 'Brand budget + BBG co-invest' },
  { label: 'Brand equity on Douyin and RedNote', tp: 'Yours, fully', dist: 'Yours, built through us' },
  { label: 'Best for', tp: 'Brands building long-term China social commerce', dist: 'Brands already known in China' },
];

const rhythms = [
  {
    name: 'Daily',
    tag: 'Content and community',
    tabId: 'daily',
    pill: 'Daily',
    title: 'Content and community',
    desc: 'Your feed stays alive. The algorithm rewards consistency, and silence costs reach faster than most brands expect.',
    outcome: 'Your feed stays alive, week after week, with a content rhythm the algorithm actually rewards.',
    image: '/Images/grow-in-china/social-commerce/cadence-daily-content.png',
    imageAlt: 'Two Chinese content editors at adjacent desks reviewing short-video edits for a Douyin feed',
    does: [
      'Daily short-video posting on Douyin and RedNote',
      'Comment moderation and DM response in Mandarin',
      'Trend monitoring and rapid-response content',
      'UGC and KOC content sourcing',
    ],
    deliverables: [
      'Weekly content calendar',
      'Daily community report',
    ],
  },
  {
    name: 'Weekly',
    tag: 'Livestream',
    tabId: 'livestream',
    pill: 'Weekly',
    title: 'Livestream',
    desc: 'Store-led livestream is the weekly rhythm that builds a real revenue line. Not a celebrity event once a quarter. A working room, three to seven nights a week.',
    outcome: 'A predictable revenue line, week after week, not just on festival days.',
    image: '/Images/grow-in-china/social-commerce/cadence-weekly-livestream.png',
    imageAlt: 'A livestream control desk during a Douyin session, screens showing sales numbers and viewer counts in real time',
    does: [
      'Three to seven Douyin livestream sessions per week',
      'Script writing, host briefing, set design',
      'Real-time Qianchuan optimization behind the stream',
      'Post-livestream sell-through and SKU performance report',
    ],
    deliverables: [
      'Weekly livestream sell-through report',
      'Monthly host performance review',
    ],
  },
  {
    name: 'Monthly',
    tag: 'KOL and KOC',
    tabId: 'kol',
    pill: 'Monthly',
    title: 'KOL and KOC campaigns',
    desc: 'The creator layer. We brief, negotiate and measure across a vetted roster, and we tell you when a quote is inflated. Most of them are at the moment.',
    outcome: 'Awareness, trust and conversion at scale, without overpaying for it.',
    image: '/Images/grow-in-china/social-commerce/cadence-monthly-kol.png',
    imageAlt: 'A Chinese KOL filming a RedNote skincare review by a window in natural afternoon light',
    does: [
      'Creator shortlist sourced from our vetted roster',
      'Brief, content review, negotiation and payment',
      'Seeding programs with 20 to 40 KOCs a month',
      'A couple of KOL collaborations a month, sometimes up to five',
    ],
    deliverables: [
      'Monthly creator performance report',
      'Cost-per-acquisition by creator',
    ],
  },
  {
    name: 'Quarterly',
    tag: 'Festival push',
    tabId: 'measurement',
    pill: 'Quarterly',
    title: 'Festival push and measurement',
    desc: "The big moments (11.11, 6.18, Women's Day, CNY) carry their weight. Festival prep starts six months out, not the week before. Then the report tells you what to change for the next one.",
    outcome: 'Festivals carry their share of the year. The post-mortem tells you what to change for the next one.',
    image: '/Images/grow-in-china/social-commerce/cadence-quarterly-festival.png',
    imageAlt: 'A Douyin store dashboard during the 11.11 festival with the sales counter climbing in red Chinese characters',
    does: [
      'Festival plan built six months out',
      'Inventory, pricing and promotion mechanics',
      'Pre-festival traffic build, festival day operations, post-festival CRM',
      'Daily reporting during the festival window',
    ],
    deliverables: [
      'Festival plan deck',
      'Post-festival report with year-on-year comparison',
    ],
  },
];

const sisterPackages = [
  {
    title: 'WeChat and Weibo',
    desc: 'Daily content, community, and KOL seeding on the platforms where considered purchases still live. For brands where trust matters more than impulse buys.',
  },
  {
    title: 'RedNote and Douyin content',
    desc: 'Short videos, notes and creator partnerships, without a storefront behind them. For brands building awareness ahead of a future commerce launch.',
  },
  {
    title: 'Always-on social retainer',
    desc: 'A single monthly fee that covers the four channels. Run by the same operators as your commerce, on a lighter scope.',
  },
];

const contextCards = [
  {
    image: '/Images/grow-in-china/social-commerce/truth-content-engine.png',
    alt: 'A Chinese content producer reviewing short-video drafts on a studio monitor, a notebook of handwritten Chinese notes beside her',
    kicker: 'Content is sales',
    title: 'Content is the sales team',
    body: 'Weak content means weak sales. If a shopper scrolls past in two seconds, the product never gets seen. We shoot it ourselves, in Mandarin, every day.',
    flow: ['Shoot', 'Edit', 'Ship'],
    chipLabel: 'Daily content sync',
    chipSub: 'Producers, editors, host, all in one room',
    avatars: [
      { letter: 'M', tone: 'blue' },
      { letter: 'J', tone: 'dark' },
      { letter: 'X', tone: 'green' },
      { letter: 'L', tone: 'amber' },
    ],
    extraAvatars: '+4',
    pinLabel: 'Output',
    pinValue: '15 to 30 videos a month',
  },
  {
    image: '/Images/grow-in-china/social-commerce/truth-daily-operation.png',
    alt: 'A Douyin livestream room mid-session, host on camera in the back, two producers watching the live dashboard in the foreground',
    kicker: 'Year-round rhythm',
    title: 'A daily operation, not a campaign',
    body: 'Douyin stores need daily posts, weekly livestreams and ads behind both. The algorithm punishes silence.',
    credentials: ['Posts', 'Livestreams', 'Ads', 'Comments'],
    chipLabel: 'Live on platform',
    chipSub: 'Eight to twenty livestreams a month',
    badge: 'The algorithm punishes silence',
    badgeSub: 'So we run the rhythm, festival or not.',
  },
];

const faqs = [
  { cat: 'Strategy', q: 'What is the real difference between e-commerce and social commerce in China?', a: 'E-commerce is search behavior. A shopper already knows what she wants and goes to Tmall or JD to find it. Social commerce is discovery behavior. She did not know she wanted it until the feed showed her. Different mindset, different content, different metrics. Most brands need both, but they need to know which one drives the business.' },
  { cat: 'Platforms', q: 'Which platforms should we be on first?', a: 'Depends on the category. Beauty and lifestyle: RedNote first, Douyin second. FMCG and food: Douyin first. B2B and considered purchases: WeChat. Premium and luxury: RedNote and WeChat. Trying to be on all four platforms in month one is the most common mistake we see. You spread thin and you win nowhere.' },
  { cat: 'Cost', q: 'How much does it cost to launch on Douyin or RedNote?', a: 'Setup runs roughly 80,000 to 150,000 RMB depending on store complexity and content needs. (We have done one launch for less, but it cut corners we now regret.) Monthly operations run 50,000 to 250,000 RMB depending on content volume, ad management, and livestream frequency. Media spend sits on top of that. Plan for at least 100,000 RMB a month in media for the first six months while you learn what actually works for your category. Add the platform deposits: 30,000 RMB for Douyin and 20,000 RMB for RedNote, both refundable.' },
  { cat: 'Cost', q: 'What does Tmall Global cost on top of that?', a: 'A 30,000 USD deposit, a 5,000 to 10,000 USD annual service fee depending on category, and a commission of 2% to 5% per transaction, plus a 1% Alipay fee. Cross-border, so no Chinese entity required.' },
  { cat: 'Strategy', q: 'How long before we see sales?', a: 'On Douyin, first sales come within weeks if you run paid traffic. Profitability takes three to six months. On RedNote, brand build comes first. Sales follow at six to nine months. Anyone promising faster is overselling.' },
  { cat: 'Operations', q: 'When should we start preparing for 11.11 or 6.18?', a: 'Six months out. The Chinese shopping festival calendar is the single most underestimated planning constraint we see with foreign brands. 11.11 prep starts in May. 6.18 prep starts in December. Brands that walk in three months out get the leftover ad inventory and the leftover KOL slots, both at premium prices.' },
  { cat: 'Strategy', q: 'Do we need a China-based legal entity?', a: 'Not always. Cross-border models (Douyin Cross-Border, Tmall Global, JD Worldwide) do not require one. Onshore Douyin, Tmall, JD does, or a TP partner who provides one. We can run either. And if you already have a WFOE that was set up for offline trade and now needs to do digital commerce, we will tell you whether to amend the business scope, set up a second entity, or stay cross-border. The wrong setup costs more than the right one ever did.' },
  { cat: 'Operations', q: 'Can we just use our global content?', a: 'Almost never. Lighting, pacing, voiceover style, on-screen text, color grading, even the way hands are framed on camera. All different. Chinese shoppers spot translated foreign content in two seconds and scroll past. We re-shoot or re-edit for the local platforms.' },
  { cat: 'Creators', q: 'What is a KOL versus a KOC?', a: 'KOL: Key Opinion Leader. Big follower count, higher fee, awareness driver. KOC: Key Opinion Consumer. Smaller audience, more authentic, conversion driver. Most campaigns need both. KOCs do the volume. KOLs do the credibility.' },
  { cat: 'Cost', q: 'How much do KOLs actually charge?', a: 'A mid-tier RedNote creator with 100,000 followers runs 15,000 to 40,000 RMB per note. A top-tier Douyin host runs 200,000 to 2 million RMB per livestream slot, plus a commission of 20% to 30% of GMV. Pure-commission deals exist with smaller hosts but margin gets thin fast. We benchmark every quote against the live market, because the rates move month to month.' },
  { cat: 'Creators', q: 'Is a celebrity livestream worth it?', a: 'Almost never. The big-name livestreams produce a spike followed by silence, and an inflated cost-per-acquisition that looks great in slide three of the campaign report. Store-led livestreams running three to seven sessions a week build a predictable revenue line. We recommend the second, nearly every time.' },
  { cat: 'Operations', q: 'How do we handle returns and customer service?', a: 'Chinese platforms require fast response, under one minute on Douyin during peak hours, and a high return tolerance. The 7-day no-reason return is standard. We run customer service in Mandarin, on platform, with response-time SLAs. Returns go through our warehouse or yours.' },
  { cat: 'Operations', q: 'What about content censorship?', a: 'Specific words, claims, and images get flagged or blocked. Health claims. Direct comparisons with competitors. Certain political and cultural references. We vet every piece of content before it goes live. Mistakes cost the account, and sometimes the account does not come back.' },
  { cat: 'Cost', q: 'How are payments handled?', a: 'WeChat Pay and Alipay dominate. Platforms collect on your behalf and settle on a schedule, usually weekly or every 15 days. Cross-border models settle in USD or EUR to your overseas account. Onshore models settle in RMB to your Chinese entity.' },
  { cat: 'Strategy', q: 'Can we run a campaign without setting up a Douyin or RedNote store?', a: 'Yes, for awareness. KOL seeding, brand notes, livestreams hosted by creators selling from their own stores. You will not own the customer or the data, but it is a valid way to test demand before opening your own store.' },
  { cat: 'Platforms', q: 'What does a typical content calendar look like?', a: 'On RedNote: 8 to 15 notes a month from the brand account, 20 to 40 KOC notes, 2 to 5 KOL notes. On Douyin: 15 to 30 short videos a month, 8 to 20 livestream sessions. Volume is not optional. The algorithms punish silence.' },
  { cat: 'Strategy', q: 'How do we measure ROI?', a: 'Three numbers we track every week: cost per acquisition, GMV per livestream hour, content engagement rate. Three numbers every month: repeat purchase rate, total media ROAS, brand search lift. Everything else is supporting detail.' },
  { cat: 'Cost', q: 'What goes wrong most often?', a: 'Three things, mostly in this order. Brands underinvest in content production. Brands hire one cheap creator and expect a whole campaign to deliver. And brands stop spending after month two because the results are not there yet, which kills the momentum right before it would have started to compound. We have watched this play out maybe a dozen times in the last two years.' },
  { cat: 'Platforms', q: 'Do we need to be on WeChat if we are already on Douyin and RedNote?', a: 'For B2C, you can probably skip WeChat in year one if the budget is tight, with one caveat: WeChat Channels has turned into a real social commerce surface over the last 18 months, and the gap with Douyin closes every quarter. For B2B, premium, and any brand selling a considered purchase, WeChat is not optional. That is where high-value customers stay long enough to buy a second and a third time.' },
  { cat: 'Platforms', q: 'How do we get started with you?', a: 'A 30-minute call with our senior team, no junior intermediary. You tell us the category, the budget range, and the timeline. We tell you what is realistic, what platforms to start with, and what the first 90 days look like. No proposal until both sides agree the project makes sense.' },
];

const faqCategories = ['All', 'Strategy', 'Cost', 'Platforms', 'Operations', 'Creators'];

const icons: Record<string, string> = {
  store: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l1.5-5h15L21 9"/><path d="M3 9v10a1 1 0 0 0 1 1h16a1 1 0 0 0 1-1V9"/><path d="M3 9h18"/><path d="M9 13h6"/></svg>`,
  creator: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="4"/><path d="M4 21c0-4 4-7 8-7s8 3 8 7"/><path d="M18 3l0.8 1.7 1.8 0.3-1.3 1.3 0.3 1.8L18 7.3 16.4 8.1l0.3-1.8L15.4 5l1.8-0.3z"/></svg>`,
  video: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="6" width="14" height="12" rx="2"/><polygon points="17 9 22 6 22 18 17 15"/><circle cx="8" cy="11" r="1.2" fill="currentColor"/></svg>`,
  film: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="16" rx="2"/><line x1="7" y1="4" x2="7" y2="20"/><line x1="17" y1="4" x2="17" y2="20"/><line x1="3" y1="9" x2="7" y2="9"/><line x1="3" y1="15" x2="7" y2="15"/><line x1="17" y1="9" x2="21" y2="9"/><line x1="17" y1="15" x2="21" y2="15"/></svg>`,
  chart: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M3 20h18"/><rect x="5" y="13" width="3" height="7"/><rect x="11" y="8" width="3" height="12"/><rect x="17" y="4" width="3" height="16"/></svg>`,
  clipboard: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><rect x="6" y="4" width="12" height="17" rx="2"/><path d="M9 4V3a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v1"/><line x1="9" y1="10" x2="15" y2="10"/><line x1="9" y1="14" x2="15" y2="14"/><line x1="9" y1="18" x2="13" y2="18"/></svg>`,
  scroll: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M4 5a2 2 0 0 1 2-2h11a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2z"/><path d="M8 7h7M8 11h7M8 15h4"/></svg>`,
  outcome: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"/></svg>`,
  doc: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M14 3H7a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h10a2 2 0 0 0 2-2V8z"/><path d="M14 3v5h5"/><line x1="9" y1="13" x2="15" y2="13"/><line x1="9" y1="17" x2="13" y2="17"/></svg>`,
};

const faqJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((f) => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a },
  })),
};

const serviceJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  serviceType: 'Social commerce management',
  provider: {
    '@type': 'Organization',
    name: 'Beyond Border Group',
    url: 'https://beyondbordergroup.com',
  },
  areaServed: { '@type': 'Country', name: 'China' },
};
---

<Layout
  title="Social Commerce in China for Foreign Brands"
  description="Social commerce in China for foreign brands. Douyin, RedNote, WeChat and Weibo stores, KOL programs, weekly livestreams, run by senior operators."
>
  <script type="application/ld+json" set:html={JSON.stringify(faqJsonLd)} is:inline />
  <script type="application/ld+json" set:html={JSON.stringify(serviceJsonLd)} is:inline />

  <div class="cbs-progress" aria-hidden="true"><div class="cbs-progress__bar"></div></div>
  <article class="cbs">

    {/* ---------- HERO ---------- */}
    <section class="cbs-hero" data-hero>
      <div class="cbs-hero__bg" aria-hidden="true" data-parallax>
        <img src="/Images/grow-in-china/social-commerce/hero-douyin-livestream.png" alt="A young Chinese livestream host on a Douyin set, ring light glowing, holding a skincare bottle mid-pitch" />
      </div>
      <div class="cbs-hero__overlay" aria-hidden="true"></div>
      <div class="cbs-hero__grain" aria-hidden="true"></div>

      <div class="container cbs-hero__inner">
        <nav class="cbs-hero__crumbs" aria-label="Breadcrumb">
          <a href="/">Home</a>
          <span aria-hidden="true">/</span>
          <a href="/#services">Grow in China</a>
        </nav>
        <span class="eyebrow eyebrow--on-dark cbs-reveal">Social commerce</span>
        <h1 class="cbs-hero__title cbs-reveal" style="--reveal-delay: 80ms;">
          <span class="cbs-hero__title-row">In China, people don't go to the store.</span>
          <span class="cbs-hero__title-row cbs-hero__title-row--accent">The store comes to their feed.</span>
          <span class="cbs-hero__title-row cbs-hero__title-row--highlight">We run that operation.</span>
        </h1>
        <p class="cbs-hero__lead cbs-reveal" style="--reveal-delay: 200ms;">
          Social commerce in China isn't a layer on top of e-commerce. It's the primary
          way most consumer brands sell here now. We run it for global consumer brands.
          The whole operation, not the deck.
        </p>
        <div class="cbs-hero__actions cbs-reveal" style="--reveal-delay: 280ms;">
          <a href="/contact" class="btn btn--primary cbs-magnetic">Talk to us</a>
          <a href="#how-we-work" class="btn btn--ghost-light cbs-magnetic">See how we work</a>
        </div>

        <div class="cbs-hero__meta cbs-reveal" style="--reveal-delay: 360ms;">
          <div class="cbs-hero__meta-item">
            <span class="cbs-hero__meta-num" data-counter="15" data-suffix="+">0</span>
            <span class="cbs-hero__meta-label">years running Douyin and RedNote for foreign brands</span>
          </div>
          <div class="cbs-hero__meta-item">
            <span class="cbs-hero__meta-num" data-counter="40" data-suffix="+">0</span>
            <span class="cbs-hero__meta-label">active social commerce stores and accounts</span>
          </div>
          <div class="cbs-hero__meta-item">
            <span class="cbs-hero__meta-num">One team</span>
            <span class="cbs-hero__meta-label">Strategy, content, livestream and ads end to end</span>
          </div>
        </div>
      </div>

      <a href="#two-truths" class="cbs-hero__scroll" aria-label="Scroll to next section">
        <span class="cbs-hero__scroll-line"></span>
        <span class="cbs-hero__scroll-text">Scroll</span>
      </a>
    </section>

    {/* ---------- TWO TRUTHS ---------- */}
    <section id="two-truths" class="section cbs-truths-section">
      <div class="cbs-truths-bg" aria-hidden="true">
        <div class="cbs-truths-bg__blob cbs-truths-bg__blob--a"></div>
        <div class="cbs-truths-bg__blob cbs-truths-bg__blob--b"></div>
        <div class="cbs-truths-bg__grid"></div>
      </div>

      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">Our approach</span>
          <h2>Two things to know before we touch your social commerce</h2>
          <p class="section-header__lead">
            Most brands approach Douyin and RedNote the way they approach any new market.
            Neither platform rewards that approach. Two things shape how we work instead.
          </p>
        </header>

        <div class="cbs-truths" data-truths>
          {contextCards.map((c, i) => (
            <article
              class={`cbs-truth${i === 1 ? ' cbs-truth--alt' : ''} cbs-reveal`}
              style={`--reveal-delay: ${i * 140}ms;`}
              data-truth-tilt
            >
              <div class="cbs-truth__inner">
                <div class="cbs-truth__stage">
                  <div class="cbs-truth__frame">
                    <img
                      src={c.image}
                      alt={c.alt}
                      loading="lazy"
                      decoding="async"
                      data-truth-parallax
                    />
                    <div class="cbs-truth__shade" aria-hidden="true"></div>
                    <div class="cbs-truth__sheen" aria-hidden="true"></div>
                  </div>

                  <div class="cbs-truth__kicker" aria-hidden="true">
                    <span class="cbs-truth__kicker-dot"></span>
                    {c.kicker}
                  </div>

                  {i === 0 && (
                    <div class="cbs-truth__chip cbs-truth__chip--bl" data-float="1">
                      <div class="cbs-truth__chip-row">
                        <span class="cbs-truth__pulse" aria-hidden="true"></span>
                        <span class="cbs-truth__chip-label">{c.chipLabel}</span>
                      </div>
                      <p class="cbs-truth__chip-sub">{c.chipSub}</p>
                      <div class="cbs-truth__avatars">
                        {c.avatars && c.avatars.map((a) => (
                          <span class={`cbs-truth__avatar cbs-truth__avatar--${a.tone}`}>{a.letter}</span>
                        ))}
                        <span class="cbs-truth__avatar cbs-truth__avatar--more">{c.extraAvatars}</span>
                      </div>
                    </div>
                  )}

                  {i === 0 && (
                    <div class="cbs-truth__pin" data-float="2">
                      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                        <rect x="3" y="6" width="14" height="12" rx="2"/>
                        <polygon points="17 9 22 6 22 18 17 15"/>
                      </svg>
                      <div class="cbs-truth__pin-text">
                        <span class="cbs-truth__pin-label">{c.pinLabel}</span>
                        <span class="cbs-truth__pin-value">{c.pinValue}</span>
                      </div>
                    </div>
                  )}

                  {i === 1 && (
                    <div class="cbs-truth__chip cbs-truth__chip--tr" data-float="1">
                      <div class="cbs-truth__chip-row">
                        <span class="cbs-truth__pulse" aria-hidden="true"></span>
                        <span class="cbs-truth__chip-label">{c.chipLabel}</span>
                      </div>
                      <p class="cbs-truth__chip-sub">{c.chipSub}</p>
                    </div>
                  )}

                  {i === 1 && (
                    <div class="cbs-truth__badge" data-float="2">
                      <span class="cbs-truth__badge-eyebrow">Why it matters</span>
                      <span class="cbs-truth__badge-title">{c.badge}</span>
                      <span class="cbs-truth__badge-sub">{c.badgeSub}</span>
                    </div>
                  )}
                </div>

                <div class="cbs-truth__content">
                  <h3 class="cbs-truth__title">{c.title}</h3>
                  <p class="cbs-truth__body">{c.body}</p>

                  {i === 0 && c.flow && (
                    <ol class="cbs-truth__flow" aria-label="Daily content workflow">
                      {c.flow.map((step, idx) => (
                        <>
                          <li class="cbs-truth__flow-step" style={`--flow-delay: ${idx * 90}ms`}>
                            <span class="cbs-truth__flow-bubble">
                              {idx === 0 && (
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><rect x="3" y="6" width="14" height="12" rx="2"/><polygon points="17 9 22 6 22 18 17 15"/></svg>
                              )}
                              {idx === 1 && (
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/><path d="M18.5 2.5a2.1 2.1 0 1 1 3 3L12 15l-4 1 1-4z"/></svg>
                              )}
                              {idx === 2 && (
                                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M22 2L11 13"/><path d="M22 2L15 22 11 13 2 9z"/></svg>
                              )}
                            </span>
                            <span class="cbs-truth__flow-label">{step}</span>
                          </li>
                          {idx < c.flow.length - 1 && (
                            <li class="cbs-truth__flow-arrow" aria-hidden="true">
                              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
                            </li>
                          )}
                        </>
                      ))}
                    </ol>
                  )}

                  {i === 1 && c.credentials && (
                    <ul class="cbs-truth__tags" aria-label="What we run, daily">
                      {c.credentials.map((tag) => (
                        <li class="cbs-truth__tag">{tag}</li>
                      ))}
                    </ul>
                  )}
                </div>
              </div>
            </article>
          ))}
        </div>
      </div>
    </section>

    {/* ---------- WHY BRANDS CHOOSE US ---------- */}
    <section class="section section--alt">
      <div class="container">
        <div class="cbs-value">
          <div class="cbs-value__copy">
            <span class="eyebrow">Why brands choose us</span>
            <h2>Most social commerce agencies have never run a store. We have.</h2>
            <p>
              Our team comes from Publicis Commerce China. We have built and run Douyin
              and RedNote stores for global consumer brands. We know which creators
              convert because we have briefed them. We know what Qianchuan rewards
              because we have spent against it every day.
            </p>
            <blockquote class="cbs-quote cbs-quote--inline">
              <p>Most of the consultants who talk about Douyin and RedNote have never actually run a store on either one. We have, and that changes what we tell you.</p>
            </blockquote>
            <p>
              We have lost an 11.11 and won the next one. When we tell you what works
              in March, it is because we were inside the platform in February.
            </p>
            <a href="/about#team" class="btn btn--secondary cbs-magnetic">Meet the team</a>
          </div>

          <ul class="cbs-promise">
            <li>
              <span class="eyebrow">What you can expect</span>
            </li>
            {promises.map((p) => (
              <li class="cbs-promise__item">
                <span class="cbs-promise__check" aria-hidden="true">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="5 12 10 17 19 8"/></svg>
                </span>
                <div>
                  <h4>{p.title}</h4>
                  <p>{p.desc}</p>
                </div>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </section>

    {/* ---------- WHAT WE DO ---------- */}
    <section class="section">
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">What we do</span>
          <h2>Six things, run by one team</h2>
          <p class="section-header__lead">
            Not four agencies, four briefs and four invoices. One operator across content,
            commerce, creators and media. The strategy that goes into the deck is the one
            that runs on the platform on Monday.
          </p>
        </header>

        <div class="cbs-services cbs-reveal">
          {services.map((s, i) => (
            <a href={s.href} class="cbs-service" style={`--reveal-delay: ${i * 60}ms;`}>
              <span class="cbs-service__icon" aria-hidden="true" set:html={icons[s.icon]} />
              <h3 class="cbs-service__title">{s.title}</h3>
              <p class="cbs-service__body">{s.body}</p>
              <span class="cbs-service__arrow">
                Learn more
                <span class="cbs-service__arrow-icon" aria-hidden="true">→</span>
              </span>
            </a>
          ))}
        </div>
      </div>
    </section>

    {/* ---------- DECISION POINT (Two models) ---------- */}
    <section id="decision-point" class="section section--alt cbs-models-section">
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">Decision point</span>
          <h2>Who owns the inventory. Two ways we run your social store.</h2>
          <p class="section-header__lead">
            The first commercial question on any Douyin or RedNote store is who owns the
            stock. That answer shapes the contract, the margin and how you book China
            revenue. We work in two models. Pick the one that fits how you want China on
            your books.
          </p>
        </header>

        <div class="cbs-tabs cbs-tabs--two cbs-reveal" data-tabs>
          <div class="cbs-tabs__nav" role="tablist">
            {models.map((m, i) => (
              <button
                type="button"
                class={`cbs-tabs__tab${i === 0 ? ' is-active' : ''}`}
                role="tab"
                aria-selected={i === 0 ? 'true' : 'false'}
                aria-controls={`model-panel-${i}`}
                id={`model-tab-${i}`}
                data-tab-target={`model-panel-${i}`}
              >
                <span class="cbs-tabs__tab-label">{m.name}</span>
                <span class="cbs-tabs__tab-tag">{m.tag}</span>
                {m.highlight && <span class="cbs-tabs__tab-pop">Most common</span>}
              </button>
            ))}
            <span class="cbs-tabs__indicator" aria-hidden="true"></span>
          </div>

          <div class="cbs-tabs__panels">
            {models.map((m, i) => (
              <div
                class={`cbs-tabs__panel${i === 0 ? ' is-active' : ''}`}
                role="tabpanel"
                id={`model-panel-${i}`}
                aria-labelledby={`model-tab-${i}`}
                hidden={i !== 0}
              >
                <div class="cbs-tabs__panel-grid">
                  <div class="cbs-tabs__lead">
                    <span class="cbs-tabs__pillset">
                      <span class="cbs-tabs__pill">{m.tag}</span>
                    </span>
                    <h3 class="cbs-tabs__title">{m.name}</h3>
                    <p class="cbs-tabs__desc">{m.desc}</p>
                    <div class="cbs-tabs__when">
                      <span class="cbs-tabs__when-label">Best when</span>
                      <p>{m.when}</p>
                    </div>

                    <div class="cbs-owns">
                      <span class="cbs-owns__label">Who owns what</span>
                      <ul class="cbs-owns__list">
                        {m.owns.map((o) => (
                          <li>
                            <span class="cbs-owns__key">{o.label}</span>
                            <span class="cbs-owns__val">{o.value}</span>
                          </li>
                        ))}
                      </ul>
                    </div>

                    <a href="/contact" class="btn btn--primary cbs-magnetic">Talk about the {m.name}</a>
                  </div>

                  <div class="cbs-tabs__body">
                    <figure class="cbs-tabs__image">
                      <img src={m.image} alt={m.imageAlt} loading="lazy" decoding="async" />
                    </figure>

                    <div class="cbs-tabs__list-wrap">
                      <span class="cbs-tabs__list-label">You receive</span>
                      <ul class="cbs-tabs__list">
                        {m.yourSide.map((b) => (
                          <li>
                            <span class="cbs-tabs__check" aria-hidden="true">
                              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="5 12 10 17 19 8"/></svg>
                            </span>
                            <span>{b}</span>
                          </li>
                        ))}
                      </ul>
                    </div>

                    <div class="cbs-tabs__list-wrap cbs-tabs__list-wrap--alt">
                      <span class="cbs-tabs__list-label">We handle</span>
                      <ul class="cbs-tabs__list">
                        {m.ourSide.map((b) => (
                          <li>
                            <span class="cbs-tabs__check cbs-tabs__check--alt" aria-hidden="true">
                              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="5 12 10 17 19 8"/></svg>
                            </span>
                            <span>{b}</span>
                          </li>
                        ))}
                      </ul>
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* ---------- HOW TO PICK: Comparison Table ---------- */}
        <div class="cbs-compare cbs-reveal">
          <div class="cbs-compare__intro">
            <span class="eyebrow">How to pick</span>
            <h3>One question, two answers</h3>
            <p>Do you want to own the customer relationship on Douyin and RedNote, or do you want a clean wholesale exit? That is what decides the model.</p>
          </div>
          <div class="cbs-compare__table-wrap">
            <table class="cbs-compare__table">
              <thead>
                <tr>
                  <th scope="col" class="cbs-compare__row-head-col"></th>
                  <th scope="col" class="cbs-compare__head cbs-compare__head--tp">
                    <span class="cbs-compare__head-label">DP model</span>
                    <span class="cbs-compare__head-sub">You own the inventory</span>
                  </th>
                  <th scope="col" class="cbs-compare__head cbs-compare__head--dist">
                    <span class="cbs-compare__head-label">Distributor model</span>
                    <span class="cbs-compare__head-sub">We own the inventory</span>
                  </th>
                </tr>
              </thead>
              <tbody>
                {compareRows.map((r) => (
                  <tr>
                    <th scope="row" class="cbs-compare__row-label">{r.label}</th>
                    <td class="cbs-compare__cell cbs-compare__cell--tp">{r.tp}</td>
                    <td class="cbs-compare__cell cbs-compare__cell--dist">{r.dist}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
          <p class="cbs-compare__note">
            The model that fits depends on two things: how big you want China social
            commerce to be in your global revenue, and how much cash flow you can put behind
            that ambition. Brands with high ambition and the working capital to fund
            inventory and media usually pick DP. Brands wanting steady China revenue without
            tying up cash usually pick distributor. Some brands run both in parallel across
            different categories. None of this is a maturity ladder.
          </p>
        </div>
      </div>
    </section>

    {/* ---------- HOW WE WORK / CADENCE ---------- */}
    <section id="how-we-work" class="section">
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">How we work</span>
          <h2>Four rhythms that run your social commerce</h2>
          <p class="section-header__lead">
            Once the model is set, the work does not stop, it just changes shape. Four
            rhythms run in parallel, all year.
          </p>
        </header>

        <div class="cbs-tabs cbs-reveal" data-tabs>
          <div class="cbs-tabs__nav" role="tablist">
            {rhythms.map((r, i) => (
              <button
                type="button"
                class={`cbs-tabs__tab${i === 0 ? ' is-active' : ''}`}
                role="tab"
                aria-selected={i === 0 ? 'true' : 'false'}
                aria-controls={`rhythm-panel-${i}`}
                id={`rhythm-tab-${i}`}
                data-tab-target={`rhythm-panel-${i}`}
                data-tab-id={r.tabId}
              >
                <span class="cbs-tabs__tab-label">{r.name}</span>
                <span class="cbs-tabs__tab-tag">{r.tag}</span>
              </button>
            ))}
            <span class="cbs-tabs__indicator" aria-hidden="true"></span>
          </div>

          <div class="cbs-tabs__panels">
            {rhythms.map((r, i) => (
              <div
                class={`cbs-tabs__panel${i === 0 ? ' is-active' : ''}`}
                role="tabpanel"
                id={`rhythm-panel-${i}`}
                aria-labelledby={`rhythm-tab-${i}`}
                hidden={i !== 0}
              >
                <div class="cbs-tabs__panel-grid">
                  <div class="cbs-tabs__lead">
                    <span class="cbs-tabs__pillset">
                      <span class="cbs-tabs__pill">{r.pill}</span>
                    </span>
                    <h3 class="cbs-tabs__title">{r.title}</h3>
                    <p class="cbs-tabs__desc">{r.desc}</p>

                    <div class="cbs-tabs__when">
                      <span class="cbs-tabs__when-label">Outcome</span>
                      <p>{r.outcome}</p>
                    </div>

                    <figure class="cbs-tabs__image">
                      <img src={r.image} alt={r.imageAlt} loading="lazy" decoding="async" />
                    </figure>
                  </div>

                  <div class="cbs-tabs__body">
                    <div class="cbs-tabs__list-wrap">
                      <span class="cbs-tabs__list-label">What we do</span>
                      <ul class="cbs-tabs__list">
                        {r.does.map((b) => (
                          <li>
                            <span class="cbs-tabs__check" aria-hidden="true">
                              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><polyline points="5 12 10 17 19 8"/></svg>
                            </span>
                            <span>{b}</span>
                          </li>
                        ))}
                      </ul>
                    </div>

                    <div class="cbs-tabs__list-wrap cbs-tabs__list-wrap--alt">
                      <span class="cbs-tabs__list-label">Deliverables</span>
                      <ul class="cbs-tabs__list">
                        {r.deliverables.map((d) => (
                          <li>
                            <span class="cbs-tabs__check cbs-tabs__check--alt" aria-hidden="true" set:html={icons.doc} />
                            <span>{d}</span>
                          </li>
                        ))}
                      </ul>
                    </div>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </section>

    {/* ---------- SISTER PRODUCT: TheRedScroll ---------- */}
    <section class="section section--alt">
      <div class="container">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">If you want social without commerce</span>
          <h2>Powered by TheRedScroll</h2>
          <p class="section-header__lead">
            Some brands want a Chinese social presence (WeChat, Weibo, Douyin content,
            RedNote notes) without the full commerce operation behind it. TheRedScroll is
            our sister agency built for exactly that. Daily content, community and KOL
            seeding, without a storefront sitting underneath.
          </p>
        </header>

        <div class="cbs-sister cbs-reveal">
          <div class="cbs-sister__hero">
            <div class="cbs-sister__logo">
              <img src="/Images/AgencyLogos/TheredScroll%20PNG.webp" alt="TheRedScroll logo" loading="lazy" decoding="async" />
            </div>
            <span class="cbs-sister__chip">Sister product</span>
            <h3 class="cbs-sister__title">Off-platform social, run by the same operators.</h3>
            <p class="cbs-sister__desc">
              When the store is not the priority but the brand still needs to show up in
              the feed, TheRedScroll handles the content and the community. Same group,
              same standards, separate brand, smaller monthly retainer.
            </p>
            <ul class="cbs-sister__stats">
              <li>
                <span class="cbs-sister__stat-num">4</span>
                <span class="cbs-sister__stat-label">channels covered: WeChat, Weibo, Douyin, RedNote</span>
              </li>
              <li>
                <span class="cbs-sister__stat-num">25+</span>
                <span class="cbs-sister__stat-label">years of Chinese social experience inside the group</span>
              </li>
              <li>
                <span class="cbs-sister__stat-num">In-house</span>
                <span class="cbs-sister__stat-label">native team, content shot in studio</span>
              </li>
            </ul>
            <div class="cbs-sister__actions">
              <a href="https://theredscroll.com" target="_blank" rel="noopener" class="btn cbs-sister__btn-primary">
                Visit TheRedScroll
                <span aria-hidden="true">↗</span>
              </a>
              <a href="/contact" class="btn cbs-sister__btn-ghost">Brief us on your social plan</a>
            </div>
          </div>

          <div class="cbs-sister__packs">
            {sisterPackages.map((p) => (
              <article class="cbs-sister__pack">
                <span class="cbs-sister__pack-icon" aria-hidden="true" set:html={icons.scroll} />
                <h4>{p.title}</h4>
                <p>{p.desc}</p>
              </article>
            ))}
          </div>
        </div>
      </div>
    </section>

    {/* ---------- PROOF: Camper mini-case ---------- */}
    <section class="section section--tight">
      <div class="container container--narrow">
        <article class="cbs-proof cbs-reveal" data-reveal>
          <div class="cbs-proof__head">
            <span class="eyebrow">Recent work</span>
            <h2>What this looks like running for a brand.</h2>
            <p class="cbs-proof__lead">
              Eighteen months of WeChat, Weibo and RED for Camper. Craft-led storytelling,
              design partnerships and a tight micro-influencer roster. Eight-figure RMB
              retail in tier-one cities followed the audience growth.
            </p>
          </div>
          <ul class="cbs-proof__stats" role="list">
            <li>
              <span class="cbs-proof__num">43k → 187k</span>
              <span class="cbs-proof__label">Combined followers, three platforms</span>
            </li>
            <li>
              <span class="cbs-proof__num">+330%</span>
              <span class="cbs-proof__label">Audience growth across WeChat, Weibo, RED</span>
            </li>
            <li>
              <span class="cbs-proof__num">38%</span>
              <span class="cbs-proof__label">Of eCommerce traffic came from RED alone</span>
            </li>
          </ul>
          <div class="cbs-proof__cta">
            <a href="/work/camper" class="btn btn--secondary">Read the Camper case</a>
            <a href="/work" class="btn btn--ghost btn--sm">See all case studies</a>
          </div>
        </article>
      </div>
    </section>

    {/* ---------- FAQ ---------- */}
    <section id="faq" class="section">
      <div class="container container--narrow">
        <header class="section-header section-header--center cbs-reveal">
          <span class="eyebrow">FAQ</span>
          <h2>Twenty questions, answered straight</h2>
          <p class="section-header__lead">
            The questions we get on the first call, with the answers we give once we know
            the brand. Filter by topic.
          </p>
        </header>

        <div class="cbs-faq-filter cbs-reveal" data-faq-filter role="tablist" aria-label="Filter FAQs by category">
          {faqCategories.map((c, i) => (
            <button
              type="button"
              class={`cbs-faq-chip${i === 0 ? ' is-active' : ''}`}
              role="tab"
              aria-selected={i === 0 ? 'true' : 'false'}
              data-faq-cat={c}
            >
              <span class="cbs-faq-chip__label">{c}</span>
              <span class="cbs-faq-chip__count">{c === 'All' ? faqs.length : faqs.filter((f) => f.cat === c).length}</span>
            </button>
          ))}
        </div>

        <div class="cbs-faq" data-faq-list>
          {faqs.map((f, i) => (
            <details
              class="cbs-faq__item cbs-reveal"
              data-faq-cat={f.cat}
              open={i === 0}
              style={`--reveal-delay: ${Math.min(i, 6) * 40}ms;`}
            >
              <summary class="cbs-faq__q">
                <span class="cbs-faq__cat-tag">{f.cat}</span>
                <span class="cbs-faq__q-text">{f.q}</span>
                <span class="cbs-faq__icon" aria-hidden="true">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
                </span>
              </summary>
              <div class="cbs-faq__a">
                <p>{f.a}</p>
              </div>
            </details>
          ))}
        </div>
      </div>
    </section>

    {/* ---------- FINAL CTA ---------- */}
    <section class="section section--tight">
      <div class="container container--narrow">
        <div class="cbs-cta">
          <span class="eyebrow eyebrow--on-dark">Let's talk</span>
          <h2 class="cbs-cta__title">Ready to sell where China actually shops?</h2>
          <p class="cbs-cta__lead">
            Tell us the category, the budget range, the platforms you are considering and
            whether you are aiming at a festival window or a steady-state launch. A senior
            member of the team replies within one working day. You will not be passed
            through a sales funnel or an auto-responder.
          </p>
          <div class="cbs-cta__actions">
            <a href="/contact" class="btn cbs-cta__btn-primary">Start the conversation</a>
            <a href="/work" class="btn cbs-cta__btn-ghost">See our case studies</a>
          </div>
        </div>
      </div>
    </section>
  </article>

  <style is:global>
    /* ---------- Container narrow helper ---------- */
    .cbs .container--narrow { max-width: 880px; }

    /* ---------- REVEAL + MOTION ---------- */
    @media (prefers-reduced-motion: no-preference) {
      .cbs-reveal {
        opacity: 0;
        transform: translateY(16px);
        transition:
          opacity 700ms cubic-bezier(0.16, 1, 0.3, 1) var(--reveal-delay, 0ms),
          transform 700ms cubic-bezier(0.16, 1, 0.3, 1) var(--reveal-delay, 0ms);
      }
      .cbs-reveal.is-in { opacity: 1; transform: translateY(0); }
      .cbs-magnetic {
        will-change: transform;
        transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1),
                    box-shadow 220ms ease, background-color 200ms ease, color 200ms ease;
      }
    }

    /* ---------- SCROLL PROGRESS BAR ---------- */
    .cbs-progress {
      position: fixed; top: 0; left: 0; right: 0;
      height: 3px; z-index: 1000;
      background: transparent; pointer-events: none;
    }
    .cbs-progress__bar {
      height: 100%; width: var(--progress, 0%);
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 60%, #9DDBF5 100%);
      box-shadow: 0 0 12px rgba(38, 154, 218, 0.55);
      transition: width 80ms linear;
    }

    /* ---------- HERO ---------- */
    .cbs-hero {
      position: relative;
      min-height: clamp(360px, 48vh, 500px);
      display: flex; align-items: flex-start;
      padding: clamp(8px, 1.25vh, 16px) 0 clamp(48px, 7vh, 80px);
      color: var(--bbg-white);
      overflow: hidden; isolation: isolate;
    }
    body:has(.cbs-hero) .cbs-hero { padding-top: clamp(112px, 10vh, 144px); }
    @media (max-width: 1024px) {
      body:has(.cbs-hero) .cbs-hero { padding-top: clamp(96px, 9vh, 116px); }
    }
    .cbs-hero__bg { position: absolute; inset: -10% 0 0; z-index: -3; }
    .cbs-hero__bg img {
      width: 100%; height: 110%;
      object-fit: cover; object-position: center;
      transform: translate3d(0, var(--parallax-y, 0px), 0) scale(1.04);
      transition: transform 80ms linear;
      filter: saturate(0.95);
    }
    .cbs-hero__overlay {
      position: absolute; inset: 0; z-index: -2;
      background:
        linear-gradient(110deg,
          rgba(8, 18, 28, 0.92) 0%,
          rgba(8, 18, 28, 0.72) 42%,
          rgba(8, 18, 28, 0.32) 100%),
        radial-gradient(120% 80% at 0% 100%,
          rgba(38, 154, 218, 0.28) 0%,
          rgba(38, 154, 218, 0) 60%);
    }
    .cbs-hero__grain {
      position: absolute; inset: 0; z-index: -1;
      background-image: radial-gradient(rgba(255, 255, 255, 0.06) 1px, transparent 1px);
      background-size: 3px 3px;
      mix-blend-mode: overlay; opacity: 0.45;
      pointer-events: none;
    }
    .cbs-hero__inner { max-width: 980px; position: relative; }
    .cbs-hero__crumbs {
      margin-bottom: var(--space-4);
      font-size: var(--fs-small);
      display: flex; gap: 0.5rem; align-items: center;
      color: rgba(255, 255, 255, 0.7);
    }
    .cbs-hero__crumbs a { color: rgba(255, 255, 255, 0.85); font-weight: 500; }
    .cbs-hero__crumbs a:hover { color: var(--bbg-white); }
    .cbs-hero__inner .eyebrow--on-dark {
      display: inline-block;
      margin-bottom: var(--space-3);
      color: #9DDBF5;
    }
    .cbs-hero__title {
      font-size: clamp(2rem, 3.4vw + 1rem, 3.75rem);
      line-height: 1.06;
      letter-spacing: -0.025em;
      color: var(--bbg-white);
      margin: 0 0 var(--space-4);
      max-width: 22ch;
      font-weight: 700;
      display: flex; flex-direction: column;
      gap: 0.05em;
    }
    .cbs-hero__title-row { display: block; }
    .cbs-hero__title-row--accent {
      color: #9DDBF5; font-weight: 500;
      font-style: italic; letter-spacing: -0.018em;
    }
    .cbs-hero__title-row--highlight {
      position: relative; display: inline-block;
      align-self: flex-start; padding: 0 0.15em;
    }
    .cbs-hero__title-row--highlight::before {
      content: ""; position: absolute;
      left: 0; right: 0; bottom: 0.04em;
      height: 0.18em;
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      border-radius: 2px;
      transform-origin: left center;
      animation: cbs-underline 900ms cubic-bezier(0.16, 1, 0.3, 1) 600ms both;
      z-index: -1;
    }
    @keyframes cbs-underline { from { transform: scaleX(0); } to { transform: scaleX(1); } }
    .cbs-hero__lead {
      font-size: clamp(1rem, 0.5vw + 1rem, 1.1875rem);
      line-height: 1.55;
      color: rgba(255, 255, 255, 0.86);
      max-width: 58ch;
      margin: 0 0 var(--space-5);
    }
    .cbs-hero__actions { display: flex; flex-wrap: wrap; gap: var(--space-3); margin-bottom: var(--space-6); }
    .cbs-magnetic:hover { transform: translateY(-2px); }

    .cbs-hero__meta {
      display: flex; flex-wrap: nowrap;
      gap: clamp(var(--space-3), 3vw, var(--space-6));
      padding-top: var(--space-5);
      border-top: 1px solid rgba(255, 255, 255, 0.14);
      max-width: none;
    }
    .cbs-hero__meta-item {
      display: flex; flex-direction: column;
      gap: 0.2rem;
      min-width: 0;
      flex: 1 1 0;
    }
    @media (max-width: 720px) {
      .cbs-hero__meta { flex-wrap: wrap; }
      .cbs-hero__meta-item { flex: 1 1 auto; }
    }
    .cbs-hero__meta-num {
      font-size: clamp(1.625rem, 1.2vw + 1rem, 2.125rem);
      font-weight: 700; letter-spacing: -0.02em;
      line-height: 1; color: var(--bbg-white);
      font-feature-settings: "tnum";
    }
    .cbs-hero__meta-label {
      font-size: 0.85rem;
      color: rgba(255, 255, 255, 0.66);
      letter-spacing: 0.02em;
    }

    .cbs-hero__scroll {
      position: absolute;
      bottom: var(--space-5);
      right: clamp(var(--space-5), 4vw, var(--space-7));
      display: flex; flex-direction: column;
      align-items: center; gap: 0.5rem;
      color: rgba(255, 255, 255, 0.7);
      font-size: 0.7rem; letter-spacing: 0.18em;
      text-transform: uppercase; font-weight: 600;
      transition: color 200ms ease;
    }
    .cbs-hero__scroll:hover { color: var(--bbg-white); }
    .cbs-hero__scroll-line {
      width: 1px; height: 40px;
      background: linear-gradient(180deg, transparent 0%, rgba(255, 255, 255, 0.6) 100%);
      position: relative; overflow: hidden;
    }
    .cbs-hero__scroll-line::after {
      content: ""; position: absolute;
      top: -100%; left: 0; right: 0;
      height: 50%;
      background: linear-gradient(180deg, transparent 0%, var(--bbg-white) 100%);
      animation: cbs-scroll-tick 1.8s ease-in-out infinite;
    }
    @keyframes cbs-scroll-tick { 0% { top: -50%; } 100% { top: 100%; } }
    @media (max-width: 720px) {
      .cbs-hero__scroll { display: none; }
      .cbs-hero__meta { gap: var(--space-4); }
    }


    /* ---------- VALUE + PROMISES ---------- */
    .cbs-value {
      display: grid;
      grid-template-columns: 1.1fr 1fr;
      gap: var(--space-8);
      align-items: start;
    }
    .cbs-value__copy h2 {
      font-size: clamp(1.75rem, 2vw + 1rem, 2.5rem);
      line-height: 1.15; letter-spacing: -0.015em;
      margin: var(--space-3) 0 var(--space-4);
    }
    .cbs-value__copy p {
      color: var(--bbg-text); line-height: 1.7;
      font-size: 1.0625rem; margin: 0 0 var(--space-4);
    }
    .cbs-value__copy .btn { margin-top: var(--space-4); }
    .cbs-promise {
      list-style: none; padding: var(--space-6); margin: 0;
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      display: flex; flex-direction: column;
      gap: var(--space-5);
    }
    .cbs-promise > li:first-child .eyebrow { margin-bottom: 0; }
    .cbs-promise__item {
      display: grid;
      grid-template-columns: 32px 1fr;
      gap: var(--space-4);
      align-items: start;
    }
    .cbs-promise__check {
      width: 32px; height: 32px;
      border-radius: 50%;
      background: var(--bbg-blue-light);
      color: var(--bbg-blue);
      display: inline-flex; align-items: center; justify-content: center;
      flex-shrink: 0;
    }
    .cbs-promise__check svg { width: 16px; height: 16px; }
    .cbs-promise__item h4 {
      font-size: 1.125rem; margin: 0 0 var(--space-2);
      letter-spacing: -0.005em; color: var(--bbg-ink);
    }
    .cbs-promise__item p {
      font-size: 0.975rem; line-height: 1.55;
      color: var(--bbg-text); margin: 0;
    }

    @media (max-width: 1024px) {
      .cbs-value { grid-template-columns: 1fr; gap: var(--space-6); }
    }

    /* ---------- SERVICES (six-card grid) ---------- */
    .cbs-services {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: var(--space-5);
    }
    .cbs-service {
      position: relative;
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      padding: var(--space-6);
      display: flex; flex-direction: column;
      gap: var(--space-3);
      text-decoration: none;
      color: inherit;
      transition:
        transform 320ms cubic-bezier(0.16, 1, 0.3, 1),
        border-color 260ms ease,
        box-shadow 260ms ease;
      overflow: hidden;
    }
    .cbs-service::before {
      content: "";
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      transform: scaleX(0);
      transform-origin: left center;
      transition: transform 360ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .cbs-service:hover {
      transform: translateY(-4px);
      border-color: var(--bbg-blue);
      box-shadow: 0 16px 32px -20px rgba(38, 154, 218, 0.4);
    }
    .cbs-service:hover::before { transform: scaleX(1); }
    .cbs-service__icon {
      display: inline-flex;
      width: 44px; height: 44px;
      align-items: center; justify-content: center;
      border-radius: 12px;
      background: linear-gradient(135deg, var(--bbg-blue-light) 0%, rgba(38, 154, 218, 0.18) 100%);
      color: var(--bbg-blue);
      margin-bottom: var(--space-2);
    }
    .cbs-service__icon svg { width: 22px; height: 22px; }
    .cbs-service__title {
      font-size: 1.125rem;
      line-height: 1.25;
      margin: 0;
      color: var(--bbg-ink);
      letter-spacing: -0.005em;
    }
    .cbs-service__body {
      font-size: 0.95rem;
      line-height: 1.6;
      color: var(--bbg-text);
      margin: 0;
    }
    .cbs-service__arrow {
      margin-top: auto;
      align-self: flex-start;
      display: inline-flex;
      align-items: center; gap: 0.4rem;
      font-size: 0.85rem; font-weight: 600;
      color: var(--bbg-blue);
      letter-spacing: 0.01em;
      padding-top: var(--space-2);
    }
    .cbs-service__arrow-icon {
      display: inline-block;
      transition: transform 220ms cubic-bezier(0.2, 0.8, 0.2, 1);
    }
    .cbs-service:hover .cbs-service__arrow-icon { transform: translateX(3px); }
    @media (max-width: 1024px) { .cbs-services { grid-template-columns: repeat(2, 1fr); } }
    @media (max-width: 640px) { .cbs-services { grid-template-columns: 1fr; } }

    /* ---------- MODELS / TABS BG ---------- */
    .cbs-models-section { position: relative; isolation: isolate; overflow: hidden; }

    /* ---------- TABS ---------- */
    .cbs-tabs {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 16px 48px -28px rgba(8, 18, 28, 0.18);
    }
    .cbs-tabs__nav {
      position: relative;
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 0;
      background: linear-gradient(180deg, var(--bbg-surface) 0%, var(--bbg-white) 100%);
      border-bottom: 1px solid var(--bbg-border);
    }
    .cbs-tabs--two .cbs-tabs__nav { grid-template-columns: repeat(2, 1fr); }
    .cbs-tabs__tab {
      position: relative; background: transparent; border: none;
      padding: var(--space-5) var(--space-4);
      text-align: left; cursor: pointer;
      display: flex; flex-direction: column;
      gap: 0.35rem;
      color: var(--bbg-text-muted);
      transition: color 240ms ease, background 240ms ease;
      border-right: 1px solid var(--bbg-border);
      font-family: inherit;
    }
    .cbs-tabs__tab:last-child { border-right: none; }
    .cbs-tabs__tab:hover { color: var(--bbg-ink); background: rgba(38, 154, 218, 0.04); }
    .cbs-tabs__tab.is-active { color: var(--bbg-ink); background: var(--bbg-white); }
    .cbs-tabs__tab-label {
      font-size: 1.0625rem; font-weight: 700;
      letter-spacing: -0.005em; line-height: 1.2;
    }
    .cbs-tabs__tab-tag {
      font-size: 0.78rem; color: var(--bbg-text-muted);
      line-height: 1.35;
    }
    .cbs-tabs__tab.is-active .cbs-tabs__tab-tag { color: var(--bbg-blue); }
    .cbs-tabs__tab-pop {
      position: absolute;
      top: var(--space-3); right: var(--space-3);
      font-size: 0.65rem; font-weight: 700;
      letter-spacing: 0.08em; text-transform: uppercase;
      color: var(--bbg-blue);
      background: var(--bbg-blue-light);
      padding: 2px 8px;
      border-radius: 999px;
    }
    .cbs-tabs__indicator {
      position: absolute;
      bottom: -1px; left: 0;
      width: 25%; height: 3px;
      background: linear-gradient(90deg, var(--bbg-blue) 0%, #5BC2F0 100%);
      border-radius: 2px 2px 0 0;
      transform: translateX(0);
      transition: transform 360ms cubic-bezier(0.16, 1, 0.3, 1);
      pointer-events: none;
    }
    .cbs-tabs--two .cbs-tabs__indicator { width: 50%; }
    .cbs-tabs__panels { background: var(--bbg-white); }
    .cbs-tabs__panel {
      padding: clamp(var(--space-5), 4vw + 1rem, var(--space-7));
      animation: cbs-tab-in 360ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    @keyframes cbs-tab-in {
      from { opacity: 0; transform: translateY(8px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .cbs-tabs__panel[hidden] { display: none; }
    .cbs-tabs__panel-grid {
      display: grid;
      grid-template-columns: 1.1fr 1fr;
      gap: clamp(var(--space-5), 3vw, var(--space-7));
      align-items: start;
    }
    .cbs-tabs__lead { display: flex; flex-direction: column; gap: var(--space-4); }
    .cbs-tabs__pillset { display: flex; flex-wrap: wrap; gap: 0.5rem; }
    .cbs-tabs__pill {
      font-size: 0.78rem; font-weight: 600;
      padding: 0.35em 0.85em;
      border-radius: 999px;
      background: var(--bbg-blue-light);
      color: var(--bbg-blue);
      letter-spacing: 0.01em;
    }
    .cbs-tabs__title {
      font-size: clamp(1.625rem, 1.8vw + 1rem, 2.25rem);
      line-height: 1.1; margin: 0;
      letter-spacing: -0.015em;
    }
    .cbs-tabs__desc {
      font-size: 1rem; line-height: 1.6;
      color: var(--bbg-text); margin: 0;
    }
    .cbs-tabs__when {
      padding: var(--space-4);
      background: var(--bbg-surface);
      border-radius: var(--radius-md);
      border-left: 3px solid var(--bbg-blue);
    }
    .cbs-tabs__when-label {
      display: block;
      font-size: 0.72rem; font-weight: 700;
      letter-spacing: 0.1em; text-transform: uppercase;
      color: var(--bbg-blue);
      margin-bottom: 0.35rem;
    }
    .cbs-tabs__when p { margin: 0; font-size: 0.95rem; line-height: 1.55; color: var(--bbg-text); }
    .cbs-tabs__body { display: flex; flex-direction: column; gap: var(--space-4); }
    .cbs-tabs__image {
      margin: 0;
      aspect-ratio: 16 / 10;
      overflow: hidden;
      border-radius: var(--radius-md);
      background: var(--bbg-border);
    }
    .cbs-tabs__image img {
      width: 100%; height: 100%;
      object-fit: cover; display: block;
    }
    .cbs-tabs__list-wrap {
      background: var(--bbg-surface);
      border-radius: var(--radius-md);
      padding: var(--space-5);
    }
    .cbs-tabs__list-wrap--alt { background: var(--bbg-blue-light); }
    .cbs-tabs__list-label {
      display: block;
      font-size: 0.72rem; font-weight: 700;
      letter-spacing: 0.1em; text-transform: uppercase;
      color: var(--bbg-text-muted);
      margin-bottom: var(--space-3);
    }
    .cbs-tabs__list-wrap--alt .cbs-tabs__list-label { color: var(--bbg-blue-dark); }
    .cbs-tabs__list {
      list-style: none; padding: 0; margin: 0;
      display: flex; flex-direction: column;
      gap: var(--space-3);
    }
    .cbs-tabs__list li {
      display: grid;
      grid-template-columns: 24px 1fr;
      gap: 0.75rem;
      align-items: start;
      font-size: 0.95rem; line-height: 1.5;
      color: var(--bbg-text);
    }
    .cbs-tabs__check {
      width: 22px; height: 22px;
      border-radius: 50%;
      background: var(--bbg-blue);
      color: var(--bbg-white);
      display: inline-flex; align-items: center; justify-content: center;
      flex-shrink: 0; margin-top: 1px;
    }
    .cbs-tabs__check--alt {
      background: var(--bbg-white);
      color: var(--bbg-blue);
      box-shadow: inset 0 0 0 1.5px var(--bbg-blue);
    }
    .cbs-tabs__check svg { width: 12px; height: 12px; }

    .cbs-owns {
      padding: var(--space-4);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-md);
      background: var(--bbg-white);
    }
    .cbs-owns__label {
      display: block;
      font-size: 0.72rem; font-weight: 700;
      letter-spacing: 0.1em; text-transform: uppercase;
      color: var(--bbg-text-muted);
      margin-bottom: var(--space-3);
    }
    .cbs-owns__list {
      list-style: none; padding: 0; margin: 0;
      display: flex; flex-direction: column;
      gap: 0.6rem;
    }
    .cbs-owns__list li {
      display: grid;
      grid-template-columns: minmax(150px, auto) 1fr;
      gap: var(--space-4);
      align-items: baseline;
      padding-bottom: 0.6rem;
      border-bottom: 1px dashed var(--bbg-border);
      font-size: 0.9rem;
      line-height: 1.45;
    }
    .cbs-owns__list li:last-child { border-bottom: none; padding-bottom: 0; }
    .cbs-owns__key { color: var(--bbg-text-muted); font-weight: 600; }
    .cbs-owns__val { color: var(--bbg-ink); }

    @media (max-width: 1024px) {
      .cbs-tabs__nav { grid-template-columns: 1fr; }
      .cbs-tabs__tab { border-right: none; border-bottom: 1px solid var(--bbg-border); }
      .cbs-tabs__indicator { display: none; }
      .cbs-tabs__panel-grid { grid-template-columns: 1fr; }
    }

    /* ---------- COMPARISON TABLE ---------- */
    .cbs-compare {
      margin-top: var(--space-8);
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      padding: clamp(var(--space-5), 3vw + 1rem, var(--space-7));
      box-shadow: 0 1px 0 rgba(0, 0, 0, 0.02);
    }
    .cbs-compare__intro { text-align: center; margin-bottom: var(--space-6); }
    .cbs-compare__intro h3 {
      font-size: clamp(1.375rem, 1.2vw + 1rem, 1.75rem);
      margin: var(--space-2) 0 var(--space-3);
      letter-spacing: -0.01em;
    }
    .cbs-compare__intro p {
      color: var(--bbg-text);
      max-width: 60ch; margin: 0 auto;
      line-height: 1.6; font-size: 1rem;
    }
    .cbs-compare__table-wrap { overflow-x: auto; }
    .cbs-compare__table {
      width: 100%; border-collapse: separate;
      border-spacing: 0;
      min-width: 640px;
    }
    .cbs-compare__head {
      background: var(--bbg-surface);
      padding: var(--space-4);
      text-align: left;
      vertical-align: top;
      border-bottom: 1px solid var(--bbg-border);
    }
    .cbs-compare__head--tp { border-bottom-color: var(--bbg-blue); }
    .cbs-compare__head--dist { border-bottom-color: var(--bbg-blue-dark); }
    .cbs-compare__head-label {
      display: block;
      font-size: 1rem; font-weight: 700;
      color: var(--bbg-ink);
      letter-spacing: -0.005em;
    }
    .cbs-compare__head-sub {
      display: block;
      font-size: 0.78rem;
      color: var(--bbg-text-muted);
      margin-top: 0.25rem;
    }
    .cbs-compare__row-head-col {
      width: 38%;
      background: var(--bbg-surface);
      border-bottom: 1px solid var(--bbg-border);
    }
    .cbs-compare__row-label {
      text-align: left;
      padding: var(--space-4);
      font-size: 0.92rem;
      font-weight: 600;
      color: var(--bbg-text-muted);
      letter-spacing: 0.01em;
      border-bottom: 1px solid var(--bbg-border);
      background: var(--bbg-white);
    }
    .cbs-compare__cell {
      padding: var(--space-4);
      font-size: 0.95rem;
      color: var(--bbg-ink);
      border-bottom: 1px solid var(--bbg-border);
      vertical-align: top;
    }
    .cbs-compare__cell--tp { background: rgba(38, 154, 218, 0.04); }
    .cbs-compare__cell--dist { background: var(--bbg-white); }
    .cbs-compare__table tbody tr:last-child .cbs-compare__row-label,
    .cbs-compare__table tbody tr:last-child .cbs-compare__cell {
      border-bottom: none;
    }
    .cbs-compare__note {
      margin: var(--space-5) auto 0;
      max-width: 70ch;
      text-align: center;
      color: var(--bbg-text);
      line-height: 1.65;
      font-size: 0.975rem;
    }

    /* ---------- QUOTE ---------- */
    .cbs-quote {
      margin: var(--space-6) auto 0;
      max-width: 760px;
      padding: var(--space-5) var(--space-6);
      background: linear-gradient(135deg, var(--bbg-blue-light) 0%, rgba(38, 154, 218, 0.10) 100%);
      border-left: 4px solid var(--bbg-blue);
      border-radius: 0 var(--radius-md) var(--radius-md) 0;
    }
    .cbs-quote p {
      margin: 0;
      font-size: 1.0625rem;
      line-height: 1.55;
      color: var(--bbg-ink);
      font-weight: 500;
      font-style: italic;
    }
    .cbs-quote--inline {
      margin: var(--space-2) 0;
      max-width: none;
      padding: var(--space-4) var(--space-5);
    }

    /* ---------- SISTER PRODUCT (TheRedScroll) ---------- */
    .cbs-sister {
      display: grid;
      grid-template-columns: 1.1fr 1fr;
      gap: var(--space-6);
      align-items: stretch;
    }
    .cbs-sister__hero {
      position: relative;
      padding: clamp(var(--space-6), 4vw, var(--space-8));
      background: linear-gradient(160deg, #1a0f1f 0%, #2a1530 60%, #401a45 100%);
      color: var(--bbg-white);
      border-radius: 24px;
      overflow: hidden;
      isolation: isolate;
      display: flex; flex-direction: column;
      gap: var(--space-4);
    }
    .cbs-sister__hero::before {
      content: "";
      position: absolute; inset: 0; z-index: -1;
      background:
        radial-gradient(60% 80% at 100% 0%, rgba(255, 90, 130, 0.25) 0%, transparent 60%),
        radial-gradient(40% 60% at 0% 100%, rgba(255, 60, 100, 0.18) 0%, transparent 60%);
    }
    .cbs-sister__logo {
      align-self: flex-start;
      background: rgba(255, 255, 255, 0.95);
      border-radius: 10px;
      padding: 0.55rem 0.9rem;
      max-width: 180px;
    }
    .cbs-sister__logo img {
      display: block; width: 100%; height: auto;
    }
    .cbs-sister__chip {
      display: inline-block;
      align-self: flex-start;
      font-size: 0.7rem; font-weight: 700;
      letter-spacing: 0.12em; text-transform: uppercase;
      color: #FFC2D1;
      background: rgba(255, 90, 130, 0.18);
      padding: 0.4em 0.85em;
      border-radius: 999px;
      border: 1px solid rgba(255, 194, 209, 0.3);
    }
    .cbs-sister__title {
      font-size: clamp(1.5rem, 1.6vw + 1rem, 2.125rem);
      line-height: 1.15;
      margin: 0;
      letter-spacing: -0.015em;
      color: var(--bbg-white);
    }
    .cbs-sister__desc {
      font-size: 1rem; line-height: 1.6;
      color: rgba(255, 255, 255, 0.82);
      margin: 0;
    }
    .cbs-sister__stats {
      list-style: none; padding: var(--space-4) 0 0;
      margin: 0;
      display: flex; flex-direction: column;
      gap: var(--space-3);
      border-top: 1px solid rgba(255, 255, 255, 0.14);
    }
    .cbs-sister__stats li {
      display: grid;
      grid-template-columns: minmax(96px, auto) 1fr;
      gap: var(--space-4);
      align-items: baseline;
    }
    .cbs-sister__stat-num {
      font-size: 1.25rem;
      font-weight: 700;
      color: #FFC2D1;
      letter-spacing: -0.01em;
      font-feature-settings: "tnum";
    }
    .cbs-sister__stat-label {
      font-size: 0.9rem;
      color: rgba(255, 255, 255, 0.78);
      line-height: 1.5;
    }
    .cbs-sister__actions {
      display: flex; flex-wrap: wrap; gap: var(--space-3);
      margin-top: var(--space-3);
    }
    .cbs-sister__btn-primary {
      background: var(--bbg-white);
      color: #2a1530;
      border: 1px solid var(--bbg-white);
      display: inline-flex; align-items: center; gap: 0.4rem;
    }
    .cbs-sister__btn-primary:hover {
      background: #FFC2D1; color: #2a1530;
    }
    .cbs-sister__btn-ghost {
      background: transparent;
      border: 1px solid rgba(255, 255, 255, 0.5);
      color: var(--bbg-white);
    }
    .cbs-sister__btn-ghost:hover {
      background: rgba(255, 255, 255, 0.12);
      border-color: var(--bbg-white);
      color: var(--bbg-white);
    }
    .cbs-sister__packs {
      display: flex; flex-direction: column;
      gap: var(--space-4);
    }
    .cbs-sister__pack {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-lg);
      padding: var(--space-5);
      display: flex; flex-direction: column;
      gap: var(--space-2);
      transition: border-color 280ms ease, box-shadow 280ms ease, transform 280ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .cbs-sister__pack:hover {
      transform: translateY(-3px);
      border-color: var(--bbg-blue);
      box-shadow: 0 14px 28px -18px rgba(38, 154, 218, 0.35);
    }
    .cbs-sister__pack-icon {
      display: inline-flex;
      width: 36px; height: 36px;
      align-items: center; justify-content: center;
      border-radius: 10px;
      background: var(--bbg-blue-light);
      color: var(--bbg-blue);
      margin-bottom: 0.4rem;
    }
    .cbs-sister__pack-icon svg { width: 18px; height: 18px; }
    .cbs-sister__pack h4 {
      font-size: 1.0625rem;
      margin: 0;
      letter-spacing: -0.005em;
      color: var(--bbg-ink);
    }
    .cbs-sister__pack p {
      font-size: 0.92rem;
      line-height: 1.55;
      color: var(--bbg-text);
      margin: 0;
    }
    @media (max-width: 1024px) {
      .cbs-sister { grid-template-columns: 1fr; }
    }

    /* ---------- FAQ ---------- */
    .cbs-faq {
      display: flex; flex-direction: column;
      gap: var(--space-3);
    }
    .cbs-faq__item {
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-md);
      overflow: hidden;
      transition: border-color 240ms ease, box-shadow 240ms ease, transform 280ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    .cbs-faq__item:hover {
      transform: translateX(2px);
      border-color: rgba(38, 154, 218, 0.4);
    }
    .cbs-faq__item[open] {
      border-color: var(--bbg-blue);
      box-shadow: 0 8px 22px -14px rgba(38, 154, 218, 0.4);
    }
    .cbs-faq__q {
      cursor: pointer;
      padding: var(--space-5);
      display: flex; align-items: center;
      justify-content: space-between;
      gap: var(--space-4);
      font-weight: 600;
      font-size: 1.0625rem; line-height: 1.3;
      color: var(--bbg-ink);
      list-style: none;
      transition: color var(--dur) var(--ease);
    }
    .cbs-faq__q::-webkit-details-marker { display: none; }
    .cbs-faq__q::marker { display: none; }
    .cbs-faq__item:hover .cbs-faq__q { color: var(--bbg-blue); }
    .cbs-faq__icon {
      flex-shrink: 0;
      width: 28px; height: 28px;
      display: inline-flex;
      align-items: center; justify-content: center;
      border-radius: 50%;
      background: var(--bbg-surface);
      color: var(--bbg-text-muted);
      transition: transform 320ms cubic-bezier(0.16, 1, 0.3, 1),
                  color var(--dur) var(--ease),
                  background var(--dur) var(--ease);
    }
    .cbs-faq__item[open] .cbs-faq__icon {
      transform: rotate(180deg);
      color: var(--bbg-white);
      background: var(--bbg-blue);
    }
    .cbs-faq__a {
      padding: 0 var(--space-5) var(--space-5);
      color: var(--bbg-text);
      line-height: 1.65;
      font-size: 1rem;
      animation: cbs-faq-in 320ms cubic-bezier(0.16, 1, 0.3, 1);
    }
    @keyframes cbs-faq-in {
      from { opacity: 0; transform: translateY(-4px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .cbs-faq__a p { margin: 0; }

    /* ---------- FAQ FILTER ---------- */
    .cbs-faq-filter {
      display: flex; flex-wrap: wrap;
      gap: 0.5rem; justify-content: center;
      margin-bottom: var(--space-6);
      overflow-x: auto;
    }
    .cbs-faq-chip {
      display: inline-flex; align-items: center;
      gap: 0.5rem;
      padding: 0.5rem 0.95rem;
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: 999px;
      cursor: pointer;
      font-size: 0.875rem;
      font-weight: 600;
      color: var(--bbg-text);
      transition: all 220ms ease;
      font-family: inherit;
      white-space: nowrap;
    }
    .cbs-faq-chip:hover { border-color: var(--bbg-blue); color: var(--bbg-blue); }
    .cbs-faq-chip.is-active {
      background: var(--bbg-blue);
      border-color: var(--bbg-blue);
      color: var(--bbg-white);
    }
    .cbs-faq-chip__count {
      font-size: 0.72rem; font-weight: 700;
      padding: 0.15em 0.5em;
      border-radius: 999px;
      background: var(--bbg-surface);
      color: var(--bbg-text-muted);
      transition: all 220ms ease;
    }
    .cbs-faq-chip:hover .cbs-faq-chip__count { background: var(--bbg-blue-light); color: var(--bbg-blue); }
    .cbs-faq-chip.is-active .cbs-faq-chip__count {
      background: rgba(255, 255, 255, 0.2);
      color: var(--bbg-white);
    }
    .cbs-faq__cat-tag {
      display: inline-block;
      font-size: 0.68rem; font-weight: 700;
      letter-spacing: 0.08em; text-transform: uppercase;
      color: var(--bbg-blue);
      background: var(--bbg-blue-light);
      padding: 0.25em 0.6em;
      border-radius: 4px;
      margin-right: var(--space-3);
      flex-shrink: 0;
    }
    .cbs-faq__q-text { flex-grow: 1; }
    .cbs-faq__item.is-hidden { display: none; }

    /* ---------- PROOF: Camper mini-case ---------- */
    .cbs-proof {
      padding: clamp(var(--space-6), 3vw + 1rem, var(--space-8));
      background: var(--bbg-surface);
      border: 1px solid var(--bbg-border);
      border-left: 6px solid var(--bbg-blue);
      border-radius: var(--radius-md);
    }
    .cbs-proof__head .eyebrow { color: var(--bbg-blue-dark); }
    .cbs-proof__head h2 {
      font-size: clamp(1.5rem, 1.5vw + 1rem, 2rem);
      line-height: 1.2;
      margin: var(--space-3) 0 var(--space-3);
      color: var(--bbg-ink);
    }
    .cbs-proof__lead {
      font-size: 1.05rem;
      line-height: 1.55;
      color: var(--bbg-text);
      max-width: 60ch;
    }
    .cbs-proof__stats {
      list-style: none;
      padding: 0;
      margin: var(--space-6) 0;
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: var(--space-4);
    }
    .cbs-proof__stats li {
      display: flex;
      flex-direction: column;
      gap: 6px;
      padding: var(--space-4) var(--space-4);
      background: var(--bbg-white);
      border: 1px solid var(--bbg-border);
      border-radius: var(--radius-sm);
    }
    .cbs-proof__num {
      font-size: clamp(1.25rem, 1vw + 1rem, 1.625rem);
      font-weight: 700;
      color: var(--bbg-blue-dark);
      line-height: 1.1;
      letter-spacing: -0.01em;
    }
    .cbs-proof__label {
      font-size: 0.85rem;
      color: var(--bbg-text-muted);
      line-height: 1.4;
    }
    .cbs-proof__cta {
      display: flex;
      flex-wrap: wrap;
      gap: var(--space-3);
      align-items: center;
    }
    @media (max-width: 720px) {
      .cbs-proof__stats { grid-template-columns: 1fr; }
    }

    /* ---------- FINAL CTA ---------- */
    .cbs-cta {
      padding: clamp(var(--space-6), 4vw + 1rem, var(--space-8)) var(--space-6);
      background: linear-gradient(135deg, var(--bbg-blue) 0%, var(--bbg-blue-dark) 100%);
      color: var(--bbg-white);
      border-radius: var(--radius-lg);
      text-align: center;
      position: relative; overflow: hidden;
    }
    .cbs-cta::before {
      content: ""; position: absolute;
      top: -40%; right: -10%;
      width: 420px; height: 420px;
      background: radial-gradient(circle at center, rgba(255,255,255,0.18) 0%, rgba(255,255,255,0) 65%);
      pointer-events: none;
    }
    .cbs-cta .eyebrow--on-dark {
      display: inline-block;
      margin-bottom: var(--space-4);
      color: rgba(255, 255, 255, 0.9);
    }
    .cbs-cta__title {
      font-size: clamp(1.75rem, 1.5vw + 1rem, 2.5rem);
      line-height: 1.15;
      color: var(--bbg-white);
      margin: 0 0 var(--space-4);
      letter-spacing: -0.015em;
    }
    .cbs-cta__lead {
      color: rgba(255, 255, 255, 0.92);
      font-size: 1.0625rem;
      max-width: 52ch;
      margin: 0 auto var(--space-5);
      line-height: 1.55;
    }
    .cbs-cta__actions {
      display: flex; flex-wrap: wrap;
      gap: var(--space-3);
      justify-content: center;
      position: relative;
    }
    .cbs-cta__btn-primary {
      background: var(--bbg-white);
      color: var(--bbg-blue-dark);
      border: 1px solid var(--bbg-white);
    }
    .cbs-cta__btn-primary:hover {
      background: rgba(255, 255, 255, 0.92);
      color: var(--bbg-blue-dark);
    }
    .cbs-cta__btn-ghost {
      background: transparent;
      border: 1px solid rgba(255, 255, 255, 0.5);
      color: var(--bbg-white);
    }
    .cbs-cta__btn-ghost:hover {
      background: rgba(255, 255, 255, 0.12);
      border-color: var(--bbg-white);
      color: var(--bbg-white);
    }
  </style>

  <script is:inline>
    (() => {
      const reduced = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

      // 1. Reveal-on-scroll
      const revealEls = document.querySelectorAll('.cbs-reveal');
      if (revealEls.length) {
        if (reduced || !('IntersectionObserver' in window)) {
          revealEls.forEach((el) => el.classList.add('is-in'));
        } else {
          const io = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add('is-in');
                io.unobserve(entry.target);
              }
            });
          }, { threshold: 0.12, rootMargin: '0px 0px -8% 0px' });
          revealEls.forEach((el) => io.observe(el));
        }
      }

      // 2. Animated counters
      const counters = document.querySelectorAll('[data-counter]');
      if (counters.length && !reduced && 'IntersectionObserver' in window) {
        const animate = (el) => {
          const target = parseFloat(el.dataset.counter || '0');
          const suffix = el.dataset.suffix || '';
          const duration = 1400;
          const start = performance.now();
          const tick = (now) => {
            const t = Math.min(1, (now - start) / duration);
            const eased = 1 - Math.pow(1 - t, 3);
            const value = Math.round(target * eased);
            el.textContent = value + suffix;
            if (t < 1) requestAnimationFrame(tick);
          };
          requestAnimationFrame(tick);
        };
        const counterIo = new IntersectionObserver((entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              animate(entry.target);
              counterIo.unobserve(entry.target);
            }
          });
        }, { threshold: 0.5 });
        counters.forEach((el) => counterIo.observe(el));
      } else {
        counters.forEach((el) => {
          el.textContent = (el.dataset.counter || '0') + (el.dataset.suffix || '');
        });
      }

      // 3. Hero parallax + scroll progress bar
      const parallaxEl = document.querySelector('[data-parallax]');
      const progressEl = document.querySelector('.cbs-progress__bar');
      const heroEl = document.querySelector('[data-hero]');
      let ticking = false;
      const onScroll = () => {
        if (ticking) return;
        ticking = true;
        requestAnimationFrame(() => {
          const scrollY = window.scrollY || window.pageYOffset;
          if (parallaxEl && heroEl && !reduced) {
            const heroH = heroEl.offsetHeight;
            if (scrollY < heroH) {
              parallaxEl.style.setProperty('--parallax-y', (scrollY * 0.18) + 'px');
            }
          }
          if (progressEl) {
            const docH = document.documentElement.scrollHeight - window.innerHeight;
            const pct = docH > 0 ? Math.min(100, (scrollY / docH) * 100) : 0;
            progressEl.style.setProperty('--progress', pct + '%');
          }
          ticking = false;
        });
      };
      window.addEventListener('scroll', onScroll, { passive: true });
      onScroll();

      // 4. Tabs (handles both 2-tab models and 4-tab cadence)
      const openTabByHash = () => {
        const hash = window.location.hash.slice(1);
        if (!hash) return;
        document.querySelectorAll('[data-tabs]').forEach((root) => {
          const matchingTab = root.querySelector(`[data-tab-id="${hash}"]`);
          if (matchingTab) matchingTab.click();
        });
      };

      document.querySelectorAll('[data-tabs]').forEach((tabsRoot) => {
        const tabs = Array.from(tabsRoot.querySelectorAll('.cbs-tabs__tab'));
        const panels = Array.from(tabsRoot.querySelectorAll('.cbs-tabs__panel'));
        const indicator = tabsRoot.querySelector('.cbs-tabs__indicator');

        const setActive = (tab) => {
          tabs.forEach((t) => {
            const active = t === tab;
            t.classList.toggle('is-active', active);
            t.setAttribute('aria-selected', active ? 'true' : 'false');
          });
          const targetId = tab.getAttribute('data-tab-target');
          panels.forEach((p) => {
            const active = p.id === targetId;
            p.classList.toggle('is-active', active);
            if (active) p.removeAttribute('hidden');
            else p.setAttribute('hidden', '');
          });
          if (indicator) {
            const idx = tabs.indexOf(tab);
            const w = 100 / tabs.length;
            indicator.style.width = w + '%';
            indicator.style.transform = `translateX(${idx * 100}%)`;
          }
        };

        tabs.forEach((tab) => {
          tab.addEventListener('click', () => setActive(tab));
          tab.addEventListener('keydown', (e) => {
            const idx = tabs.indexOf(tab);
            if (e.key === 'ArrowRight') {
              e.preventDefault();
              const next = tabs[(idx + 1) % tabs.length];
              next.focus(); setActive(next);
            } else if (e.key === 'ArrowLeft') {
              e.preventDefault();
              const prev = tabs[(idx - 1 + tabs.length) % tabs.length];
              prev.focus(); setActive(prev);
            }
          });
        });

        const initialActive = tabs.find((t) => t.classList.contains('is-active')) || tabs[0];
        if (initialActive) setActive(initialActive);
      });

      // Open tab from URL hash on load and on hash change
      openTabByHash();
      window.addEventListener('hashchange', openTabByHash);

      // 5. Smooth scroll for anchors (also triggers tab open via hashchange)
      document.querySelectorAll('a[href^="#"]').forEach((link) => {
        link.addEventListener('click', (e) => {
          const id = link.getAttribute('href');
          if (!id || id === '#') return;
          const target = document.querySelector(id);
          if (!target) return;
          e.preventDefault();
          target.scrollIntoView({ behavior: reduced ? 'auto' : 'smooth', block: 'start' });
          const tabId = id.slice(1);
          document.querySelectorAll('[data-tabs]').forEach((root) => {
            const matchingTab = root.querySelector(`[data-tab-id="${tabId}"]`);
            if (matchingTab) matchingTab.click();
          });
          history.replaceState(null, '', id);
        });
      });

      // 6. Two-truths: cursor tilt + scroll-driven image parallax
      const truths = document.querySelectorAll('[data-truth-tilt]');
      if (truths.length && !reduced) {
        truths.forEach((card) => {
          const onMove = (e) => {
            const rect = card.getBoundingClientRect();
            const x = (e.clientX - rect.left) / rect.width;
            const y = (e.clientY - rect.top) / rect.height;
            const ry = (x - 0.5) * 5;
            const rx = (0.5 - y) * 4;
            card.style.setProperty('--ry', ry.toFixed(2) + 'deg');
            card.style.setProperty('--rx', rx.toFixed(2) + 'deg');
            card.setAttribute('data-tilt-active', '');
          };
          const onLeave = () => {
            card.style.setProperty('--ry', '0deg');
            card.style.setProperty('--rx', '0deg');
            card.removeAttribute('data-tilt-active');
          };
          card.addEventListener('mousemove', onMove);
          card.addEventListener('mouseleave', onLeave);
        });

        const parallaxImgs = document.querySelectorAll('[data-truth-parallax]');
        let truthsTicking = false;
        const updateTruthParallax = () => {
          parallaxImgs.forEach((img) => {
            const frame = img.parentElement;
            if (!frame) return;
            const rect = frame.getBoundingClientRect();
            const vh = window.innerHeight;
            if (rect.bottom < 0 || rect.top > vh) return;
            const center = rect.top + rect.height / 2;
            const offset = (center - vh / 2) / vh;
            const shift = Math.max(-16, Math.min(16, -offset * 22));
            img.style.setProperty('--img-y', shift.toFixed(1) + 'px');
          });
        };
        const onTruthsScroll = () => {
          if (truthsTicking) return;
          truthsTicking = true;
          requestAnimationFrame(() => {
            updateTruthParallax();
            truthsTicking = false;
          });
        };
        window.addEventListener('scroll', onTruthsScroll, { passive: true });
        window.addEventListener('resize', onTruthsScroll, { passive: true });
        updateTruthParallax();
      }

      // 7. FAQ accordion - close others on open
      const faqItems = document.querySelectorAll('.cbs-faq__item');
      faqItems.forEach((item) => {
        item.addEventListener('toggle', () => {
          if (item.open) {
            faqItems.forEach((other) => {
              if (other !== item && other.open) other.open = false;
            });
          }
        });
      });

      // 7. FAQ category filter
      const faqFilter = document.querySelector('[data-faq-filter]');
      const faqList = document.querySelector('[data-faq-list]');
      if (faqFilter && faqList) {
        const chips = Array.from(faqFilter.querySelectorAll('.cbs-faq-chip'));
        const items = Array.from(faqList.querySelectorAll('.cbs-faq__item'));
        chips.forEach((chip) => {
          chip.addEventListener('click', () => {
            const cat = chip.dataset.faqCat;
            chips.forEach((c) => {
              const on = c === chip;
              c.classList.toggle('is-active', on);
              c.setAttribute('aria-selected', on ? 'true' : 'false');
            });
            items.forEach((item) => {
              const matches = cat === 'All' || item.dataset.faqCat === cat;
              item.classList.toggle('is-hidden', !matches);
            });
          });
        });
      }
    })();
  </script>
</Layout>
